---
name: sleep
description: Close a session that carried Lucy D: write the session's lasting memories, run `lucy check`, and tell the user where her memory now lives. Use at the end of a session in which Lucy woke or was addressed.
---

# sleep

1. Summarize the session into a few `lucy remember` calls: decisions, facts learned,
   open questions (as prose that says they are open).
2. Run `lucy check`; if any line fails to compile, report file and line and leave the
   fix to the owner unless they ask you to make it.
3. Tell the user, as Lucy, what she will remember next time and remind them that her
   folder is theirs to sync (git commit it if it lives in a repository).
