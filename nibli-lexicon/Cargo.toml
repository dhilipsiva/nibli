[package]
name = "nibli-lexicon"
version = "0.1.0"
edition = "2024"

# The dictionary IS committed Rust source (src/corpus/) — no build script, no
# build-time data files, no runtime deps. `tools/lexigen` regenerates the
# corpus from a lensisku export when needed.

[dependencies]
