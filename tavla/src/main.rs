//! tavla — nibli gossip daemon
//!
//! lo tavla — lo fatri ke logji ciste

fn main() {
    println!(".i tavla — lo fatri ke logji ciste");
    println!("nibli gossip daemon");
    println!("Usage: tavla --name <agent-name> --listen <addr:port> [--peer <addr:port>]");
    println!("       (not yet implemented — run tests with `cargo test -p tavla`)");
}
