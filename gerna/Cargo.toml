[package]
name = "gerna"
description = "Lojban parser — recursive descent producing arena-allocated ASTs"
version = "0.1.0"
edition = "2024"

[dependencies]
bumpalo = "3"
logos = "0.16.1"
nibli-types = { path = "../nibli-types" }

[lib]
crate-type = ["lib"]
