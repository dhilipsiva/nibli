[package]
name = "smuni"
description = "Semantic compiler — Lojban AST to First-Order Logic IR"
version = "0.1.0"
edition = "2024"

[dependencies]
gerna = { path = "../gerna" }
lasso = "0.7.3"
nibli-types = { path = "../nibli-types" }
phf = "0.13.1"

[build-dependencies]
phf_codegen = "0.13.1"

[lib]
crate-type = ["lib"]
