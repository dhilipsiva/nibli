[package]
name = "nibli-types"
description = "Canonical type definitions shared across the pipeline"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]
