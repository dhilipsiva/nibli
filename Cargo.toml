[workspace]
members = [
    "gerna",
    "smuni",
    "logji",
    "gasnu",
    "lasna",
    "nibli"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
