[workspace]
members = [
    "gerna",
    "smuni",
    "logji",
    "gasnu",
    "lasna",
    "nibli",
    "nibli-ui",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
strip = true
