[package]
name = "lucy-core"
description = "Lucy D, Phase 1: the signed, append-only epistemic operation log's core types (canonical encoding, domain-separated identifiers, causal provenance, durable uncertainty)"
version.workspace = true
edition = "2024"
license.workspace = true
repository.workspace = true
homepage.workspace = true
# Tier Z (RELEASING.md): repo-only until Lucy has a release track of its own.
publish = false

[dependencies]
blake3 = "1"
ed25519-dalek = "2"
