# Nibli

A Zero-Hallucination Symbolic Reasoning Engine.

Nibli is a deterministic theorem prover compiled to WebAssembly (WASI P2). It compiles Lojban natural language syntax into First-Order Logic and performs inference via demand-driven backward-chaining over an indexed fact store. Every conclusion is formally derived — no hallucination, no approximation.

## Environment

- **OS:** Ubuntu on WSL2 (accessed from Windows via `\\wsl.localhost\Ubuntu\...`)
- **Dev shell:** Nix flake — all tools (rustc, cargo-component, wac, just, wasmtime) come from `flake.nix`
- **Enter dev shell:** `nix develop --extra-experimental-features nix-command --extra-experimental-features flakes`
- **Run commands from Windows side:** `wsl -d Ubuntu -e bash -lc "cd ~/projects/dhilipsiva/nibli && nix ... develop --command bash -c '<CMD>'"`
- **Set `CARGO_INCREMENTAL=0`** if running cargo from Windows side (filesystem lock issues)

## Build & Test

All commands must run inside the Nix dev shell. Use `just` as the primary task runner (see `Justfile`):

| Command | What it does |
|---------|-------------|
| `just run` | Full pipeline: clean WASM -> build lasna component -> launch REPL |
| `just check` | Fast type-check all workspace crates (`cargo check --workspace`) |
| `just test` | Run all unit tests (`cargo test --lib -- --nocapture --test-threads=1`) |
| `just test-engine` | Run nibli-engine integration tests (full pipeline: parse → compile → reason) |
| `just test-gerna` | Run gerna (parser) tests only |
| `just test-backend` | Run Python backend tests |
| `just test-tavla` | Run tavla gossip tests |
| `just test-all` | Run every test suite (unit + integration + Python + tavla) |
| `just build-wasm` | Build single lasna WASM component |
| `just build-gasnu` | Build native Wasmtime host gasnu (runner) |
| `just backend` | Start the Python reference compute backend (port 5555) |
| `just run-with-backend` | Build + run with `NIBLI_COMPUTE_ADDR=127.0.0.1:5555` |
| `just server` | Start GraphQL API server for Transparency Triad (port 8081) |
| `just ui` | Launch Transparency Triad web UI dev server (Dioxus, port 8080) |
| `just clean` | `cargo clean` |
| `just fuzz-parse [SECS]` | Fuzz gerna parser (requires `cargo +nightly`). Pass seconds to limit run time. |
| `just fuzz-assert [SECS]` | Fuzz nibli-engine assert_text (full pipeline) |
| `just fuzz-query [SECS]` | Fuzz nibli-engine assert + query (stateful KB) |

**Important:**
- Always use `cargo test --lib` (NOT `cargo test`) — cdylib linker chokes on WIT export symbols containing `@`
- **Regenerate WIT bindings:** `cargo component build` (bindings appear in each crate's `src/bindings.rs`)
  - Note: full build fails on `io-extras` crate (`#![feature]` on stable). Bindings still generate successfully before the failure.
- **REPL uses reedline** — does not work with piped stdin
- Logji (reasoning) tests require `--test-threads=1` (shared global state via RefCell). The Justfile handles this.

## Compute Backend

The gasnu (runner) acts as a TCP client to an external compute backend server via JSON Lines protocol.

- **Env var:** `NIBLI_COMPUTE_ADDR=host:port` — configures the backend address at startup
- **REPL command:** `:backend [host:port]` — show or change backend address at runtime
- **Protocol:** One JSON object per line. Request: `{"relation":"tenfa","args":[{"type":"number","value":8.0},...]}`. Response: `{"result":true}` or `{"error":"..."}`.
- **Fallback:** Built-in arithmetic (pilji/sumji/dilcu) always handled locally. Unknown predicates forward to external backend. If no backend configured, returns error.
- **Lazy connection:** TCP connects on first external dispatch, auto-reconnects on failure.
- **Reference server:** `python/nibli_backend.py` — handles pilji, sumji, dilcu, tenfa (exponent), dugri (log). Extend via `HANDLERS` dict.

## Architecture

Core component crates + runtime surfaces:

| Crate | Lojban meaning | Role | Key files |
|-------|---------------|------|-----------|
| `gerna` | grammar | Lojban text -> AST -> flat WIT buffer | `grammar.rs`, `ast.rs`, `lib.rs` (flattener), `lexer.rs` |
| `smuni` | meaning | Flat AST buffer -> FOL logic IR -> flat WIT logic buffer | `semantic.rs`, `ir.rs`, `lib.rs` (flattener) |
| `logji` | logic | FOL logic buffer -> backward-chaining assert/query | `lib.rs` (single file, all logic) |
| `lasna` | fasten/connect | Glue: chains gerna -> smuni -> logji | `lib.rs` |
| `gasnu` | agent/doer | Native Wasmtime host, REPL, external compute backend TCP client | `main.rs` |
| `nibli-engine` | — | Wasmtime engine wrapper library (shared by nibli-server, tavla) | `lib.rs` |
| `nibli-server` | — | GraphQL API server (async-graphql + axum, port 8081) | `main.rs` |
| `nibli-ui` | — | Dioxus web UI for Transparency Triad (browser, port 8080) | `main.rs` |
| `tavla` | talk | Gossip daemon: federated knowledge propagation over TCP/WebRTC | `lib.rs`, `main.rs` |
| `nibli-agent` | — | LLM-driven gossip peer that connects to `tavla` | `main.rs` |
| `nibli` | — | Native debug REPL and `nibli-validate` tooling | `main.rs`, `src/bin/validate.rs` |
| `python/` | — | Reference compute backend server (TCP + JSON Lines) | `nibli_backend.py` |

- **WIT interfaces:** `wit/world.wit` defines `ast-types` (gerna output), `logic-types` (FOL IR), `gerna`, `smuni`, `logji`, `compute-backend`, `lasna`. `wit/gossip.wit` defines gossip protocol types (agent identity, vector clocks, envelopes, trust, transport). `cargo component build -p lasna` regenerates `lasna/src/bindings.rs`; individual crate bindings regenerate with `cargo component build -p <crate>`.
- **WIT worlds:** `lasna-pipeline` (single WASM component importing `compute-backend`, exporting `lasna`; gerna/smuni/logji linked as internal Rust crate deps). Legacy `gerna-component`, `smuni-component`, `logji-component` worlds retained for individual crate binding regeneration.
- **Rust structs:** `LasnaPipeline` (WASM component), `GernaComponent`, `SmuniComponent`, `LogjiComponent` (binding stubs, not deployed as separate components).
- **Cross-component data:** Flat index-based arrays (`AstBuffer`, `LogicBuffer`) with `u32` indices — no pointers across WASM boundaries.
- **Compute dispatch:** logji uses injectable function pointers (`logji::register_compute_dispatch`) instead of cfg-gated WIT imports. Lasna registers host-bridge functions at Session creation; native mode (nibli-engine) leaves dispatch unregistered (returns error for external predicates).

## Canonical Runtime Surfaces

Use these assumptions when selecting entrypoints:

- `gasnu` is the canonical local/operator runtime for the theorem prover. It is the main single-node REPL and the default way to exercise the WASM-hosted pipeline.
- `nibli-server` is the canonical API/runtime for the Transparency Triad stack. `nibli-ui` is the canonical browser frontend for that server.
- `tavla` is the canonical gossip transport/runtime. It owns peer transport, sync, and hub-style deployment. The server can attach to a `tavla` peer; it is not the transport owner.
- `nibli-engine` is an internal embedding library, not a user-facing runtime surface.
- `nibli` is developer tooling: a native direct-crate REPL and the `nibli-validate` binary used for validation/data-generation workflows. It is not the canonical production runtime.
- `nibli-agent` is a specialized LLM gossip peer built on the canonical `tavla` network. Treat it as an integration/demo runtime, not the base theorem-prover runtime.

## Code Conventions

- Gerna (parser) tests use `as_bridi(&r.sentences[0])` helper to unwrap `Sentence::Simple`
- Grammar tests use `parse_ok()` / `parse_err()` + token constructors `cmavo()`, `gismu()`
- Smuni (semantics) tests use `compile_one(selbris, sumtis, bridi)` helper returning `(LogicalForm, SemanticCompiler)`
- `resolve(&compiler, &spur)` helper to get string from interner in tests
- Connective enums (Je/Ja/Jo/Ju) are shared between selbri and sumti connectives
- BAI tags map to underlying gismu: ri'a->rinka, ni'i->nibli, mu'i->mukti, ki'u->krinu, pi'o->pilno, ba'i->basti

## Codebase Exclusions

When analyzing or searching the codebase:
- **Exclude `docs/` folder** — generated/reference documentation, not source
- **Exclude `**/bindings.rs`** — auto-generated by `cargo component build`, not hand-written

## Known Issues

- `cargo component build` fails on `io-extras` crate — pre-existing, unrelated to our changes. Bindings generate before the failure.
- **rustc ICE in `check_mod_deathness`** — `wasmtime::component::bindgen!` macro triggers compiler panic in library crates (not binary crates like gasnu). Workaround: `#![allow(dead_code)]` at crate root in nibli-engine. Fixed in rustc 1.94.0 (updated via flake).

## Roadmap

See `todo.md` for the full phased roadmap (Phases 1-5), dependency graph, and technical debt tracker.

## Pre-commit Checklist

Before every commit, always:
1. Update `CLAUDE.md` — bump Current Status if a roadmap phase was completed
2. Update `todo.md` — remove completed tasks
3. Update `README.md` — if Lojban coverage or reasoning capabilities changed
4. Then commit all code + doc changes together

## Current Status

Roadmap status: Phase 1, Phase 2, and Phase 3 are complete. `todo.md` no longer contains open roadmap blockers; remaining work is deferred or incremental rather than required for production-readiness alignment.

Completed through all Tier 1 items + full Tier 2 + full Tier 3 + full Tier 4 (production reasoning features: conjunction introduction, fuel limits, error variants, WASI sandboxing, clone-free connectives, arena allocator) + C2 (non-monotonic reasoning / belief revision) + C3 (temporal reasoning) + C4 (event semantics — Neo-Davidsonian) + C5 (description term opacity — `le` vs `lo`) + SkolemFn multi-dependency + event-decomposed universal rule compilation fix (condition-side ∃ as pattern variables) + proof trace memoization (DAG deduplication via ProofRef) + **egglog removal** (replaced egglog equality saturation with demand-driven backward-chaining over indexed fact store) + **legacy representation removal** (replaced legacy string interner + SortedU32Vec fact store with typed GroundTerm/GroundFact/StoredFact storage; untraced queries use structural unification via unify_facts(); all query paths (untraced and traced) now use typed StoredFact storage and structural unification) + **subs HashMap migration** (migrated `subs: HashMap<String, String>` to `HashMap<String, GroundTerm>` across all query/assertion paths — eliminates per-query string parsing via `parse_repr_to_ground_term()`; domain member iteration uses typed `GroundTerm` directly; removed ~120 lines of string serialization/parsing code including `parse_repr_to_ground_term`, `parse_repr_to_term`, `build_skolem_fn_repr`, string-based `CartesianProduct`, and dual `domain_members_cache`).

**Book-vs-code audit pass (correctness fixes + GDPR case study):** A full audit of the engine against the book (`book/`) is tracked in `todo.md`. Landed correctness fixes: (1) **negation-as-failure for negated rule antecedents** (negated `poi`/conjunct conditions now evaluated correctly, not as positive requirements); (2) **predicate-cache poisoning fix + cross-depth tabling** (only definitive True/False cached; iterative deepening no longer re-clears each pass); (3) **honest traced provenance** (du-equivalence substitution emits `EqualitySubstitution`; the bogus `holds:true` fallback replaced with `PredicateNotFound`/false); (4) **smuni relative-clause firewall** (ambiguous implicit-`ke'a` clauses are rejected rather than silently dropping the binding); (5) **forethought biconditional `go A gi B`** parses and lowers to `Biconditional`; (6) **ground conditionals/biconditionals now reason** (`ganai`/`go … gi`/`.i jo` route through `compile_forall_to_rule`, so event-decomposed operands match via pattern variables). Each shipped with regression tests that fail pre-fix. **GDPR case study** (`gdpr.lojban`): rewritten as a maximal, reproducible Article 5/6/7/9/15/17/33 compliance KB (`la .adam.`/`la .akmes.`/`la .gugli.`) with the consent-withdrawal belief-revision flip; covered by a parse test (`pipeline_gdpr_lojban_all_lines_parse`) and end-to-end `gdpr_*` tests in `nibli-engine/tests/integration.rs`; Chapter 20 rewritten to match with captured engine output. **Drug-interaction case study** (`drug-interactions.lojban`): rewritten to encode the warfarin/fluconazole/CYP2C9 mechanism chain — proxy vocabulary (`fanta`=inhibits, `se katna`=metabolized-by, `cinla`=narrow-index, `zenba`=concentration-up, `ckape`=toxicity-risk, `kajde`=alert), `la .adam.` patient, an apixaban/CYP3A4 negative control, and a phenytoin second substrate; mechanism steps grounded per substrate via `ganai ge … gi …` conditionals (which reason), the alert step (`ro lo xukmi poi ckape cu kajde`) a general universal rule; covered by a parse test (`pipeline_drug_interactions_lojban_all_lines_parse`) and nine `ddi_*` end-to-end tests (headline alert, negative control, conjunction gate, witness extraction, temporal discrimination, belief-revision via inhibitor discontinuation, regimen count + dose-sum aggregation); Chapter 21 rewritten with captured engine output, a CDS/pharmacovigilance practitioner-impact table, and a proxy-vocabulary honesty register. **Engine work:** added `count_witnesses_text`/`aggregate_text` (+ `EngineAggregateOp` re-export) to `nibli-engine`, exposing logji's witness-count/aggregate at the embedding layer. **Known limitations filed** (`todo.md`): a negated event-decomposed restrictor (`poi na <selbri>`) is dropped at rule compilation (GDPR Art 17 erasure uses the query-time negation `na se curmi` as a sound workaround); a binary-condition universal rule never fires and prenex `zo'u` is unimplemented (so the general multi-variable CYP join rule is grounded per substrate); retracting a ground-conditional antecedent that feeds a downstream universal rule can leave a stale derivation OR hang the post-retraction query, so both mechanism steps are grounded (only the `ckape → kajde` alert rule is universal); `du` was not reachable through surface Lojban (surface `zmadu`/numeric evaluation was fixed 2026-06-12 — see the numeric-group follow-up fix below; surface `du` itself was fixed 2026-06-15 — see the surface-`du` follow-up fix below). ~~No open HIGH audit items remain.~~ **Superseded (2026-06-10):** a 35-agent deep code-review panel (12 unit reviewers + 4 soundness/security/performance/test audits, every finding adversarially adjudicated with runtime probes — 11 confirmed, 3 partial, 0 refuted) re-opened HIGH-severity items. Headline class: **fail-open compilation** — every pipeline stage silently drops constructs it cannot represent instead of rejecting them (unrepresentable rule antecedents register unconditional rules; zero-ingest assertions return success with a fact id; gerna lexer truncation / flattener body-index corruption / connective re-pairing; smuni drops rel clauses on `la`/`le`/pro-sumti). Plus reasoning-core defects (`best_result` ResourceExceeded wipe → cached wrong False, unbounded `du`-fallback recursion), production gaps (nibli-server reasoning unbounded + uncancellable, gasnu component bricks after its own fuel trap, belief-revision durability broken at every persistence layer), and verification debt (Ch 20 GDPR queries do not reproduce against the shipped corpus; WASM runtime has zero behavioral tests). All filed in `todo.md` under "Deep code-review panel findings (2026-06-10)"; full panel output in `code-review-panel-2026-06-10.json`. Book-side findings (separate panel, same date) are in `book/revisions.md` + `book/review-panel-2026-06-10.json`.

**Panel remediation (2026-06-10, same day): the 13-pin known-failure backlog is CLOSED.** Process: every probe was first pinned as a `#[ignore]`d test encoding desired behavior (`nibli-engine/tests/known_failures.rs`, `logji/tests/known_failures_fol.rs`, `known_failures_fatal.rs`), verified RED, then fixed and promoted — the file now runs **14 live guards with an empty backlog** (run by `just verify-harness`, which is in `ci`). Landed fixes: **fail-closed rule compilation** (untemplatable antecedent/conclusion atoms reject the assertion instead of registering under-conditioned rules — `ganai` with tensed/disjunctive antecedents no longer fires on an empty KB; negated-conclusion universals rejected instead of parking as dead `__fallback__` rules); **zero-ingest guard** (bare `.i ja` disjunctions and `.i ju` xor reject instead of returning Ok + a phantom fact id; Count exempted; negations exempted by design); **`best_result` ResourceExceeded wipe** fixed at all four loop sites (entailed queries no longer cache wrong False / NAF wrong True on raw FOL); **`du`-equivalence unbounded recursion** fixed via shared-visited cycle guard (stack-overflow SIGABRT → graceful termination); **smuni meaning-loss batch** (rel clauses on `la`/`le`/pro-sumti conjoin into the matrix with the ambiguity firewall applied — no more unsound TRUE; FA-overflow fails closed; tanru-in-poi false rejection fixed via Unspecified-role emission + per-event firewall counting; `da` after `ro lo` now closes INSIDE the universal — correct ∀∃ scope, rule compiles and fires); **gerna front-end batch** (lexer error channel — unlexable runs surface as positioned parse errors instead of silent truncation; flattener Abstraction/Restricted body indices fixed via push_sentence return values + index-target tests); **retraction multiplicity** (a fact survives while any live record asserts it; all roots walked) and **negated-ground contradiction detection** (`na <selbri>` recorded in a dedicated negative-fact registry with event-Skolem-normalized template groups; `check_contradictions` flags a contrary asserted positive; NAF/CWA query semantics unchanged). Also: **`FactStore: Send`** (nibli-server compiles again), **engine-API retraction persisted** (no resurrect-on-reopen, replay errors propagated), **clippy-runtime green for the first time** (full lint cascade cleared), **workspace rustfmt-normalized** (fmt-check green), and the **first fully-green `just ci`** in the project's history. Verification at close: guards 14/0, logji 239/0, smuni 95/0, gerna 308/0, integration 42/0, fol 3/0, fatal 1/0. Manuscript-side: `book/tools/verify_book.py` (+ `just verify-book`) gates every Lojban example in the book through the engine (parse + vocab; caught `xanlu` and 13 real parse failures). **Still open** (todo.md): the production set (server reasoning unbounded/uncancellable, gasnu post-trap brick, gasnu replay ID drift, dead WasiFactStore, redb growth, compute trust puncture), the nondeterministic stratification guard, partial remainders inside the [~] items, and the verification debt — headlined by the **GDPR full-corpus query blowup** (Ch 20's first query >240s debug against the loaded corpus), the next target. **(Done 2026-06-11: >240s → 7ms release, CI-pinned by `gdpr_full_corpus_lawful_basis_query_completes`; the same fix resolved the DDI retract+rebuild hang.)** Follow-ups landed same day: **gasnu non-interactive script mode** (stdin-not-a-TTY or `--script <file>` bypasses reedline, sharing the interactive dispatch path — byte-faithful REPL transcript capture for the book; smoke-tested by `just smoke-gasnu-script`); **DDI corpus toxicity step is now the genuine universal** `ro lo xukmi poi zenba poi cinla cu ckape` (only the cross-entity concentration step remains grounded — binary-restrictor universals and prenex `zo'u` still open); and the **book P0/P1 pass** (5 parallel chapter groups + appendices): xanlu→jmive, all examples re-validated, four-valued contract aligned everywhere, soundness claims reconciled bidirectionally, external facts + citations fixed — `book/tools/verify_book.py` now reports **0 findings**, and `verify-book-vocab` joined the `ci` recipe. **Book P0.3 transcript capture-regeneration (Wave 2) is now DONE (2026-06-12):** `book/tools/capture_book.py` replaces every printed "engine output" block with real, re-checkable gasnu captures; the tool gained gloss-aware merge (preserves indented English glosses), `<!-- capture: reset -->` session-group markers (fresh process per group), and a segment-count guard that aborts rather than splice a dead session's output. All transcript chapters (8/9/10/11/12/18/20/21) pass the whole-manifest `--check` twice (byte-stable); Ch 12's consent case study was repaired (it had captured `[Host Error]` traps), Ch 20/21 restyled from `query:/=>` pseudo-transcripts into real `:load`/query/`:retract` REPL sessions, and Ch 18's end-to-end walkthrough fixed (>300s dead-backend-dispatch timeout → ~3s; threaded-session contradictions in W4/W6/W8 corrected; W7 reframed around the compute boundary). Ch 19 is validated-and-excluded (it depicts the Transparency-Triad UI, not the REPL). New `just verify-book-capture` recipe gates this (pre-release, NOT in `ci` — needs the WASM+gasnu build). Remaining book-side: the deferred P2/P3 structural items only.

**Follow-up fix — back-translation glosses (smuni-dictionary):** gismu/lujvo gloss resolution is now a three-tier chain: new `GISMU_GLOSS_OVERRIDES` (`bilga`→"must", `curmi`→"permit") → `FALLBACK_GISMU_ENTRIES` (the 50 curated core words now gloss identically in XML and no-XML builds: `gerku`→"dog", `prenu`→"person") → first jbovlaste glossword. Fixes the Transparency Triad default syllogism back-translating as "all the bitch animal"; pinned by `test_back_translate_default_syllogism` ("all the dog animal", matching book Ch 19). Entry count unchanged (10,521); arities diff-verified identical.

**Follow-up fix — stacked-`poi` conjoin (smuni):** stacked `poi` relative clauses now CONJOIN into the universal restrictor instead of overwriting (`resolve_sumti`'s `Sumti::Restricted` arm in `smuni/src/semantic/helpers.rs` clobbered the inner clause's restrictor; `ro lo X poi P poi Q cu R` compiled to `Q ∧ gadri → R`, dropping P — a soundness bug, a rule firing on an incomplete antecedent). Fixed by conjoining via `LogicalForm::And` while preserving `inject_variable` and the ambiguity-rejection firewall; pinned by `stacked_poi_conjoins_both_clauses` (failed pre-fix; lib 297/89/232 + integration 41 green). The drug-interaction toxicity step nonetheless stays a ground conditional: expressing it as the now-correct universal rule `ro lo xukmi poi zenba poi cinla cu ckape` makes the inhibitor-discontinuation belief-revision query hang (>200s) under retract+rebuild — a separate non-termination bug filed in `todo.md`.

**Follow-up fix — deterministic witness/proof output ordering (logji):** REPL output is now byte-reproducible run-to-run (prerequisite for the book transcript-capture pass). The HashSet-derived ordering item is closed by sorting once at the cache/return boundaries: `ensure_domain_members_cached` sorts the typed domain-member cache (drives ForAll iteration + ForallVerified proof order), `collect_candidates` sorts its candidate vector, `query_find_inner` sorts binding sets by their canonical sorted (var, term) key (no dedup — `count_witnesses` semantics unchanged), and the `[Skolem]` diagnostic line sorts its HashMap mapping by variable name. Pinned RED→GREEN by `find_witness_ordering_is_deterministic_across_kb_instances` + `domain_member_cache_order_is_deterministic` (logji) and `find_witness_output_order_is_deterministic` (nibli-engine integration); empirically verified by 8 separate gasnu script-mode process invocations producing byte-identical output. Residual non-REPL surfaces (check_contradictions violation order, off-by-default `[Forward]` print order) re-filed narrowly in `todo.md`.

**Follow-up fix — entailment ∃ candidate narrowing (logji) + typed-mirror durability (nibli-engine):** The Ch 12 ∃-heavy nested-abstraction query blowup is CLOSED: `? lo prenu cu se bilga lo nu lo datni cu se pilno` against the chapter's 11-Skolem universal rule went from ~40s native debug / 10B-WASM-fuel exhaustion (which trapped and bricked the gasnu session, corrupting the chapter's captured transcripts) to **0.4s debug** end-to-end. Mechanism: both `check_formula_holds` Exists arms (untraced + traced) now enumerate via `collect_entailment_candidates` (logji/src/kb.rs) — mandatory positive anchors only (no Or-branch anchors, no compute/`du`/`zmadu`/`mleca`/`dunli` anchors), candidates = predicate-index hits (equivalence-expanded) ∪ rule-derivable values, where a dependent-Skolem conclusion template contributes an anchored cartesian for THAT base name only (a name-mismatched SkolemFn can never unify) and a PatternVar conclusion keeps the full member+registry superset; falls back to the old full domain × registry-cartesian scan when no mandatory anchor exists. Completeness argument in the doc comment; pinned by `ch12_consent_case_study_traced_query_completes` (watchdog thread, 10s debug budget; RED 40s → GREEN 0.4s). Landing the narrowing unmasked a real durability bug the old enumeration had been hiding: a fact retracted at the STORE layer (`NibliStore::retract_fact`, no live engine) resurrected into the reopened KB via the eagerly-loaded typed redb mirror (query-visible while `list_facts` showed empty — the old test passed only because the empty domain-member cache blinded ∃ enumeration to the index). Fixed in `NibliEngine::open`: the typed mirror is CLEARED before the registry replay (the tombstone-aware, remote-merge-aware registry is the single durable source of truth) — which also closes the typed-redb unbounded-growth item (duplicate rows on every reopen). Integration 44/0, logji 243/0, full `just ci` green.

**Follow-up fix — gasnu trap recovery (the post-trap brick is CLOSED):** Any WASM trap (fuel, memory, guest panic) permanently poisons the Wasmtime component instance — component-model semantics forbid re-entering it — so gasnu's one-Store-forever REPL bricked after every trap ("cannot enter component instance" on all later commands, "cannot remove owned resource while borrowed" at exit; this is how Ch 12's captures got corrupted in Wave 2). Fixed in two commits: (A) the 450-line dispatch closure became a `Repl` struct with `instantiate_session()` factored for reuse (the bindgen `GuestSession` borrows the bindings struct, so the accessor is derived per call from the owned `pipeline` field — required once rebuild replaces it); (B) every successful KB mutation is journaled host-side (`JournalEntry`: AssertText/AssertDirect/Retract/RegisterCompute; `:load` journals each asserted line hermetically; `:reset` clears; startup persistence replay SEEDS the journal; `:merge` rebuilds it), a host-level call error sets a **lazy** `needs_rebuild` flag, and `prepare_session()` (all 13 former refuel sites) re-instantiates from the compiled Component + replays the journal before the NEXT session call — lazy so an intervening `:fuel`/`:memory` raise applies to the replay; determinism makes the rebuilt session byte-identical (same fact ids, same Skolems; guest `[Skolem]` diagnostics print during replay, making the rebuilt state visible). Replay failures are bounded (stop at first error, "Replay incomplete (i of N)"); replay skips persistence (re-inserting would spuriously tick the store's HLC); both exit-path `resource_drop`s ignore errors. Also fixed in the same change: `format_host_error` classifies via the typed `wasmtime::Trap` (OutOfFuel) + full-chain scan instead of the top Display line (fuel traps printed as `[Host Error]` + backtrace before — the Ch 20 "memory limit" message was actually a mislabeled fuel trap), and `StoreLimits` now sets `trap_on_grow_failure(true)` at both builder sites so a memory-cap denial surfaces as an attributable "[Limit] Memory" error instead of an unrelated guest-abort trap. Pinned by `just smoke-gasnu-trap-recovery` (assert → `:fuel 1000` → trap correctly labeled → `:fuel` raise → next query rebuilds, replays 1 command, answers TRUE with fact #0 reproduced → no brick, no exit error) + 3 chain-based unit tests; stress-verified on the Ch 20 full-corpus session at default budget (24-entry replay, double-trap recovery, `:facts` correct, exit 0). gasnu 49/0, smoke-gasnu-script green, full-manifest book capture --check byte-clean, `just ci` green.

**Follow-up fix — surface-numeric evaluation via decomposed-group routing (logji):** Every numeric query through surface Lojban returned a wrong FALSE (`? li pa no cu pilji li re li mu`, `? li mu cu zmadu li ci`, external `tenfa` dispatch — all dead): event decomposition put the operands in sibling `rel_xN(_ev0, n)` role predicates while the evaluators read only the head node's args; the unit tests hand-built the flat form and hid the regression. CLOSED by `try_evaluate_numeric_group` (logji/src/compute.rs): at the `∃ev` boundary of BOTH `check_formula_holds` evaluators (untraced + traced — they must agree, `run_entailment_check_with_proof` runs both), detect the exact group shape (one head — ComputeNode, or Pred ∈ {zmadu,mleca,dunli} — over the event var, plus `rel_xN(ev, arg)` roles with contiguous N; ANY other conjunct falls through, so tanru and asserted non-numeric facts stay on the normal path), gather the role args in x-order, and route by relation name arithmetic-first: comparison (`"numeric"` trace tag) → built-in arithmetic (`"arithmetic"`) → backend dispatch for ComputeNode heads (`"backend"`; dispatch error degrades to fall-through, so a no-backend `tenfa` query is a fast FALSE, not a hang). Computed False is definitive (matches the flat arms; store-shadowing policy stays filed); NO auto-ingestion (a fresh-Skolem-event ingestion per query would accumulate duplicates — recompute is free); traced arm emits the existing `ComputeCheck` proof rule (gasnu renders `Compute (arithmetic): pilji -> TRUE`). Rider: the flat ComputeNode arms now also try built-in arithmetic BEFORE backend dispatch (the documented Layer-2 ordering, matching gasnu's `evaluate()` and the batch path — closed the filed inversion item). Pinned RED→GREEN by 12 decomposed-shape logji unit tests (`test_decomposed_*`, incl. extra-conjunct/non-numeric fall-through and traced-ComputeCheck) + 5 surface integration tests (`surface_numeric_*`); logji 255/0, integration 49/0, full `just ci` green. Remaining gaps filed narrowly (find/∀/Count/rule-condition groups; `du` over `li`). Book: Ch 18 W7 upgraded from honesty-caveat to captured working demo; Ch 21's threshold/Honest-Boundaries register updated. Also: `just build-wasm` now runs `cargo fmt -p lasna` after binding regeneration (the auto-generated bindings.rs kept tripping `ci`'s fmt-check).

**Follow-up fix — faithful dependent-Skolem rendering at the witness/proof surface (logji):** `ground_term_to_logical_term` (written for compute dispatch, where an opaque token suffices) was also used to convert `[Find]` witness bindings and `ExistsWitness` proof-step terms, collapsing `SkolemFn("sk_1", adam)` to bare `sk_1` — distinct dependent witnesses (`sk_1(adam)` vs `sk_1(sk_4)`) printed identically, surfacing as confusing duplicate `[Find]` rows and base-name-only proof labels (caught by the Wave 2 book capture of Ch 9's dog-likes-cat walkthrough). New `witness_term_to_logical_term` renders SkolemFn/DepPair via `to_display_string()` (`sk_1(adam)`) at the four read-surface call sites (`query_find_inner` + three `ExistsWitness` sites); compute dispatch unchanged. Pinned RED→GREEN by `test_find_witnesses_dependent_skolem_terms_stay_distinct` + `test_proof_trace_exists_witness_renders_dependent_skolem` (logji 243/0, integration 44/0).

**Follow-up fix — binary-condition universal rules fire (smuni `se`-in-`poi` rel-clause):** a universal whose restrictor contained a CONVERTED two-place relation (`ro lo gerku poi se prami la .alis. cu danlu`, "every dog loved by alis is an animal") compiled to a `UniversalRuleRecord` that never fired — a real bug confirmed by runtime probe (the negative control, gadri-not-asserted, correctly stayed FALSE; the positive case wrongly returned FALSE too). Root cause was in **smuni**, not the reasoning core: in a relative clause the implicit `ke'a` subject was injected post-hoc (`inject_variable`) into the first unspecified `_x1` ROLE predicate, but `se`/`te`/`ve`/`xe` conversion in `apply_selbri` had already swapped the explicit sumti out of x1 and vacated that slot — so `poi se prami la .alis.` compiled `prami(dog, alis)` ("dog loves alis") instead of `prami(alis, dog)` ("loved by alis"), exact-swapping the roles versus how the same relation is ASSERTED (`la .rex. cu se prami la .alis.` correctly applies `se` because `rex` is the explicit x1 before conversion). Fixed by placing the clause's implicit `ke'a` as the x1 ARGUMENT of the clause's main bridi BEFORE conversion — a one-shot `pending_clause_subject` on `SemanticCompiler`, set in `resolve_sumti`'s `Sumti::Restricted` arm and consumed by the first `compile_bridi` (when the body has no explicit x1), routing `ke'a` through `se` to the correct underlying role and skipping the (conversion-blind) post-hoc inject. This also lets a non-converted 2-place poi clause (`poi prami la .alis.`) compile correctly instead of being over-rejected by the x1-ambiguity firewall. Pinned RED→GREEN by `binary_restrictor_rule_fires` / `binary_restrictor_negative_control` / `binary_restrictor_constant_second_place_fires` (nibli-engine integration); live-probed through the WASM/gasnu pipeline (`(prami ∧ gerku → danlu): danlu -> TRUE`); no regressions (smuni 95/0, gerna 308/0, integration 53/0, full `just ci` green, GDPR/DDI suites unchanged). **Still open** (`todo.md`): multi-universal argument-place rules (`ro lo gerku cu pendo ro lo mlatu`) remain a SEPARATE, larger defect — they fail at COMPILATION (the conclusion is itself a nested `∀y.(mlatu(y) → pendo(x,y))`, needing prenex-flattening of nested universals) plus a firing-side unbound individual-`x__`-var enumeration gap.

**Follow-up — prenex `ro da ro de zo'u` (multi-variable quantified rules): front-end LANDED, firing-perf STAGED.** The idiomatic multi-variable rule form now parses and lowers end-to-end: a new `Sentence::Prenex` AST variant (flat `nibli_types::ast` + arena `gerna::ast`), `try_parse_prenex` in `gerna/src/grammar/sentence.rs` (parses `(ro (da|de|di))+ zo'u`, backtracking cleanly so `ro lo …` stays a universal description; `zo'u` already lexed), the gerna flattener `push_sentence` arm, and smuni lowering (`compile_sentence`'s Prenex arm wraps the body in nested leading `ForAll`; a `prenex_vars` set on `SemanticCompiler` excludes prenex-bound vars from the `da`/`de`/`di` existential closure so they are universally — not existentially — quantified). lasna's go'i snapshot machinery (`visit_sentence`/`rebase_sentence_inplace`/`resolve_sentence_go_i`) handles the new variant. The rule COMPILES (leading ForAll → `compile_forall_to_rule`, so it avoids the nested-consequent rejection) and FIRES CORRECTLY — pinned by `prenex_symmetric_rule_fires`, `prenex_cross_entity_join_fires` (the CYP `∀da,de,di. fanta(da,di) ∧ se-katna(di,de) → zenba(de)`), and `prenex_cross_entity_join_negative_control` (nibli-engine integration; native, green) + `test_prenex_two_universals`/`test_ro_lo_description_is_not_a_prenex` (gerna). **STAGED (see `todo.md`):** multi-variable rule FIRING is pathologically expensive — a 3-var prenex query does ~10^9 ops even for 3 facts (native ~0.5s; gasnu/WASM **exhausts the 10B fuel budget**), because the conclusion event var is an over-approximated N-dependency `SkolemFn` (`members^dep_count` witness cartesian; same root as the filed DepPair-precision item). So the prenex is parse+reason-correct but not yet practical in the fuel-capped runtime for ≥2 free join variables. Full `just ci` green; gerna 310/0, smuni 95/0, integration 56/0; no regressions. **(STAGED status CLOSED 2026-06-15 — see the firing-perf follow-up fix below.)**

**Follow-up fix — prenex multi-variable firing-perf (the `members^dep_count` blowup is CLOSED):** the 3-variable CYP join (`ro da ro de ro di zo'u ganai ge da fanta di gi de se katna di gi de zenba`, queried `? la .uarfarin. cu zenba`) went from **10B-fuel exhaustion (a `[Limit]` trap) in gasnu/WASM** to **TRUE in ~3s WASM-debug under the default budget**. The fix is the single COUPLED change the 2026-06-15 measurement pass identified (precise deps alone regressed correctness because the over-approximation was load-bearing for binding): **(1) precise dependent-Skolem deps** — `compile_forall_to_rule` (logji/src/rules.rs) now restricts each conclusion existential's `SkolemFn`/`DepPair` dependencies to the universals its existential var CO-OCCURS with in the consequent atoms (new `atom_var_args` helper over the flattened consequent), so the CYP conclusion `zenba(de)` gets `dep_count 1` (only `de`), not 3 — collapsing the `members^dep_count` witness cartesian; **(2) index-anchored join binding** — `bind_join_vars_from_index` (logji/src/reasoning.rs) binds the condition-only individual `x__` vars (the inhibitor `da`, the enzyme `di`, which the precise deps no longer carry) during firing by looking each event-bound role condition up in `arg_position_index` and propagating shared vars to a fixpoint, and the per-combo candidate filters skip pruning on conditions that still carry an unbound individual var (otherwise the join var and its event var deadlock circularly). Applied to ALL FOUR backward-chain enumeration blocks (untraced/traced × normal/temporal) for parity — soundness preserved by the downstream ground condition-check (a spurious binding fails it), completeness because every `(da,di)` pair appears via its event. Pinned RED→GREEN by `test_dependent_skolem_precise_deps_single_universal` (CYP-shape conclusion `dep_count == 1`; `test_multi_dep_skolem_registry` stays `== 2`) and re-validated by `prenex_cross_entity_join_fires` (TRUE), `prenex_cross_entity_join_negative_control` (FALSE), `prenex_symmetric_rule_fires`, and `prenex_join_terminates_without_blowup` (12-noise watchdog). gasnu fuel probe: CYP TRUE (3-fact and 12-noise) under the default 10B fuel with no `[Limit]`; negative control FALSE. logji 256/0, integration 57/0 (GDPR full corpus, DDI, Ch12 watchdog unchanged), full `just ci` green, `smoke-gasnu-script` green. The general multi-variable join is now PRACTICAL, not just expressible; only the OBJECT-POSITION surface sugar `ro lo gerku cu pendo ro lo mlatu` still needs nested-consequent compilation support (write it as a prenex meanwhile), and the DDI cross-entity step's optional single-prenex re-encode stays deferred behind the retract+rebuild-hang guard.

**Follow-up fix — surface `du` (identity) selbri + `na du` inequality (logji equality reasoning is now reachable from Lojban):** logji has always had a complete `du` union-find equality layer (equivalence classes, assertion-time merging, reflexive/transitive query, fact transfer), but the surface parser rejected `du` as a selbri — so identity facts could only be injected at the fact-store layer (a book-named Honest-Boundaries limitation + todo G-M1). Now `la .X. cu du la .Y.` (and the cu-less observative `la .X. du la .Y.`) parses, lowers, and feeds the union-find end-to-end. Three-layer change: **gerna** — a `du` arm in `try_parse_tanru_unit_base` (gerna/src/grammar/selbri.rs) mirroring `go'i` (→ `Selbri::Root("du")`); **smuni** — the crux: `apply_selbri`'s `Selbri::Root` arm (smuni/src/semantic/selbri.rs) special-cases `du` to emit a FLAT 2-arg `du(x1, x2)` predicate (NO Neo-Davidsonian event decomposition), because logji's union-find interception (`kb.rs` — `relation == "du" && args.len() == 2`) and du-query arm never match the decomposed `∃e. du(e) ∧ du_x1 ∧ du_x2` form; a >2-place `du` is rejected fail-closed in `compile_bridi` (where the dropped-overflow sumti are visible); **logji** — the positive path needed NO change (equivalence transfer works through event-decomposed queries — `? la .varfarin. cu xukmi` succeeds via the equivalence-expansion fallback substituting the canonical `coumadin`); only `na du` inequality is new — `check_contradictions` gains a section that flags `na du(X, Y)` when X and Y share a union-find canonical root, catching both direct and TRANSITIVE equality (`du(X,Z) ∧ du(Z,Y)`) that the store-membership check misses (inequality queries already work via the NotNode handler over the du-query). Pinned RED→GREEN: gerna `test_du_as_selbri_with_cu`/`test_observative_du_without_cu`/`test_du_does_not_break_du_u`; smuni `test_du_lowers_flat_not_event_decomposed`/`test_du_with_more_than_two_sumti_is_rejected`; logji `test_na_du_transitive_contradiction`/`_unrelated_no_contradiction`/`_inequality_query`; nibli-engine `du_surface_equivalence_transfers_fact`/`_is_symmetric`/`_negative_control`/`na_du_surface_contradiction`. gasnu/WASM live probe: `la .djan. cu du la .jan.` + `la .djan. cu gerku` → `? la .jan. cu gerku` TRUE under the default fuel (no `[Limit]`); `li pa du li pa` TRUE (reflexivity, now reachable), `li pa du li re` FALSE; 3-place `du` → `[Semantic Error]`. gerna 313/0, smuni 97/0, logji 260/0, integration 61/0, full `just ci` green. Tensed `du` stays atemporal by design (the union merge fires only for `StoredFact::Bare`); `se/te du` conversion is a symmetric no-op (out of scope); the proof-trace EqualitySubstitution-for-directly-asserted-du item stays filed.

**Follow-up fix — gerna error-recovery hardening (depth-guard leak + afterthought re-pairing):** two coupled defects in gerna's per-sentence error recovery, both reachable in any multi-sentence input (the UI's Lojban tab is one parse). **(P-H5, robustness)** the recursion depth guard leaked across recovery — `enter()`/`leave()` don't pair on `?` early returns and `skip_to_next_dot_i` never reset `depth` (a single `usize` on the per-parse `Parser` reused across sentences), so N malformed sentences poisoned the shared counter and a later VALID sentence spuriously failed with "maximum nesting depth exceeded". **(P-H4, soundness)** after recovery, an afterthought connective (`.i ja B`) unconditionally popped the last successfully-parsed sentence as its left operand, fabricating a `Connected` disjunction across the recovery gap the author never wrote (`mi klama .i lo .i ja do prami` → Connected(Ja, mi-klama, do-prami)). Fix (gerna/src/grammar.rs): RAII was proven borrow-infeasible (a `Drop` guard holding `&mut Parser` conflicts with the body's own `&mut self` re-borrows), so `skip_to_next_dot_i` resets `self.depth = 0` at the recovery boundary (the malformed subtree is discarded; this fn is only called from `parse_text`'s top level, so depth==0 post-recovery is correct); a new `recovery_active: bool` on `Parser` is set there too and, in the afterthought arm, suppresses the cross-gap pop — emitting a positioned "afterthought connective has no valid prior sentence" error and keeping the well-formed right as a standalone — and is cleared on the next clean standalone parse or pairing attempt (so `good .i bad .i good2 .i ja good3` still re-pairs good2+good3). G-L8 (top-level break after garbage) was investigated and kept separate (already correct: the "break" is the normal loop exit with a reported "unconsumed tokens" error; only the error POSITION is a cosmetic latent). Pinned RED→GREEN by `test_depth_resets_after_recovery_then_valid_parses`, `test_afterthought_after_recovery_does_not_repair_prior`, plus two controls (`test_afterthought_pairs_normally_without_recovery`, `test_afterthought_repairs_after_intervening_good_sentence`); regression guards (error-recovery, deeply-nested depth, all afterthought-connective variants, plain `.i`) stay green. gerna 317/0, full `just ci` green; gerna-only change (no downstream/WASM impact for valid input).

**Follow-up fix — NAF stratification guard is now SCC-based and deterministic (P-H2):** logji's `check_stratification` (logji/src/rules.rs) — the negation-as-failure soundness guard that rejects a rule set whose predicate dependency graph has a recursive cycle through a negated condition — was both **nondeterministic** (it iterated `graph.keys()` in HashMap order, so the DFS root, and thus the verdict, flipped run to run) and **position-blind** (the `cycle_has_neg = *is_neg || (has_neg && !dep_neg)` decision counted a negative edge on the path *into* a cycle as if it were *inside* it). Net effect: the unstratifiable program `{a←¬b; b←¬c; c←b}` was accepted ~⅓ of process runs. Replaced the buggy colored DFS with the standard definition — **a program is unstratifiable iff some strongly-connected component contains a negative edge** — via an inline iterative (explicit-stack) Tarjan `compute_sccs` (node set = graph keys ∪ edge targets; sorted node + neighbor iteration → a canonical partition independent of HashMap layout / rule-registration order; explicit stack so a long positive chain can't overflow), then rejecting iff any edge `(u→v, neg)` has both endpoints in one SCC (so an incoming external negative edge is structurally excluded, and a negative self-loop `a←¬a` — a size-1 SCC — is caught uniformly, no `len()>1` gate). Caller (`register_rule` + rollback) unchanged. **Honest reachability:** this is a determinism + defensive-core-correctness fix, NOT a closed surface-soundness hole — fail-closed compilation (the negated event-decomposed antecedent of `ganai na P gi Q` is rejected before any negative edge reaches the graph) means no current surface-Lojban construct produces a flat negated rule antecedent, so the guard is exercised only by raw-FOL / hand-built rules and is the safety net for any future grammar/smuni change that emits one. Pinned RED→GREEN by `test_neg_cycle_rejected_all_orderings` (all 6 insertion orders × a fresh-KB loop — reliably red pre-fix as the reseeded dep-graph HashMap samples the wrong-accept root), `test_compute_sccs_partition` (Tarjan port + edge-target-as-node), with `test_positive_cycle_accepted_all_orderings` and `test_neg_self_loop_rejected` as guards; regression guards (`test_safe_stratified_negation`, `test_safe_positive_recursion`, `test_ground_material_conditional_no_false_positive`, `test_retraction_rebuilds_dep_graph`) stay green. logji 260/0, full `just ci` green; logji-only change (no surface/REPL/book behavior change).

**Follow-up fix — proof trace can never contradict the verdict (Stage 1 of the dual-evaluator unification; EG-M2 + LP-M3 display half):** a proof query ran TWO independent recursions — `check_formula_holds` (four-valued, authoritative) for the verdict and `check_formula_holds_traced` (boolean) to build the `ProofTrace`, discarding the traced verdict (`let _ = holds`) — and they collapsed four-valued→bool differently, so the displayed proof could assert a decided FALSE under a non-definitive verdict (a `ForallCounterexample`/`ExistsFailed`/`PredicateNotFound` under `Unknown`/`ResourceExceeded`, or a NAF-true `Negation` off a merely-`Unknown` inner — which also mislabelled `has_naf_dependency` onto the wire). For a zero-hallucination prover, a proof that contradicts its own answer is a credibility defect. Stage 1 reconciles the trace to the authoritative result: at the four definitive-failure sites (`Not`/`ForAll`/`Exists`/`Predicate` in `check_formula_holds_traced`, logji/src/reasoning.rs) the decided-FALSE rule is emitted ONLY when a four-valued recheck shows the sub-result is definitively `False` — otherwise a non-committal `ProofRule::PredicateCheck(("indeterminate", reason))` (reusing an existing variant → no WIT/bindings change) — and `run_entailment_check_with_proof` (lib.rs) forces each per-root step's `holds` to the authoritative `result.is_true()`. `has_naf_dependency` becomes correct for free. Pinned RED→GREEN by `trace_does_not_contradict_unknown_cyclecut`, `trace_does_not_show_counterexample_under_depth_exceeded`, `trace_naf_flag_false_when_inner_is_unknown`, `trace_exists_failed_only_when_all_definitively_false` (a positive cycle built from witness-free ground material conditionals), and the over-suppression guard `forall_genuine_counterexample_still_traced`; all `test_proof_trace_*` + four-valued regression guards stay green. logji 269/0, integration 61/0 (GDPR full corpus + Ch12 watchdog unchanged — the rechecks are bounded re-walks at failing leaves only), full `just ci` green; logji-only. **Deferred (Stage 2, filed in todo.md):** the double evaluation + the 4× backward-chain duplication still stand — the clean collapse (one four-valued evaluator with an optional trace sink, deleting `check_formula_holds_traced`, one shared `visited`) is higher-risk (entangled with the firing-perf/DepPair work) and is a separate pickup; it also removes the Stage-1 recheck re-walks and the residual And/Or-short-circuit nested-NAF imprecision. **Note:** `just verify-book-capture` is red on Ch 9's `?? la .adam. nelci ma` (a dependent-Skolem find witness renders `sk_1(_)` ×2 instead of `sk_1(adam)`) — confirmed PRE-EXISTING and independent of this change (a `git stash` + fresh WASM build reproduces it), filed under the witness-dedup item; do not recapture the buggy output.

**Follow-up fix — backward-chain candidate-filter unification (Stage 2a of the dual-evaluator collapse; the firing-perf-critical half of the 4×-block dedup):** the per-rule unbound-event-variable firing loop is implemented four times in `logji/src/reasoning.rs` (untraced/traced × normal/temporal), and the most divergent — and most firing-perf-critical — sub-block is the candidate FILTER: the untraced sites filtered in **2 phases** (decidable `typed_fact_is_asserted` → recursive `check_predicate_in_kb_typed`) with **`negated_condition_indices` inversion in both phases** threading a shared `visited`, while the traced sites filtered in a **single `.is_true()`-only phase** with no negation inversion and a fresh `HashSet` per check — so the proof path pruned candidates differently from the answer path. Stage 2a extracts ONE `filter_event_candidates` helper (partitions the single-var conditions into decidable/recursive, applies the 2-phase NAF-aware check with the `fact_has_unbound_pattern_var` skip, optional `tense_fact` for the two temporal sites) and delegates all four call sites to it — lifting the traced filter up to the untraced form. Net −56 lines. **Behavior-preserving, NOT a RED→GREEN fix:** the planned RED tests (a traced proof that prunes a negated single-var condition the untraced verdict keeps; a cycle yielding divergent traced/untraced cut shapes) proved UNCONSTRUCTIBLE — the divergence is unreachable on current surface Lojban (negated event-decomposed antecedents fail closed at compile, so no flat negated single-var condition reaches the event-var filter; and the traced filter's per-call fresh `HashSet` only differs from a shared `visited` on cycles the candidate filter never reaches). So 2a is verified by the full GREEN contract (logji 269/0, integration 61/0 — all `prenex_*` firing-perf guards, `test_dependent_skolem_precise_deps_single_universal`, every `test_proof_trace_*`, the Stage-1 trace-consistency guards, GDPR full corpus + DDI + Ch12 watchdog) PLUS the gasnu CYP fuel probe (the 3-var prenex join → TRUE under default 10B fuel, no `[Limit]`; negative control FALSE) — the hard gate that a wrong filter would blow without any unit-test failure. `just ci` green, `smoke-gasnu-script` green, `verify-book-capture` clean except the waived pre-existing Ch9 `[Find]` nondeterminism (find_witnesses, untouched here). **Deferred (Stage 2b/2c, filed in todo.md):** the COMBO loop + four-valued machinery folding into one `try_backward_chain_core<S: TraceSink>` with a shared `visited` (the full LP-L2 close), collapsing the two formula evaluators (delete `check_formula_holds_traced`, single-eval verdict authority), and the depth-aware memo — each higher-risk and independently verifiable.

**Follow-up fix — backward-chain core merge (Stage 2b of the dual-evaluator collapse; LP-L2 closed + the 4×→1 untraced/traced collapse):** the rule-firing enumeration was implemented FOUR times in `logji/src/reasoning.rs` (untraced/traced × normal/temporal), diverging in load-bearing ways — the untraced path accumulated a **four-valued** result and threaded a **shared `visited`** (cycle-cut), while the traced path used a **boolean `.all()`**, a **fresh `HashSet` per condition** (no frame cycle-guard), and checked each firing rule's conditions twice. Stage 2b introduces a monomorphized `trait TraceSink { const RECORDING: bool; fn push; fn trace_child }` with a ZST `NoOpSink` (the untraced hot path — `const RECORDING` constant-folds every `if S::RECORDING { … }` so the recording branches are dead-code-eliminated, zero indirect-call overhead) and a `RecordingSink { steps, memo }` (re-enters `trace_predicate_provenance_typed` for child derivations). One `try_backward_chain_core<S: TraceSink>(fact, &inner, depth, &mut visited, &mut sink) -> (QueryResult, Option<u32>)` now runs the four-valued accumulation UNCONDITIONALLY (so the verdict is authoritative and byte-identical to the former `try_backward_chain_typed`) and emits `Derived`/`Negation` proof steps via `emit_derived` only when `S::RECORDING`; `try_backward_chain_typed` is a thin `core::<NoOpSink>().0` wrapper, `trace_predicate_provenance_typed` calls `core::<RecordingSink>` (its `&mut inner` narrowed to `&` — verified vestigial), and `try_backward_chain_traced_typed` is DELETED (~330 lines; net −69 in reasoning.rs). Borrow-clean: the predicate cache is a thread-local `RefCell` (not an `inner` field) and the backward-chain never mutates `inner`, so the core stays on a shared `&KnowledgeBaseInner` — no widening. **Closes LP-L2** (the recording walk threads the SAME shared `visited` as the verdict walk; the former per-condition fresh-`HashSet` cycle asymmetry is gone) and makes the traced rule-firing path natively four-valued; the recording verdict now comes from the same accumulation as the untraced verdict (so they cannot disagree on a cycle). **Verdict-NEUTRAL:** both formula evaluators and verdict authority are untouched (that flip is Stage 2c). Pinned by new `backchain_temporal_trace_label_present` (a tensed goal's `Derived` step carries the `[past]` label) + `backchain_cycle_cut_trace_parity` (untraced and recording paths agree on `Unknown(CycleCut)` — regression LOCK, honestly not RED since leaf-gating already made it consistent); the existing `test_proof_trace_du_substitution_rule_derived` guards the equality-fallback seam (the gate `root.is_none()` ⟺ the former `Option<u32>` None-trigger). Full GREEN contract: logji 271/0, integration 61/0 (all `prenex_*` firing-perf + Stage-1 + GDPR/DDI/Ch12), `just ci` green, gasnu CYP fuel probe (3-var join TRUE / negative control FALSE under default 10B fuel, no `[Limit]`), `smoke-gasnu-script` green, and `verify-book-capture` clean on EVERY proof-trace chapter (C11/C12/C18/C20/C21) — the only drift is the pre-existing, waived Ch9 `?? la .adam. nelci ma` `[Find]` nondeterminism (a `find_witnesses` `??` path, untouched here). `NoOpSink` zero-cost is guaranteed by the `const RECORDING` DCE + `#[inline(always)]` no-ops and empirically confirmed by the fuel probe (the cargo-llvm-lines spot-check was deferred — the tool is not in the toolchain). **Deferred (Stage 2c/2d, filed in todo.md):** the formula-evaluator merge (`check_formula_holds<S>`, delete `check_formula_holds_traced`, single-eval verdict authority via the existing `TraceSink`, guarded by a verdict corpus + git-stash differential because the And/Or short-circuit canonicalization can drop a right-conjunct's compute auto-ingestion) and the depth-aware memo. Also deferred: the normal↔temporal phase split inside the core (a small tense-keyed-driver dedup).

**Follow-up fix — formula-evaluator merge + verdict authority (Stage 2c of the dual-evaluator collapse; LP-M4 + the LP-M3 residual closed):** a proof query evaluated each root TWICE — `check_formula_holds` (four-valued, authoritative) for the verdict AND `check_formula_holds_traced` (boolean+step-index) for the trace — then reconciled `steps[idx].holds = result.is_true()`; the traced path also re-walked sub-formulas four-valued at every NotNode/ForAll/Exists/Predicate leaf (the Stage-1 leaf-gating). Stage 2c collapses both FORMULA evaluators into one `check_formula_holds_core<S: TraceSink>(buffer, node, subs, &mut inner, tense, &mut sink) -> (QueryResult, u32)` (reusing the 2b `TraceSink`/`NoOpSink`/`RecordingSink`): every arm computes the four-valued verdict EXACTLY as the former untraced evaluator and, when `S::RECORDING`, also pushes its `ProofStep` (`holds = verdict.is_true()`) and returns the index. The thin `check_formula_holds` wrapper delegates to `core::<NoOpSink>` (bare hot path, recording DCE'd, byte-equivalent verdict); a new `pub(super) check_formula_holds_recording` wraps `core::<RecordingSink>` for the proof path; `run_entailment_check_with_proof` makes ONE call per root and the Stage-1 root reconciliation + the NotNode/ForAll/Exists four-valued re-walks are DELETED (the sub-verdict is natively in hand — decided-failure-vs-indeterminate branches on the returned `QueryResult` being `== False`); `check_formula_holds_traced` + `check_formula_holds_inner` are GONE. **`combine_root_results` (the multi-root fold) is kept** — distinct from the formula-level `combine_*`. **And/Or now four-valued short-circuit** on a DEFINITIVELY-decided left (False for And, True for Or) — verdict-identical to `combine_conjunction`/`combine_disjunction` (the only skipped operand is the one the left already decides). Two documented, verdict-NEUTRAL consequences: (a) a short-circuited compute-RIGHT conjunct's auto-ingestion is dropped on the BARE path — but a compute predicate recomputes on every direct query, so no later verdict can depend on it (the only theoretical flip vector, a universal rule keyed on a compute relation, is not expressible through the surface compiler; probed by `compute_conjunct_ingestion_drop_is_verdict_neutral`); (b) an Unknown-LEFT And now evaluates + records the right conjunct (`Conjunction[l,r]`) where the former boolean traced path short-circuited on any falsy left — confined to non-definitive queries (book proofs are all-True), invisible to `verify-book-capture`, pinned by `unknown_left_and_evaluates_right_conjunct`. The `Or` trace rule (`DisjunctionIntro("left"/"right"/"neither")`) and the numeric `PredicateCheck(("numeric", detail))` detail are preserved verbatim. **Shipped behind a dual-path verdict corpus** (`nibli-engine/tests/verdict_corpus.rs`, committed FIRST as a green baseline): ~14 reps each asserting the verdict on BOTH `query_holds` (bare) and `query_text_with_proof` (proof) PLUS the cross-path `assert_eq!(bare, proof)` — the standing divergence guard. Full GREEN contract: logji 272/0 (all 5 Stage-1 guards + `assert_trace_consistent` + every `test_proof_trace_*` + `backchain_cycle_cut_trace_parity` + four-valued `test_query_result_*`), integration 61/0 (all `prenex_*` firing-perf), verdict_corpus 3/0, `just ci` green, gasnu CYP fuel probe (3-var join TRUE / negative control FALSE under default 10B fuel, no `[Limit]` — no perf regression from the merge), `smoke-gasnu-script` green, `verify-book-capture` clean on EVERY proof-trace chapter (C11/C12/C18/C20/C21) — the only drift remains the pre-existing, waived Ch9 `?? la .adam. nelci ma` `[Find]` nondeterminism (a `find_witnesses` path, untouched). `NoOpSink` zero-cost: `const RECORDING` DCE + `#[inline(always)]` no-ops + the fuel probe (cargo-llvm-lines deferred — not in the toolchain). **Deferred (Stage 2d):** depth-aware memo (`HashMap<String,u32>` → `HashMap<(String,usize),u32>`) closing cross-depth memo poisoning under iterative deepening — never changes verdicts. **Book:** a separate book-repo prose touch-up (Ch 11 §"how each formula type records its proof" + Ch 7 And/Or-as-`&&`/`||`) reconciles the "if the left conjunct fails the right is never evaluated" wording with the new "definitively false/true vs non-definitive" short-circuit.

**Follow-up fix — proof-memo not-found poisoning (Stage 2d, the final piece of the dual-evaluator collapse):** the proof memo (`trace_predicate_provenance_typed`'s `memo: HashMap<String,u32>`, mapping a fact's display → its already-built `ProofStep` so a repeat emits a lightweight `ProofRef`) memoized at four sites — three `holds:true` and depth-INDEPENDENT (Asserted / backward-chain `Derived` / `EqualitySubstitution`), but the fourth, the `PredicateNotFound` fallback, is `holds:false` and **depth-RELATIVE** (reached only when a True-verdict fact, encountered during trace-building at a recursion depth `>= max_chain_depth`, can't be re-derived within the remaining budget). Because DFS does not guarantee shallow-first across sibling branches, that not-found could be memoized first and then reused at a SHALLOWER depth (where the fact DOES derive) via the memo-hit `ProofRef` — "not found (see above)" for a derivable fact. Verdict-NEUTRAL (the verdict comes from the thread-local `TYPED_PRED_CACHE`, never the proof memo) and absent from all-True winning book proofs (they never reach the horizon), but a real trace-fidelity defect. **Fix (approach B): stop memoizing the depth-relative `PredicateNotFound`** — delete only that site's `memo.insert`, mirroring the verdict cache's definitive-only policy. The three `holds:true` memoizations stay, so sound provable-fact dedup (the DAG compaction the memo exists for) is preserved, and every surviving memo entry — hence every `ProofRef` — provably points at a `holds:true` derivation; a genuinely not-found fact now re-emits a fresh leaf each time (strictly honest, a few extra leaves in pathological traces). The originally-sketched depth-keyed `(String,usize)` memo (approach A) was **rejected as strictly inferior** (validated by an adversarial Plan-agent review): it would ALSO drop cross-depth dedup of provable facts AND manufacture a NEW not-found-for-provable artifact when a provable fact re-derives at a deeper recursion depth and hits the horizon — fixing nothing B misses. Pure two-line change (no type/signature/borrow churn; `memo` stays `HashMap<String,u32>`). Pinned by `proof_ref_children_are_holds_true` (the invariant, asserted over memo-heavy / du / syllogism shapes — now ALSO enforced inside `assert_trace_consistent`, so every trace guard inherits it) + `proof_trace_byte_deterministic_3x` (the trace is byte-reproducible run-to-run); a direct RED reproduction of the cross-recursion-depth poisoning is unconstructible (the 2a/2b family — the verdict cache + `any_rule_conclusion_unifies` horizon short-circuit collapse the scenario, and the query must still RETURN at that pass), so the property + determinism locks stand in. Full GREEN: logji 274/0, integration 61/0, verdict_corpus 3/0, `just ci` green, gasnu CYP fuel probe (TRUE / negative control FALSE, no `[Limit]` — the fix doesn't touch the verdict/firing path), `smoke-gasnu-script` green, `verify-book-capture` byte-clean on every proof-trace chapter (C11/C12/C18/C20/C21) — only the pre-existing waived Ch9 `[Find]` drift. Book-NEUTRAL (no recapture). **The dual-evaluator collapse (2a→2d) is COMPLETE:** one candidate filter, one backward-chain core, one formula evaluator, one verdict-authoritative proof walk, and a poisoning-free proof memo.

**Follow-up fix — proof query builds its trace once, at the resolving depth (perf):** `query_entailment_with_proof_inner` iterative-deepened `1..=max`, calling `run_entailment_check_with_proof` (which builds a full `ProofStep` tree) at EVERY probe depth and keeping only the last. The predicate cache amortizes the VERDICT across depths, but the trace-structure BUILD is NOT cross-depth-cached, so a query whose shallowest proof is at depth D rebuilt and discarded D−1 partial traces (the book's multi-hop GDPR/DDI proofs paid this every query). Split into two phases: **Phase 1** finds the resolving depth with the CHEAP untraced walk (`run_entailment_check`, no `ProofStep` alloc on the discarded probes; explicit `match`-restore on the `Err` path, never `?`); **Phase 2** builds the trace ONCE at that depth (`run_entailment_check_with_proof`, cache warm from Phase 1). Saves D−1 trace-builds for D>1; the D=1 common case costs one extra cache-cold untraced walk (negligible — no alloc). **Verdict-NEUTRAL and byte-identical trace** (validated by an adversarial Plan-agent review AND empirically): the trace descent never shortcuts on the verdict cache (it always re-derives the recorded subtree), rule selection/child order are pure functions of the rule set + bindings (cache-independent), the proof `memo` is fresh per build, and `fact_store` is set-idempotent for the only state the trace reads (`typed_fact_is_asserted`) — so pre-warming the cache / pre-running auto-ingestion across Phase 1 cannot change Phase 2's recorded tree. Pinned by `proof_trace_equals_pinned_resolving_depth` (the deepening loop's trace == a single build pinned at the resolving depth, over a 2-hop chain whose proof resolves at depth >1 — GREEN before AND after, the invariant lock). Full GREEN: logji 275/0, integration 61/0, verdict_corpus 3/0, `just ci` green, gasnu CYP fuel probe (TRUE / negative control FALSE, no `[Limit]`), `smoke-gasnu-script` green, `verify-book-capture` byte-clean on every proof-trace chapter (C11/C12/C18/C20/C21 — the multi-hop proofs directly exercise build-once-at-D) except the pre-existing waived Ch9 `[Find]` drift. Book-NEUTRAL. The verdict corpus's cross-path `assert_eq!(bare, proof)` stays the standing verdict-neutrality guard. (Filed follow-up, out of scope: `assert_typed_fact` appends duplicate `StoredFact`s to `arg_position_index` on re-ingestion — a pre-existing, trace-invisible latent memory wart; see `todo.md`.)

**Follow-up fix — witness dedup + dependent-Skolem find binding (the last `verify-book-capture` RED, Ch 9, is CLOSED):** `?? la .adam. nelci ma` (over `gerku(adam)` + `ro lo gerku cu nelci lo mlatu`) nondeterministically emitted the SAME binding twice and rendered the dependent-Skolem cat UNBOUND as `_v0 = sk_N(_)` instead of `sk_N(adam)` — the one chapter `just verify-book-capture` was red on (a long-waived `[Find]` drift). Three coordinated fixes: **(A) dedup at the find return boundary** — `query_find_inner` (logji/src/lib.rs) now canonical-sorts THEN `dedup_by_key`s binding sets (at GroundTerm level), so `count_witnesses`/`aggregate` count each distinct binding ONCE (an inflated count is a hallucinated quantity); fixes overcounting from every source the item listed (Or-overlap where one entity satisfies both disjuncts, equivalence-class expansion, the Skolem duplicate). **(B) find↔verdict candidate-collector unification** — `find_witnesses`' ExistsNode arm (logji/src/reasoning.rs) now shares the verdict path's anchored, `Unspecified`-stripped `collect_entailment_candidates` instead of a find-only `collect_candidates` + a generic non-anchored registry cartesian; the old path inserted the raw conclusion template `SkolemFn("sk_N", PatternVar)` (rendering `sk_N(_)`) and double-counted bound witnesses. Deletes the now-orphaned `collect_candidates`/`extract_from_rules`/`collect_predicate_anchors` (logji/src/kb.rs) — a DRY win continuing the dual-evaluator collapse. **(C) transitive dependent-Skolem deps (the deeper root cause)** — the 2026-06-15 "precise deps" change computed a conclusion existential's universal-dependencies by DIRECT atom co-occurrence, so an event-decomposed existential (the cat `_v1`, which co-occurs only with the event var `_ev0` in `nelci_x2(ev, cat)`, not with the dog `_v0` directly) got `dep_count 0` and was mis-registered as INDEPENDENT (`sk_N(_)`) — not just an unbound render but a latent SOUNDNESS bug (two dogs would share one cat). `compile_forall_to_rule` (logji/src/rules.rs) now restricts deps to the universals in the existential's CONNECTED COMPONENT over the consequent's variable-sharing graph (the cat reaches the dog transitively through the shared event var → `dep_count 1`); direct co-occurrence is the 1-hop case, so `zenba(de)`/`zdani(x,y,z)` are unchanged and the `members^dep_count` firing-perf guard holds (no blowup). Pinned RED→GREEN by `find_dependent_skolem_witness_event_decomposed_is_bound` (nibli-engine — the REAL event-decomposed pipeline, which the flat logji helper does not exercise) + `find_dependent_skolem_witness_is_bound_and_unique`/`count_witnesses_dedups_or_overlap`/`find_dependent_skolem_deterministic_across_instances` (logji). Full GREEN: logji 278/0, integration 62/0, `test_dependent_skolem_precise_deps_single_universal` + all `prenex_*` firing-perf guards unchanged, `just ci` green, and **`verify-book-capture` is now byte-clean on Ch 9 with NO recapture** (the engine reproduces the checked-in `sk_3(adam), sk_1(adam)`) — the previously-waived Ch9 `[Find]` drift is resolved, so every transcript chapter passes.

**Follow-up fix — compute-backend trust puncture (cache-invalidation correctness + Python hardening + trust docs):** the HIGH/security panel item is closed except a deferred provenance remainder. **(1) Cache invalidation (the correctness core):** compute auto-ingestion (built-in arithmetic OR an external TCP backend) asserts a ground fact MID-QUERY via `assert_typed_fact`, but that path did NOT invalidate the predicate result cache (`TYPED_PRED_CACHE`) — a later same-query lookup could return a stale cached verdict (order-dependent wrong answers, a determinism break in the firewall). Fixed structurally: `assert_typed_fact` (logji/src/rules.rs) now calls `clear_typed_pred_cache()` immediately after `fact_store.insert`, so "the cache is never stale w.r.t. the store" is an invariant at the MUTATION POINT instead of call-site discipline (the panel's "enforced two crates away" critique). It CLEARS entries but KEEPS the cache enabled (`clear_typed_pred_cache` doesn't touch `PRED_CACHE_ENABLED`) — cross-depth tabling is preserved and cycle-cutting is a separate `visited` set, so it's termination-safe; of the 9 `assert_typed_fact` callers the 5 compute sites run mid-query (cache enabled — the fix's target) and the 4 assertion-time callers run with the cache disabled+empty (a free no-op). Pinned RED→GREEN by `assert_typed_fact_invalidates_pred_cache` (white-box at the mutation point, via `check_predicate_in_kb_typed`) and `compute_autoassert_invalidates_stale_cache` (e2e single-query through the compute auto-assert path; both verified RED with the fix git-stashed). **(2) Python reference backend hardened** (`python/nibli_backend.py`): per-request catch-all `except Exception` (a huge-exponent `tenfa` OverflowError no longer kills the connection thread), a non-dict-JSON guard (`123`/`[1,2,3]` were an AttributeError on `.get`), and a bounded `concurrent.futures.ThreadPoolExecutor` (`NIBLI_BACKEND_WORKERS`, default 32) replacing the unbounded per-connection thread spawn (`executor.shutdown(wait=False)` on Ctrl+C). 3 new tests (`test_large_exponent_survives_connection`, `test_non_dict_json_returns_error`, `test_bounded_pool_concurrency`); `test-backend` added to the `ci` recipe so the hardening is gated. **(3) Trust model documented:** doc comments on `register_compute_dispatch`/`dispatch_to_backend` (logji/src/compute.rs) name the plaintext/unauthenticated channel, the mid-query auto-assert that downstream rules chain on, and the backend-in-trusted-computing-base requirement; book Ch 17 already documents this. **Deferred (filed in todo.md):** provenance marking of backend-derived facts — compute facts are already non-durable (no FactRecord, not replayed by rebuild), so the operator-trust model + the cache fix are the substantive defense; surfacing backend origin in proof traces (a WIT `proof-rule` change + 4 host adapters) is its own transparency follow-up. Full GREEN: logji 280/0, `just ci` green (now including `test-backend`), Python backend 14/0.

**Follow-up fix — flat direct-injected facts are now text-queryable (event-decompose at injection):** the trusted programmatic-injection APIs — `nibli-engine::assert_fact_direct`, the WIT `assert-fact` (lasna), and the REPL `:assert` — stored a FLAT `Predicate((rel, args))` (e.g. `gerku(adam)`), but a surface text query `la .adam. cu gerku` compiles to the event-decomposed `∃ev. gerku(ev) ∧ gerku_x1(ev, adam) ∧ gerku_x2(ev, zo'e)` and structurally never matched it — so an injected fact was invisible to text queries (findable only via raw-FOL / same-shape direct queries). Fixed by event-decomposing at injection: a new public `smuni::compile_injected_fact(relation, args)` (smuni/src/lib.rs) mirrors `apply_selbri`'s `Selbri::Root` arm EXACTLY — `fit_args` pads to `JbovlasteSchema::get_arity_or_default` (the SAME query-side arity, NOT the divergent `smuni_dictionary` one), then `event_decompose`, then the existing `flatten_form` — so an injected fact is byte-identical to a surface assertion of the same predicate (same fresh-event-Skolem, same role predicates). `du` is the one exception: it stays a FLAT 2-arg `du(x1,x2)` predicate (logji's union-find equality interception only fires on `relation == "du" && args.len() == 2`; decomposing it would silently disable equality reasoning — mirrored from the surface `du` arm, guarded by the existing `known_failures_fatal.rs` `du` test). `nibli-engine::assert_fact_direct` and lasna's WIT `assert_fact` now call the helper; `:assert` is fixed transitively. NO WIT/bindings regen (signature unchanged, only the stored shape). **Option B** (a query-time flat fallback) was rejected as unsound — matching the event-type predicate against a flat fact while skipping the role predicates false-positives when a query constrains a higher role place; **Option A** (document-only) was rejected as leaving the bug live. The retraction-multiplicity guard `duplicate_ground_fact_survives_retracting_one_copy` was re-encoded to inject the IDENTICAL flat fact twice at the LOW-LEVEL `kb().assert_fact` (each public injection now mints a fresh event Skolem → two `:assert gerku adam` are DISTINCT facts, so the same-ground-fact multiplicity must be exercised below the public API). Pinned RED→GREEN by `injected_fact_matches_surface_text_query`, `injected_fact_multiplace_arity_padding_matches_text_query` (a 5-place `klama` injected with 2 args, padded + matched), and `injected_fact_is_findable_as_witness` (all stash-verified RED). Verified end-to-end through the lasna WASM component: `:assert gerku adam` → `[Skolem] _ev0 ↦ sk_0` → `? la .adam. cu gerku` → `[Query] TRUE` with the event-decomposed proof trace. Full GREEN: smuni 97/0, integration 65/0, known_failures 15/0, `just ci` green, gasnu WASM probe TRUE.

**Follow-up fix — assertion atomicity (rebuild-on-failure) + `current_assertion_id` leak + rebuild integrity:** four coupled belief-revision-soundness defects in `logji/src/lib.rs`. **(1) Assertion was not atomic:** `process_assertion` processes `logic.roots` in a loop mutating live state; if a LATER root failed, `assert_fact_inner` returned via `?` BEFORE inserting the `FactRecord`, leaving earlier roots' facts in the store with no record — ORPHANED (un-listable, un-retractable). Fixed via **rebuild-on-failure**: capture the `process_assertion` Result and, on Err, `Self::rebuild_inner` to reconstruct the KB from the durable `FactRecord` registry — the failed assertion has no record, so replay reproduces the exact pre-assertion state, discarding the partial mutation. Chosen over a stage-then-commit refactor of the ~13-field mutation set (high-risk); rebuild is already the rollback mechanism for retraction, so this reuses it (O(N) only on the rare rejected-assertion path; happy path adds just a Result capture). **(2) `current_assertion_id` leaked:** the `?` skipped the `= None` clear, so the stale id mis-attributed the NEXT assertion's rules in `rule_source_map`; now cleared UNCONDITIONALLY. `assert_fact_with_id` (the persistent-replay path) never SET it — now it does (also closing the separately-filed Low item: replayed rule-producing facts no longer escape `rule_source_map`). Both paths got the atomic rollback too. **(3) `rebuild_inner` cleared `predicate_registry`,** losing user-declared `arg_sorts` (`set_predicate_sorts`) — now saved before the clear and restored into the re-populated registry. **(4) `rebuild_inner` swallowed replay errors** (`let _ = process_assertion`) — now COLLECT-AND-CONTINUE: replay every surviving fact (store stays maximally consistent), accumulate `(id, error)`, and return `Err("rebuild replay errors: …")` so a fact that fails to replay surfaces through `retract_fact` instead of vanishing silently. Pinned RED→GREEN by `multi_root_partial_failure_is_atomic` (2-root buffer, root1 a bare disjunction → Err; root0 rolled back, no orphan record), `failed_assertion_does_not_leak_assertion_id` (`current_assertion_id` is None after a failed assertion), and `rebuild_preserves_user_arg_sorts` (sorts survive a ForAll-retraction rebuild) — all stash-verified RED. The Part-4 behavioral change (retract→rebuild now surfaces replay errors) broke no retraction/persistence corpus. Full GREEN: logji 283/0, integration 65/0, known_failures 15/0, `just ci` green (incl. `test-store`/`test-persistence-replay`/`test-sync-retraction`).

**Follow-up fix — lasna go'i snapshot trap (graft-from-clone) + fail-closed nested go'i:** lasna resolves the `go'i` pro-bridi by storing a `SelbriSnapshot` of the last bridi in `last_relation` and grafting it into the current parse when go'i appears — but `graft_snapshot` DRAINED the stored snapshot's vecs via `Vec::append`, and only `assert_text` wrote `last_relation` back, so two well-formed inputs trapped the WASM component: `mi klama` (assert) → `? go'i` (query drains the snapshot) → `? go'i` again (grafts the empty husk → `root = snap.root + sb` out of bounds → indexing panic = component trap; gasnu survived via the 2026-06-12 trap-recovery rebuild but the command was lost and the guest bug stood). Fixed by grafting from a CLONE (`SelbriSnapshot` already `#[derive(Clone)]`): `resolve_go_i` now does `last_snapshot.as_ref().map(|snap| graft_snapshot(ast, &mut snap.clone()))`, so the stored snapshot is never mutated and go'i is idempotent across calls. ALSO (secondary, same item): go'i in a position the resolver never descends into (abstraction body `mi nelci lo nu go'i`, relative clause, or tanru `mi sutra go'i`) silently compiled a literal `go'i` predicate — now FAILS CLOSED with an explicit error via a read-only reachability walk from `ast.roots` (`sentence_reaches_go_i`/`selbri_reaches_go_i`/`sumti_reaches_go_i`, exhaustive matches mirroring the `rebase_*` nested-index ground truth; a resolved top-level go'i's residual node is unreachable, so only genuinely-nested go'i is flagged). Pure Rust logic in `lasna/src/lib.rs` — no WIT/bindings change. Pinned RED→GREEN by lasna's FIRST session-lifecycle tests — `resolve_go_i_does_not_drain_snapshot`, `repeated_go_i_resolves_in_bounds`, `nested_go_i_is_rejected` (all verified RED via a temporary in-file fix-revert) — plus a `smoke-gasnu-goi` end-to-end recipe (`la .adam. cu gerku` then `? la .adam. go'i` TWICE → both `[Query] TRUE` with the event-decomposed proof, no trap-rebuild, no brick; on `main` the 2nd query traps). Full GREEN: lasna 16/0, `just ci` green, `smoke-gasnu-goi`/`smoke-gasnu-script`/`smoke-gasnu-trap-recovery` pass, `verify-book-capture` byte-clean (no book transcript uses nested go'i). Deferred (filed in todo.md): the snapshot is selbri-only (bare `? go'i` repeats the selbri with the new/empty sumti, not the full bridi → `mi klama` then `? go'i` is FALSE); queries don't update `last_relation`; recursive resolution of nested go'i.

**Follow-up fix — count-quantifier retraction leak (route CountNode retraction to rebuild):** a count-quantified assertion (`Count(x, n, body)`) generates `n-1` extra Skolem witnesses during assertion (`generate_count_extra_witnesses`: `fresh_skolem` + `note_entity`, plus an asserted witness fact for a flat body). On retraction, `retract_fact_inner`'s `has_skolems` check matched only `ExistsNode`/`ForAllNode`, so a flat `CountNode` buffer took the INCREMENTAL path — which removes only the buffer's own ground leaves (a CountNode has none) — leaking the generated witnesses (facts + `known_entities`) and the advanced `skolem_counter` into later ∀/∃/Count/find enumeration. Fixed by adding `LogicNode::CountNode(_)` to the `has_skolems` match (logji/src/lib.rs), so count retractions take the full `rebuild_inner` path (resets counters/entities, replays surviving records with the retracted count assertion excluded → its witnesses are never regenerated) — composing with the just-hardened rebuild (predicate_registry preserved, replay errors propagated). Honest scope: surface `ci lo gerku cu danlu` event-decomposes its body (carrying an event `ExistsNode`), so it ALREADY rebuilt — the leak was reachable only via flat `CountNode` buffers (a defensive belief-revision-soundness fix). Pinned RED→GREEN (stash-verified) by `retract_count_quantified_fact_removes_witnesses` (assert `Count(x,2,gerku)` → a witness dog exists → retract → 0 witnesses). logji 284/0, `just ci` green; `test_count_exact_match`/`test_count_mismatch` and the retraction/rebuild suite unchanged.

**Follow-up fix — IR diet Phase 1 (the StoredLogicNode persistence mirror is ELIMINATED):** the TODO named *seven* parallel logic-IR representations bridged by six hand-written conversion lattices, so adding one IR variant touched ~8 sites across 6 crates. Exploration confirmed most are NOT eliminable (smuni's interner tree, logji's post-Skolem `GroundTerm`, the auto-generated per-crate WIT bindings, and nibli-protocol's distinct gossip/JSON wire format are all load-bearing distinct concerns), but exactly ONE was a true structural mirror that existed only because the canonical neutral IR lacked serde: `nibli-store::StoredLogicNode`/`StoredLogicBuffer` (a hand-maintained copy of `nibli_types::logic::LogicNode`/`LogicBuffer`, with nibli-engine carrying a 6-function conversion lattice — `term_to_stored`/`*_from_stored`/`node_*`/`buf_*`, ~80 lines — to bridge them). Phase 1 deletes the mirror: the zero-dep `nibli-types` crate gains an **optional `serde` feature** (`#[cfg_attr(feature = "serde", derive(Serialize, Deserialize))]` on `LogicalTerm`/`LogicNode`/`LogicBuffer`; `PartialEq` added to `ProofRule` for Phase-2 round-trip tests; the feature stays OFF for lasna's WASM build, so no serde leaks into the guest — nibli-engine is not in lasna's dep tree), nibli-engine enables it and persists `nibli_types::logic::LogicBuffer` **directly** via postcard (write path `postcard::to_allocvec(&buf)`, replay path `postcard::from_bytes::<LogicBuffer>(&fact.payload)` — the conversion lattice is gone), and `nibli-store` deletes `StoredLogicNode`/`StoredLogicBuffer` while **keeping** `StoredLogicalTerm` (still used by `StoredAssertion::Direct`, gasnu's direct-fact path — gasnu is unchanged). The postcard byte layout changed, so `SCHEMA_VERSION` is bumped **1→2**; the existing version guard hard-rejects a stale DB (real persisted DBs — gasnu's `nibli.redb`, the seeded `gdpr-agent.redb`/`drug-agent.redb` — are regenerable from their `.lojban` seeds; a per-payload migration is a separate follow-up). Pinned by `logic_buffer_serde_postcard_roundtrip_covers_all_variants` (all 13 nodes + 5 terms through postcard, ported from the deleted nibli-store roundtrip test) + `open_rejects_mismatched_schema_version` (write v1 meta directly → reopen → `Err(SchemaVersion { expected: 2, found: 1 })`); the existing disk round-trip integration tests (`persistent_engine_replays_asserted_facts_after_reopen` / `_honors_store_retractions_after_reopen` / `_retraction_via_engine_api_survives_reopen`) stay green as the behavioral write↔replay-inverse gate. Verified `just check` → `just test-store` (19/0) → `just test-engine` (65/0) → `just test-persistence-replay` → full `just ci` green. **Phase 2 (conversion-lattice codegen) was evaluated and DECLINED on readability grounds:** mapping the actual conversion sites showed three distinct `ProofRule` converters across two RHS families — lasna `convert_proof_rule` (→ WIT guest, tuple variants), nibli-engine `rule_to_json` and gasnu `rule_to_proto` (both → nibli-protocol, but with *named struct fields whose names are bespoke per variant*, e.g. `DisjunctionCheck { detail }`, `ExistsWitness { var, term }`, `EqualitySubstitution { original, du_facts, substituted }`). A `macro_rules!` codegen can't *derive* those field names from a variant list — it must spell all 19 out anyway — and the only genuinely-identical pair (nibli-engine ≡ gasnu) is a mere 2× collapse (lasna's RHS differs), below the rule-of-three threshold and costing jump-to-definition navigability for those call sites. Instead, the high-value/low-risk piece of the plan's intent shipped: a `#[doc(hidden)] __exhaustiveness_guard(&LogicNode, &LogicalTerm, &ProofRule)` anchor in `nibli-types/src/logic.rs` whose wildcard-free matches force `E0004` at ONE discoverable location the moment any of the three enums gains a variant, with a doc-comment checklist naming every conversion site (lasna / nibli-engine / gasnu / `wit/world.wit` / nibli-protocol / logji + the serde round-trip test) a contributor must then update. Pinned by `exhaustiveness_guard_is_callable`; the readable hand-written converters are kept and the residual duplication is accepted by design. logji/smuni/gerna unaffected; full `just ci` green.

**Follow-up fix — connected sumti under a place tag / BAI modal + CLL place counter (smuni soundness):** two coupled defects in `smuni`'s `compile_bridi` (`smuni/src/semantic/compile.rs`), both silent meaning-loss in a zero-hallucination prover. **(A) connected sumti under a tag/modal dropped the right operand.** gerna parses `fa mi .e do klama` as `Tagged(Fa, Connected(mi, Je, do))` and `mi klama ri'a do .e ti` as `… ModalTagged(Ria, Connected(do, Je, ti))` (the tag is parsed first, then `try_parse_sumti` consumes the connective INSIDE — gerna/src/grammar/sumti.rs:19-39, 112-121), so the connected sumti is nested UNDER the wrapper. The old distribution loop matched only a BARE top-level `Sumti::Connected`, so the wrapped case fell through to `resolve_sumti`, whose Connected arm returns only the LEFT operand — `.e do` / `.e ti` were silently dropped (`fa mi .e do klama` ≡ `fa mi klama`). Fixed by generalizing distribution: a new `find_connected_term` peeks one wrapper layer (bare / `Tagged` / `ModalTagged`) and `distribute_connected` splits the bridi into two, PRESERVING the wrapper over each operand — for the tagged/modal case it synthesizes `Tagged(tag, operand)` / `ModalTagged(modal, operand)` nodes into an owned `Vec<Sumti>` (the original buffer is an immutable slice) and recurses with `&ext`; the bare case reuses existing operand ids unchanged (byte-identical to the prior path). The BAI-modal case correctly yields TWO flat modal predicates (`rinka(do,mi) ∧ rinka(ti,mi)`). **Multiple connected sumti in one bridi all distribute** via the existing early-return + recursion (the recursive `compile_bridi` re-scans `&ext` for the next connected term) — the filed "only the first connected sumti is split" Low item was a MISDIAGNOSIS for the bare case (already correct; now pinned by `test_two_connected_sumti_both_split`). **(B) untagged-after-tagged placement ignored the CLL place counter.** The old code filled untagged sumti into the first FREE slot; CLL (ch.9, FA cmavo) says `fa/fe/fi/fo/fu` set a running place counter and a following UNTAGGED sumti fills the place AFTER the last tag. Canonical `klama fi le zarci do` wrongly put `do` in x1; now a single-pass `next_place` counter (tags set `next_place = idx+1`; untagged skip already-filled slots then fill `next_place`; BAI modals do NOT advance it) correctly places `le zarci` in x3 and `do` in x4. The FA-overflow firewall and the `du` n-ary fail-closed check are preserved (the latter now driven by an `overflow_untagged` counter replacing the old `untagged_iter.len()`); the `ke'a` x1 pre-fill is preserved by the skip-filled-slots step. Added `PlaceTag::to_index()`/`name()` to `nibli-types`. Pinned RED→GREEN by `test_fa_tagged_connected_distributes`, `test_bai_modal_connected_distributes`, `test_two_connected_sumti_both_split`, `test_cll_place_counter_resumes_after_fi`, `test_untagged_before_tag_still_fills_x1` (smuni) + `connected_sumti_under_fa_holds_for_both`, `connected_under_fa_negative_control`, `cll_place_counter_fi_then_untagged` (nibli-engine integration); 3 smuni + 2 integration verified RED pre-fix for the documented reasons. smuni 102/0, integration 68/0, full `just ci` green (GDPR/DDI/verdict corpora unchanged). **Deferred (filed in todo.md):** three small fail-closed hardening remainders kept out of scope — over-arity UNTAGGED sumti still silent-drop (only `du` fails closed), tag-on-tag collision (`fe X fe Y`) still last-wins, and connected sumti in `be`-args / relative-clause bodies (positions the distributor doesn't descend into) still keep only the left operand in `resolve_sumti`'s residual Connected arm.

**Follow-up fix — quantifier-closure scoping: `ma_vars` frame isolation + `da`/`de`/`di` in BAI modals / be-bei args (smuni soundness):** two coupled under-quantification defects in `smuni`'s `compile_bridi` (`smuni/src/semantic/compile.rs`), both producing malformed/under-quantified FOL. **(1) `ma_vars` bleed.** `SemanticCompiler.ma_vars` is a compiler-global accumulator (`resolve_sumti`'s `ma` arm pushes a fresh var; `compile_bridi` drained the WHOLE vec at its end). A relative-clause body (`resolve_sumti` `Sumti::Restricted`) or abstraction body (`apply_selbri` `Selbri::Abstraction`) triggers a NESTED `compile_bridi` mid-flight, whose `drain(..)` stole the OUTER bridi's pending `ma` vars (unlike `rel_clause_var`/`kea_used`/`pending_clause_subject`/`ka_open_var`, `ma_vars` was not frame-isolated) — leaving the outer matrix's `ma` var FREE (masked downstream only because logji binds witnesses by name). Fixed by frame-scoping the drain exactly like `matrix_conjunct_checkpoint`: `let ma_checkpoint = self.ma_vars.len();` at `compile_bridi` entry, and `self.ma_vars.drain(ma_checkpoint..)` at the close — each frame drains only the `ma` vars it pushed; a nested frame's checkpoint is taken AFTER ancestor pushes, so it can no longer steal them (no per-call-site save/restore needed; at top level the vec is empty between roots so `drain(0..) == drain(..)`, behavior-identical). **(2) `da`/`de`/`di` in a BAI modal or be/bei arg was never existentially closed.** The closure collection scanned ONLY `&args` (the main bridi's positioned args); a `da` inside `ri'a da` (conjoined as a flat `rinka(da, mi)` predicate) or `klama be da` (merged into an event role predicate inside `apply_selbri`'s `WithArgs`) never appears in `&args`, so it stayed a free `Variable(da)` → logji stored it as `GroundTerm::PatternVar` in a "ground fact" (non-ground masquerading as ground). Fixed by replacing the `&args` scan with a binder-tracking pre-order walk of the ALREADY-BUILT `final_form` (new associated fn `collect_free_logic_vars`): at the scan point `final_form` already contains the modal predicates and the be/bei role predicates, but NOT yet the description ForAll/Exists (folded just below) nor the rel-clause restrictors (closed by their own frame), so the walk catches exactly the right vars. Binder tracking (push on `Exists`/`ForAll`/`Count`, including `Count` as a binder) skips a `da` an abstraction body already closed (no double-wrap) and the `prenex_vars` check is preserved (a prenex-bound `da` in a modal is NOT re-closed). The result feeds the UNCHANGED `has_universal_quantifier` inside-vs-outside-ForAll wrap, so a modal `da` under a universal (`ro lo gerku cu klama ri'a da`) is closed by `Exists` UNDER the ForAll — root stays ForAll for logji's rule compilation. Pinned RED→GREEN by `test_ma_in_rel_clause_not_stolen`, `test_da_in_bai_modal_closed`, `test_da_in_be_arg_closed`, `test_da_in_modal_under_universal_stays_inside_forall`, `test_be_arg_and_modal_da_de_both_closed` (5 verified RED pre-fix — the ma test leaked the fresh ma var `_v0`, the others left `da`/`de` free), plus the GREEN guards `test_prenex_da_in_modal_not_reclosed` (prenex `da` not re-closed), `test_abstraction_da_not_double_wrapped` (exactly one `Exists(da)`), and `test_du_with_da_closed` (flat-du shape doesn't hide the var); via a new `free_vars`/`count_exists_binding` test-helper pair. smuni 110/0, full `just ci` green (GDPR/DDI/verdict corpora + all existing `da`/`de`/`di`/`ma` tests — which are order-agnostic/dedup — unchanged). **Deferred (filed in todo.md):** position-aware scope — a `da` textually BEFORE a `ro lo` still compiles ∀∃ (wrapped on the presence of ANY universal, not left-to-right textual position), sound but imprecise; likewise `da` interleaved with an ExactCount.

**Follow-up fix — tensed rule antecedents compile + fire (logji completeness):** a universal/conditional rule with a TENSED ANTECEDENT — `ro lo gerku poi pu citka cu xagji` ("every dog that ATE is hungry"), or the ground conditional `ganai la .adam. pu bajra gi la .adam. danlu` — was REJECTED at compile time (fail-closed). smuni wraps tense LAST in `compile_bridi` (after event decomposition), so the `poi pu citka` condition is `Past(Exists(ev, And(citka(ev), citka_x1(ev,x))))`; logji's condition machinery (`collect_condition_exists`, `flatten_conjuncts_through_exists`, `build_rule_template_fact`) never descended through `Past/Present/Future`, so the whole tensed sub-tree reached `build_rule_template_fact` as one opaque atom → its `_ => None` arm → the caller fail-closed the entire rule. **The matching/firing side was already fully tense-agnostic** — `unify_facts` (`kb.rs`) requires EXACT tense-wrapper match (a `Past(citka(x__v0))` template unifies a `Past(citka(rex))` fact and never a Bare/Future one), the event-var enumeration / `filter_event_candidates` operate on `fact.inner().args`, and temporal lifting's `apply_tense_to_fact` re-tenses ONLY `Bare` facts (so an intrinsically-tensed condition is never clobbered). So the fix is COMPILE-ONLY, in `logji/src/rules.rs`, mirroring the proven assert-path idiom (`build_stored_fact_from_node`/`collect_ground_facts`, which already thread a `tense: Option<&str>` accumulator): (1) `collect_condition_exists` descends `Past/Present/Future/Obligatory/Permitted` so the event `ev` under a tensed condition is registered as a pattern var; (2) `flatten_conjuncts_through_exists` carries `tense: Option<&'static str>` and returns `Vec<(u32, Option<&'static str>)>`, descending `Past/Present/Future` (→ `Some("Past")`…) so each leaf atom carries its tense; (3) `build_rule_template_fact[_with_negation]` take a `tense` param and emit `StoredFact::with_tense(...)` (the `_ => None` catch-all stays, so disjunction / nested-quantifier / deontic antecedents + tensed CONCLUSIONS still fail-closed); the xorlo-presupposition witness path threads the same tense. CONCLUSIONS stay bare (tensed-goal derivation from a bare conclusion is already handled by temporal lifting). Pinned RED→GREEN by `test_tensed_antecedent_rule_compiles_and_fires` + the `_no_premise`/`_wrong_tense`/`_bare_premise` negative controls (logji, all 4 RED pre-fix — the rule failed to compile so `assert_buf` panicked) and `tensed_restrictor_rule_fires` + 3 controls + `ganai_tensed_antecedent_fires_with_premise` (nibli-engine integration). The existing `ganai_tensed_antecedent_must_not_fire_unconditionally` guard now exercises the compile-succeeds-but-does-not-fire path (it was `if assert.is_ok() { assert !fires }` and its comment already anticipated the fix) — no edit needed; soundness holds because the tensed condition is checked against the store with exact tense. WHOLE-RULE tense (a top-level `pu` wrapping the entire universal) is still stripped by the spine walk (marked `// SCOPE:`), deontic/disjunctive antecedents stay fail-closed — both deferred in todo.md. logji 288/0, integration 73/0, full `just ci` green (temporal-lifting + tense-discrimination + GDPR/DDI/verdict corpora unchanged).

**Follow-up fix — gasnu restart-replay fact-ID drift (production-hardening, belief-revision durability):** gasnu (the WASM-host REPL) persists facts to redb via `nibli-store`, but on restart `replay_persisted` re-asserted each persisted fact via the WIT `session.assert-text`/`assert-fact` calls — which mint FRESH guest ids (0,1,2…) and DISCARD the returned id, never using the persisted `StoredFactRecord.id`. So the live session's fact-id namespace DIVERGED from the durable store's: `:retract <id>` (which retracts the live session AND tombstones `nibli_store` by id) and `:facts` (shows live ids) operated on mismatched namespaces → after a reopen, the wrong store record was tombstoned / `:facts` showed wrong ids / a surviving fact became an un-retractable "zombie" (worst with a tombstone gap — e.g. assert 0,1,2; retract 1; reopen → live {0,1}, store {0,2}). `:merge` had the same bug. The native `nibli-engine` already does it right (`replay_from_store` → `kb.assert_fact_with_id(buf, label, fact.id)`); gasnu lacked the WIT surface to. Fixed by adding `session.assert-text-with-id`/`assert-fact-with-id` to `wit/world.wit` (lasna threads the caller-chosen id to the logji KB's `assert_fact_with_id`, which honors the id and advances `fact_counter`; the `last_relation` go'i snapshot + the parse-warnings abort are preserved via shared `assert_*_inner(Option<u64>)` helpers). gasnu now: (1) replays the durable store id at EVERY replay path — startup `replay_persisted`, `:merge`, and the trap-recovery JOURNAL — via the with-id calls, so live==store; (2) the host journal carries the id (`AssertText { text, id }`/`AssertDirect { relation, args, id }`), with new-fact paths (`:assert`/`:load`/bare-text) journaling the freshly-minted guest id and replay paths journaling the store id — so a trap-rebuild AFTER a restart reconstructs the SAME id namespace (proven necessary by the tombstone-gap trace: a fresh process resets the guest counter to 0, so fresh-id replay drifts past any gap). `Retract`/`:retract` are unchanged (ids are now stable). The **redb schema is UNCHANGED** (`SCHEMA_VERSION` stays 2; `StoredAssertion`/`StoredFactRecord` untouched) — no migration, and an existing drifted `nibli.redb` self-corrects on reopen (its stored ids become the live ids). Pinned by a new `smoke-gasnu-persist-replay` recipe (pre-release; RED on the pre-fix build): run-1 asserts 3 facts + retracts the middle one, run-2 reopens the SAME db and must show the surviving STORE ids (`#0`,`#2`, gap preserved, no zombie `#1`), retract the high store id (`#2`), and the query flips FALSE. `cargo component build -p lasna` regenerated `lasna/src/bindings.rs` (gitignored — regenerated from the committed WIT); gasnu's `bindgen!` picks the host calls up at compile. Verified: `just check`, full `just ci` green, `just build-wasm`, the new `smoke-gasnu-persist-replay` PASS, and the existing `smoke-gasnu-script`/`-trap-recovery`/`-goi` (the journal-rebuild + `last_relation` canaries) stay green. **Deferred (todo.md):** the dead `WasiFactStore` cleanup; CRDT cross-node id-collision on `:merge` (pre-existing, the with-id replay faithfully reflects the store's post-merge decision).

**Follow-up cleanup — delete dead `WasiFactStore`:** `logji/src/wasi_fact_store.rs` (a WASI-guest-side append-only/LRU fact store) was gated by a `wasi-store` Cargo feature that was never enabled anywhere and never instantiated outside its own tests — superseded by the host-side gasnu/nibli-store persistence. Deleted the file, the `#[cfg(feature = "wasi-store")] pub mod wasi_fact_store;` line (logji/src/lib.rs), the `wasi-store` feature, and the now-orphaned `postcard` optional dep (logji/Cargo.toml — `postcard` was used ONLY by that file; the `serde` feature/dep stays, used by `StoredFact`'s serde derive). The `FactStore` trait + `KnowledgeBase::with_store` API are unchanged (still used by nibli-engine's redb store). No behavior change. Full `just ci` green.

**Follow-up fix — compute dispatch thread-local → per-instance (architecture + server-compute unblock, part 1 of 2):** logji's external compute dispatch was a pair of thread-local fn-pointer globals (`EVAL_FN`/`BATCH_EVAL_FN` in compute.rs) set by `register_compute_dispatch`. On the multithreaded `nibli-server` (reasoning runs in `tokio::task::spawn_blocking` workers) the registration would land on one worker's thread-local while reasoning ran on another → every worker saw `None` → external compute (the Python backend: tenfa/dugri/custom predicates; built-in pilji/sumji/dilcu work without dispatch) was structurally UNREGISTRABLE, and it contradicted logji's documented "no global state" model. Moved the dispatch into `KnowledgeBaseInner` as `compute_eval`/`compute_batch_eval` (plain `Option<fn>` — fn-pointers are `Copy`, read through a shared `&inner` with no interior mutability; mirrors the `cancel` flag exactly: in `Clone`, `new()`→None, NOT cleared by `reset()` since it is configuration not derived state). `register_compute_dispatch` (thread-local) is DELETED and replaced by per-KB `KnowledgeBase::set_compute_dispatch` (+ a `NibliEngine` passthrough + a `EngineComputeRequest` re-export). `dispatch_to_backend`/`dispatch_batch_to_backend`/`try_evaluate_numeric_group`/`batch_evaluate_compute_for_members` now take `&KnowledgeBaseInner`; the reasoning call sites pass `&*inner` (a `&mut`→`&` reborrow — legal, coexists with the prior `members` shared borrow). lasna's `Session::new` registers per-KB (`kb.set_compute_dispatch(eval_via_host, batch_eval_via_host)`) instead of the thread-local global. Pinned by `per_instance_compute_dispatch_is_isolated` (nibli-engine integration): engine_a with a stub dispatch resolves external `tenfa` TRUE, engine_b with the SAME compute-predicate registration but NO dispatch returns FALSE on the same thread — independence the old shared thread-local could not provide. Verified: `just check`, full `just ci` green (surface-numeric pilji/sumji/dilcu + all suites unchanged), `just build-wasm` + `smoke-gasnu-script`/`-trap-recovery`/`-goi` green (lasna now registers per-KB). **Deferred:** the predicate-result cache thread-local (part 2); a real native TCP compute-backend client for nibli-engine/nibli-server (`NIBLI_COMPUTE_ADDR` socket, mirroring gasnu's host bridge) — this change only unblocks the mechanism + adds the `set_compute_dispatch` API.

**Follow-up fix — predicate-result cache thread-local → per-instance (architecture, part 2 of 2; logji is now genuinely global-state-free):** the predicate-result cache — the backbone of cross-depth tabling under iterative deepening — was the last pair of thread-local globals in logji (`TYPED_PRED_CACHE: RefCell<HashMap<StoredFact, QueryResult>>` in reasoning.rs + `PRED_CACHE_ENABLED: Cell<bool>` in kb.rs), shared across every KB on a thread, with multi-KB-per-thread correctness resting on clear-at-every-query discipline enforced two crates away. Moved both into `KnowledgeBaseInner` as `pred_cache: RefCell<HashMap<StoredFact, QueryResult>>` + `pred_cache_enabled: Cell<bool>` (interior mutability — the backward-chain reads/writes through the shared `&inner` in `check_predicate_in_kb_typed`; `Clone`→fresh empty/false, `new()`→empty/false, `reset()`→clear+false). The cache accessors now take `&KnowledgeBaseInner`: `clear_typed_pred_cache(inner)` (`inner.pred_cache.borrow_mut().clear()`), `clear_and_enable_pred_cache(inner)`, `enable_pred_cache(inner)`, `invalidate_pred_cache(inner)` (clear+disable); the in-`check_predicate_in_kb_typed` lookup/write gate on `inner.pred_cache_enabled.get()`. The ~14 orchestration sites thread `&inner` while preserving the exact tabling protocol — **clear-once at query start, enable-no-clear per iterative-deepening pass** (cross-depth tabling), clear-keep-enabled at mid-query compute auto-ingest (`assert_typed_fact`, which now clears the per-instance cache right after `fact_store.insert`), clear+disable at assert/retract/reset, only-definitive caching unchanged. The 3 query-start sites (`query_entailment_inner`/`query_entailment_with_proof_inner`/`query_find_inner`) use a short set-and-drop borrow (`let configured_max = { let inner = self.inner.borrow(); clear_and_enable_pred_cache(&inner); inner.max_chain_depth };`) — the enabled-flag+cleared-cache persist in `inner` across the deepening re-borrow loop; all cache access routes through the passed `&inner` (never a fresh `self.inner` re-borrow under a live `borrow_mut`). **Byte-identical for single-KB embedders** (gasnu/lasna/nibli-engine — one KB per thread/session); a strict isolation improvement for the multithreaded server. Pinned by `pred_cache_is_per_instance_no_cross_kb_leak` (KB-A caches `False` for `danlu(adam)`; KB-B has `gerku(adam)` + `∀x.gerku(x)→danlu(x)` so derives TRUE — no cross-KB poison, structurally impossible to fail with per-instance caches) plus the unchanged tabling guards (`proof_trace_equals_pinned_resolving_depth`, `assert_typed_fact_invalidates_pred_cache`, `compute_autoassert_invalidates_stale_cache`, verdict_corpus, GDPR full corpus, DDI, Ch12 watchdog). The "no global state" doc claim (logji/src/lib.rs:18) is now literally true — all mutable state (facts, rules, the predicate-result cache, compute dispatch, the cancel flag) lives per-instance on `KnowledgeBaseInner`; there are no global or thread-local statics. Verified: `just check`, full `just ci` green (logji 289/0 incl. the new isolation test), `just test-engine` 65/0, `just build-wasm` + all four gasnu smokes (`-script`/`-trap-recovery`/`-goi`/`-persist-replay`) green. **Deferred (unchanged):** the real native TCP compute-backend client for nibli-engine/nibli-server is a separate FOLLOW-UP feature; this two-part change unblocks the mechanism + adds the `set_compute_dispatch` API.

**Follow-up fix — `noi` (non-restrictive) relative clauses + `close_quantifier` DRY (smuni soundness):** `noi` was compiled IDENTICALLY to `poi` (restrictive) — `resolve_sumti`'s `Sumti::Restricted` arm never inspected `rel_clause.kind`, folding every clause body into the quantifier's DOMAIN restrictor. So `ro lo gerku noi barda cu klama` ("every dog, which is big, goes") compiled the wrong rule `∀x. (gerku(x) ∧ barda(x)) → klama(x)` — it wrongly REQUIRED each dog to be big and never derived that the dogs ARE big. Fixed in two commits. **(1) DRY prerequisite:** the ~80-line 5-arm quantifier-closure loop was duplicated AND already diverged across `compile.rs` (bridi-level) and `selbri.rs` (be/bei args); extracted ONE `close_quantifier(entry, form_to_wrap, …)` method on `SemanticCompiler` (`smuni/src/semantic/helpers.rs`), both `.into_iter().rev()` loops now call it (`entry` moved by value → no `&mut self` aliasing). Pure refactor, byte-identical (smuni 110/0, integration 74/0, verdict_corpus 3/0). **(2) `noi` soundness:** added `noi_restrictor: Option<LogicalForm>` to `QuantifierEntry` (distinct from the poi/voi `restrictor`); the Restricted arm now branches on `rel_clause.kind` — `Noi` routes to `noi_restrictor`, `Poi`/`Voi` to `restrictor`, each with its own `.take()`-conjoin so mixed/stacked clauses (`poi P noi Q`) populate both fields independently; `close_quantifier` folds `noi_restrictor` on the MATRIX side — for `Universal`/`UniversalLe` into the CONSEQUENT (`∀x. desc(x) → (matrix ∧ noi)`, which is exactly `(∀x.desc→matrix) ∧ (∀x.desc→noi)` — the standard non-restrictive reading), for `Existential` into the body (logically identical to poi under ∃, ∧ is flat). The `noi` clause is Neo-Davidsonian event-decomposed, so a universal with `noi` yields a TWO-existential consequent (`klama-event ∧ barda-event`); `compile_forall_to_rule` registers both as dependent Skolems in separate connected components (each `dep_count 1`, no `members^dep_count` blowup, no fail-closed rejection) and fires — a previously unexercised path. **Residual (documented + pinned):** under an exact-count quantifier `noi` is folded into the counted body (== restrictive) — the principled `Count(…) ∧ ∀x.(desc→noi)` would need `close_quantifier` to emit two conjuncts; this is a STRICT improvement (the status quo already treated all `noi` restrictively, so Count is left where it was, not regressed). `voi` stays grouped with `poi` (restrictive-designating, sound default). Pinned RED→GREEN by `noi_incidental_predicate_is_asserted` (nibli-engine integration — `gerku(rex)` + the noi rule derives BOTH `klama(rex)` AND `barda(rex)`; RED pre-fix since `barda` was an antecedent) + `poi_restrictive_does_not_assert_incidental` (guard) and the smuni structural `noi_restrictor_is_in_universal_consequent_not_antecedent` / `poi_restrictor_stays_in_universal_antecedent` / `noi_under_exact_count_is_restrictive_documented_residual`. Doc claims at `smuni/src/semantic.rs:14`/`:53` fixed; book C5/C6 prose flipped (they previously PROMISED `noi==poi` as a disclosed limitation). smuni 113/0, integration 76/0, verdict_corpus 3/0, known_failures 15/0, full `just ci` green, `verify-book` 0 findings, `verify-book-capture` byte-clean on every transcript chapter. **Deferred (filed in todo.md, unchanged):** the exact-count residual; the small connected-sumti fail-closed remainders (incl. connected sumti in rel-clause bodies).

**Follow-up fix — pre-sort rule buckets, drop the per-node clone + re-sort (logji hot-path perf):** `collect_matching_rules_typed` ran on every backward-chaining node visit and did `universal_rules.get(rel).cloned()` (cloning every `Arc` in the bucket — refcount bumps) THEN `.sort_by_key(Reverse(priority))` — **every call, in the hot recursion** — even though the buckets (`HashMap<String, Vec<Arc<UniversalRuleRecord>>>`) only change at rule registration / priority-change. Moved the sort from the hot READ path to the cold WRITE path: each bucket is now kept descending-priority-sorted at mutation time (a new `sort_rule_bucket` helper called in `register_rule` after each push and in `set_rule_priority` after the mutation loop — `set_rule_forward` is untouched, it doesn't change priority), and the read fn (renamed `collect_matching_rules_typed` → `matching_rules_typed`) returns a borrowed `&[Arc<UniversalRuleRecord>]` slice — **no clone, no per-call sort**. A `debug_assert!(rules.is_sorted_by_key(Reverse(priority)))` in the read fn is a suite-wide net (every query in every test checks it; zero release cost). The invariant already held by construction (new rules are priority 0 = the descending-order minimum, so appending preserves order; `set_rule_priority` sets a whole bucket equal), so this is **behavior-preserving** — the explicit write-site sorts make it robust to future changes (per-rule priority, a non-zero default). Borrow-verified at all 3 call sites incl. the `inner: &mut` site in `check_formula_holds_core` (its loop body only does shared reborrows `&*inner` + `sink.push` on a separate `&mut S`, so the shared slice borrow coexists under NLL). `trigger_forward_rules` is untouched — it collects forward rules across ALL buckets into its own Vec and sorts that (independent of per-bucket order; off by default). Pinned by `matching_rules_bucket_stays_descending_after_late_registration` (a high-priority rule then a late default-priority rule for the same conclusion → bucket stays `[p10, p0]`) + the suite-wide debug_assert; existing `test_priority_*` unchanged. logji 290/0, integration 76/0 (GDPR/DDI preserved), verdict_corpus 3/0, full `just ci` green, `just build-wasm` + `smoke-gasnu-script` green (the native `prenex_cross_entity_join_fires` already passes, and the change strictly REDUCES per-node WASM fuel, so the multi-var join can only get more comfortable under budget).

**Follow-up fix — dedup `arg_position_index` on re-ingestion (logji memory growth + join-binding suppression):** `assert_typed_fact` (the single path that adds a fact to BOTH the `fact_store` HashSet and the secondary `arg_position_index` — `HashMap<(String, usize), HashMap<GroundTerm, Vec<StoredFact>>>`) pushed into the index leaf `Vec` UNCONDITIONALLY, so re-ingesting an `Eq`-identical ground fact appended a duplicate. Two consequences: (1) **O(queries) memory growth** — compute auto-ingestion calls `assert_typed_fact` mid-query on every successful query, so a hot compute predicate (a flat, Skolem-free compute fact recomputed each query) accumulated duplicate index rows (the HashSet store absorbed the dup, the index Vec did not); (2) **join-binding suppression (soundness-adjacent)** — the sole consumer `bind_join_vars_from_index` binds a condition-only `x__` join var only when its anchored `matching` set has exactly one element (`matching.len() == 1`); duplicate leaf entries inflated that count, so a genuinely-unique match looked ambiguous and the binding was suppressed → a multi-variable join rule could fail to fire (wrong FALSE). Latent (verdict-invisible in all exercised scenarios), but real. **Fix:** gate the index population on `!inner.fact_store.contains(&fact)` — `fact_store.insert` is the ONLY insert site (verified single call), so the store and index are always populated together (*fact ∈ store ⟺ fact ∈ index*); populating the index only for a fact new to the store keeps them consistent (exactly one index entry per fact) and re-ingesting an identical fact becomes a no-op for the index. **O(1)** (one HashSet `contains`), chosen over the per-leaf `Vec::contains` guard (O(leaf), can degrade to O(n²) when many distinct facts share a `(predicate, position, value)` leaf) and over a `HashSet<StoredFact>` leaf (rejected — the leaf is iterated in insertion order by the consumer, and output determinism depends on that order; the `Vec` stays). No `FactStore` trait change (a `contains` + idempotent `insert`). Consistency across the other mutators is automatic: rebuild clears the store then replays via the same path (so `contains` is false → each surviving fact indexed once); retraction's `entries.retain(|f| f != fact)` now removes the single copy. **Note:** asserting the same SURFACE line twice does NOT trigger this — Neo-Davidsonian decomposition mints a fresh event Skolem per assertion, so those facts are distinct (not `Eq`-duplicates); the duplicate arises only from `Eq`-identical re-ingestion (compute auto-assert of a flat fact, or a direct double-assert of a flat fact). Pinned RED→GREEN by `arg_index_dedups_reingested_fact` (logji white-box: assert an identical flat fact twice → the leaf has exactly 1 entry, not 2 — git-stash-verified RED at `left: 2, right: 1`); the behavioral join-suppression case is covered transitively (the dedup makes `matching.len()` reflect distinct facts) with the `prenex_*` firing tests + verdict corpus as the behavioral net, and the multiplicity guard `test_retract_duplicate_ground_fact_keeps_live_copy` (multiplicity is tracked via FactRecords, not index dups) stays green. logji 291/0, integration 76/0 (all prenex/GDPR/DDI), verdict_corpus 3/0, full `just ci` green, `just build-wasm` + `smoke-gasnu-script` green. Determinism preserved (Vec leaf, insertion order).

**Follow-up fix — du-equivalent ASSERTED facts render as `EqualitySubstitution`, not `Asserted` (logji proof-trace honesty):** when a queried fact held because a `du`-equivalent variant was DIRECTLY ASSERTED (`du(coumadin, warfarin)` + `xukmi(coumadin)` asserted, query `xukmi(warfarin)`), the proof trace labeled it `Asserted(xukmi(warfarin))` — but `xukmi(warfarin)` was never asserted; it holds only by substituting `warfarin → coumadin` through the `du` equality. For a transparency-first prover the proof must show the substitution. Root cause: `trace_predicate_provenance_typed`'s first check was `if typed_fact_is_asserted(fact, inner) { emit Asserted }`, and `typed_fact_is_asserted` returns true for BOTH an exact store hit AND an equivalence-variant hit — collapsing them into one `Asserted` label. **Fix (logji/src/reasoning.rs):** split that single check into a two-way branch whose COMBINED condition is identical to the old `typed_fact_is_asserted` (so the trace fires in exactly the cases the verdict treats as asserted — trace↔verdict consistency preserved, only the LABEL of the equivalence sub-case changes): exact `fact_store.contains(fact)` → `Asserted`; else (equivalence non-empty) a directly-asserted equivalent variant → `EqualitySubstitution((original, du_note, substituted))` whose child recursively traces the variant (bottoming out at the asserted variant's exact-`Asserted` leaf). `typed_fact_is_asserted_with_equivalence` was refactored `bool → Option<StoredFact>` (returns the matching variant; its sole caller `typed_fact_is_asserted` now `.is_some()`s it — the verdict path's `bool` semantics are untouched). The du-note formatting was extracted into a shared `du_substitution_note(orig, variant)` helper, used by both the new asserted branch and the existing RULE-DERIVED equality path (a small DRY win). The existing rule-derived equality fallback (guarded `!= "du"`, probes variants via `check_predicate_in_kb_typed(...).is_true()`) is UNCHANGED — it now handles only the case where the equivalent variant is rule-DERIVED (not asserted), since asserted variants are caught by the new branch first (confirmed: `test_proof_trace_du_substitution_rule_derived` stays green — `danlu(adam)` is rule-derived, not asserted, so `with_equivalence` returns None for it). `EqualitySubstitution` was already wired through every surface (nibli-engine `rule_to_json`, gasnu `rule_to_proto`, lasna `convert_proof_rule`, the WIT `proof-rule` record), so NO WIT/bindings change. Pinned RED→GREEN by `test_proof_trace_du_substitution_directly_asserted` (asserts `xukmi(coumadin)` directly + `du(coumadin, warfarin)`, query `xukmi(warfarin)` traced → contains a holds:true `EqualitySubstitution`, NO `Asserted` step claims `xukmi(warfarin)`, the substitution's child is the asserted `xukmi(coumadin)`; git-stash-verified RED — pre-fix no EqualitySubstitution + a bare `Asserted(xukmi(warfarin))`). **Book-NEUTRAL:** Ch 11's `Asserted` table entry already reads "ground truth DIRECTLY asserted by user" — the fix brings the code into line with the book's existing (correct) claim rather than the reverse; `verify-book-capture` byte-clean. logji 292/0, integration 76/0 (cross-path verdict consistency via verdict_corpus 3/0, GDPR/DDI preserved), full `just ci` green, `build-wasm` + `smoke-gasnu-script` green.

**Implemented features:**
- Prenex `ro da [ro de ...] zo'u <body>`: multi-variable universally-quantified rules parse to a `Sentence::Prenex` node and lower to nested leading `ForAll` (the idiomatic multi-variable rule form). Firing is practical under the default 10B fuel budget even for multi-variable joins (firing-perf fix landed 2026-06-15: precise dependent-Skolem deps + index-anchored join — see the firing-perf follow-up fix below).
- Surface `du` (identity) selbri: `la .X. cu du la .Y.` (and the cu-less observative `la .X. du la .Y.`) parses and lowers to a FLAT 2-arg `du(x1, x2)` fact that feeds logji's union-find equality classes — drug-synonym/identity fact transfer is now writable in a corpus, not just injectable. `na du` inequality is contradiction-checked against the union-find canonical roots (direct + transitive); >2-place `du` is rejected fail-closed; `du` stays atemporal (tensed `du` does not merge).
- Lexer + recursive-descent gerna/parser (gismu, cmavo, cmevla, lujvo)
- Gadri descriptions (lo/le/la), universal (ro lo/ro le), numeric quantifiers (PA lo/le, su'o lo); description selbri supports se-conversion (`lo se krinu`), tanru (`lo sutra gerku`), and na negation (`lo na se curmi`)
- Place tags (fa/fe/fi/fo/fu), BAI modal tags (ri'a, ni'i, mu'i, ki'u, pi'o, ba'i), fi'o...fe'u
- Selbri: root, tanru (Neo-Davidsonian event decomposition — shared event variable resolves intersective fallacy), conversion (se/te/ve/xe — works both at selbri level and within tanru units for tight binding: `menli se ponse`), negation (na), grouping (ke...ke'e), compounds (zei), be...bei...be'o
- Relative clauses (poi/noi/voi) with ke'a, implicit variable injection (ambiguous cases with nested descriptions rejected as error — requires explicit ke'a), clause stacking
- Sumti connectives (.e/.a/.o/.u + nai), selbri connectives (je/ja/jo/ju + na negation on right operand: `je na se fanta`)
- Sentence connectives (forethought: ge...gi, ga...gi, ganai...gi; afterthought: .i je/ja/jo/ju with na/nai)
- Abstractions (nu, du'u, ka with ce'u, ni, si'o)
- Tense (pu/ca/ba), deontic attitudinals (ei/e'e)
- Quoted literals (lu...li'u), number sumti (li + PA)
- Skolemization (independent + dependent under ∀ via SkolemFn)
- All universals compile to backward-chaining rule templates (UniversalRuleRecord) + xorlo presupposition Skolems (restrictor domain guaranteed non-empty)
- SkolemFn constructor for dependent Skolems with multi-dependency support: single dep `(SkolemFn "sk_N" dep)`, multi-dep via `DepPair` nesting `(SkolemFn "sk_N" (DepPair dep0 dep1))` — handles `∀x.∀y. → ∃z.` patterns
- Demand-driven backward-chaining reasoning engine (replaced egglog): typed fact store (HashSet<StoredFact> with predicate-indexed lookup) + UniversalRuleRecord templates with typed StoredFact conditions/conclusions for structural unification via unify_facts(); ground material conditionals (Or(Not(P),Q)) auto-registered as zero-variable rules for modus ponens
- Count quantifier (exactly N) for numeric descriptions
- da/de/di existential quantifier closure (bare logic variables now properly wrapped in ∃)
- Host-managed WIT resources: `resource knowledge-base` (logji) + `resource session` (lasna interface)
- KnowledgeBase uses `RefCell` (not `Mutex`) — single-threaded WASI, no global state in logji
- Numerical comparison predicates: zmadu (>), mleca (<), dunli (==) evaluated at query time on Num terms
- Computation dispatch WIT protocol: `compute-backend` interface, `ComputeNode` IR variant, predicate registry in lasna
- Built-in arithmetic evaluation: pilji (multiply), sumji (add), dilcu (divide) with query-time dispatch fallback chain
- Host-provided compute backend with wasmtime linker integration in gasnu
- Generic external compute backend: TCP + JSON Lines client in gasnu, lazy connect, auto-reconnect
- Python reference backend server: pilji, sumji, dilcu, tenfa (exponentiation), dugri (logarithm)
- Compute result auto-ingestion: successful compute dispatch results automatically asserted into KB fact store as ground predicates
- Direct fact assertion: `assert-fact` WIT method on session resource bypasses Lojban parsing for trusted programmatic injection
- REPL `:assert` command: `:assert <relation> <arg1> <arg2> ...` for direct fact injection (numbers auto-detected, else constant)
- Deontic predicates: bilga (obligation), curmi (permission), nitcu (necessity) — standard gismu, work through full pipeline
- Bidirectional material conditional rewrite enables modus ponens/tollens on sentence connectives (ganai...gi)
- Deontic attitudinals: ei (obligation/should), e'e (competence/permission/may) — sentence-level modifiers, transparent wrapper nodes in reasoning
- Lujvo morphological recognition: Logos regex `[a-z']{5}[a-z']*[aeiou]` captures 6+ char brivla; longest-match prevents cmavo prefix theft; PHF dictionary handles arity lookup
- Observative sentences & go'i pro-bridi: gerna accepts sentences without explicit selbri (inserts implicit `go'i`), lasna resolves go'i via `SelbriSnapshot` deep-clone preserving full selbri structure (negation, conversion, tanru, be/bei args, abstractions) across calls
- Metalinguistic `sa` construct-class erasure: proper selma'o classification (28 classes) with backward-walk to matching grammatical class; graceful fallback to single-word erase for unclassified cmavo
- Existential witness extraction: `query-find` WIT method + `find_witnesses` logji function returns all satisfying binding sets for existential variables; `ma` question pro-sumti compiles to existential variable (like da/de/di); REPL `??` prefix for find queries
- Proof trace generation: `check_formula_holds_traced` builds proof tree as it recurses, recording which rule/axiom was applied at each step (15 proof rule variants); `query-entailment-with-proof` / `query-text-with-proof` WIT methods; REPL `?` now always returns the traced query result with indented proof output
- Multi-hop derivation provenance: backward-chaining traces derived facts through universal rule chains (e.g., `gerku(alis) → danlu(alis) → xanlu(alis)`); `UniversalRuleRecord` captures rule templates at compilation time; structural unification matches conclusion templates against queried facts; `Asserted(fact)` leaf nodes distinguish ground truths from `Derived(label, fact)` nodes with recursive children; depth-limited (10) with graceful fallback to opaque `PredicateCheck`
- Gerna error recovery: per-sentence recovery (skip to next `.i` on parse failure, continue parsing remaining sentences); `ParseResult` carries both partial results and errors; exact line:column reporting via pointer arithmetic on token `&str` slices; WIT `parse-error` and `parse-result` types; lasna surfaces parse warnings
- WASM fuel limits: Wasmtime fuel-based execution limits prevent unbounded computation; per-command refuel in REPL; configurable via `NIBLI_FUEL` env var or `:fuel` REPL command; friendly `[Limit]` message on fuel exhaustion
- WASM memory limits: Wasmtime `StoreLimits` caps WASM linear memory growth; configurable via `NIBLI_MEMORY_MB` env var (default 512 MB) or `:memory` REPL command; prevents adversarial fact store growth from crashing host
- Conjunction evaluation: `And(A, B)` checked by recursively verifying both conjuncts hold individually via backward-chaining; no eager materialisation of And terms; event Skolem constants (`_ev*`) tracked in separate `known_event_entities` set — visible for witness search and proof tracing but NOT registered in InDomain
- WIT typed error variants: shared `nibli-error` variant (syntax/semantic/reasoning/backend) replaces `Result<_, String>` across all 14 WIT functions; `syntax-detail` record carries line:column; lasna propagates via `?`; gasnu pattern-matches for structured `[Syntax Error]`/`[Semantic Error]`/`[Reasoning Error]`/`[Backend Error]` REPL output
- WASI capability sandboxing: replaced `inherit_stdio()` with `inherit_stdout().inherit_stderr()` — WASM components get only stdout/stderr (for diagnostic prints), no stdin, no filesystem, no network, no env vars
- Clone-free Jo/Ju connectives: added `Biconditional` and `Xor` variants to `LogicalForm` IR — each operand stored once, expansion to And/Or/Not happens during flattening where operands are u32 indices (zero-cost copy); eliminated 6 deep `.clone()` calls across 3 compilation sites
- Arena-allocated gerna AST: bumpalo `Bump` arena replaces all `Box<T>` with `&'arena T` in AST types; 24 heap allocations per parse batched into contiguous arena chunks; arena created per `parse_text()` call, freed in one shot after flattening; the pattern supports `Bump::reset()` memory reuse for batch corpus processing, though `reset()` is not currently called (fresh arena per parse)
- Non-monotonic reasoning / belief revision: fact registry with per-assertion FactId (u64), FactRecord stores cloned LogicBuffer + label + retracted flag; retraction marks fact withdrawn then rebuilds fact store from surviving base facts (sound because backward-chaining re-derives on demand); `retract-fact` and `list-facts` WIT methods on both `knowledge-base` and `session` resources; REPL `:retract <id>` and `:facts` commands; idempotent retraction; `rebuilding` flag suppresses diagnostic prints during replay
- Temporal reasoning: `Past`/`Present`/`Future` tense wrappers in typed fact store; tense wrappers preserved end-to-end (assertion, query, rule compilation, proof tracing, witness extraction); tense conjunction elimination rules; temporal entity extraction for conjunction introduction; temporal lifting of universal rules (timeless rules automatically fire on tensed premises to derive tensed conclusions via backward-chaining); tense-aware backward-chaining provenance; strict tense discrimination (Past != Future != bare)

- Neo-Davidsonian event semantics: every predication decomposes into event type predicate + Lojban-native role predicates (`klama(e) ∧ klama_x1(e, alis) ∧ klama_x2(e, paris)`); fresh `_ev0`, `_ev1` event variables separate from entity `_v0` variables; tanru share event variable between modifier and head (`sutra gerku` → `∃e. gerku(e) ∧ gerku_x1(e, x) ∧ sutra_x1(e, x)`), resolving the intersective fallacy; `event_decompose()` method on SemanticCompiler produces `∃e. type(e) ∧ role_x1(e, a1) ∧ ...`; all role predicates emitted (including Unspecified/zo'e) for inject_variable compatibility; `inject_variable` and `count_unspecified_predicates` updated to only target `_x1` role predicates; recursive selbri (Converted/Negated/Grouped/Connected/WithArgs) get event decomposition automatically via delegation; abstraction inner forms (`nu`/`du'u`/`ka`/`ni`/`si'o`) event-decomposed naturally through `compile_sentence`

- Description term opacity (`le` vs `lo`): `la` gadri now compiles to `Constant` (like proper names), `le` stays as opaque `Description` rigid designator; `Description` terms tracked via `known_descriptions` in logji engine, enabling conjunction introduction and universal rule participation; `ro le` uses opaque `le_domain_{name}` restrictor (distinct from veridical `gerku` restrictor of `ro lo`); `PA le` similarly uses opaque domain restrictor; all three query functions (entailment check, proof trace, witness extraction) enumerate both `Const` and `Desc` domain members; `QuantifierKind::UniversalLe` and `ExactCountLe` variants preserve gadri distinction through quantifier closure

- Event-decomposed universal rule compilation: condition-side existential variables (from Neo-Davidsonian event decomposition in universal rule antecedents) are compiled as pattern variables in backward-chaining rule templates instead of dependent Skolem functions; `collect_condition_exists` identifies condition-side ∃ variables after `decompose_implication`; `flatten_conjuncts_through_exists` strips Exists wrappers to expose individual predicate atoms for pattern matching; conclusion-side ∃ variables correctly remain as `SkolemFn` for canonical witness creation; xorlo presupposition generates fresh Skolem constants for event variables; backward-chaining provenance enumerates domain member witnesses for unbound event pattern variables in condition templates; enables multi-hop temporal reasoning with event semantics (e.g., `pu gerku(alis) + ∀gerku→danlu + ∀danlu→jmive` derives `pu jmive(alis)`)

- Proof trace memoization: `HashMap<String, u32>` memo table threaded through `trace_predicate_provenance`, `try_backward_chain_traced`, and `check_formula_holds_traced` deduplicates repeated sub-proof derivations; when the same predicate has already been proved, subsequent requests emit a lightweight `ProofRef(fact)` WIT variant instead of re-expanding the full derivation tree; `proof-ref` variant added to WIT `proof-rule`; gasnu displays `(see above): <fact>` for memoized references; memo created fresh per `query_entailment_with_proof_inner` call (no cross-query leakage)
- Proof trace short-circuit AND + step cleanup: `check_formula_holds_traced` AND handler short-circuits on left failure (matching `check_formula_holds` which uses Rust `&&`), preventing exponential blowup when nested existential witness search tries many candidate bindings; ∃ witness loop truncates `steps` vector on failed candidates (checkpoint/rollback) to prevent orphaned step accumulation; reduces worst-case from O(M^D) to O(M×D) where M=domain members, D=∃ nesting depth
- Ground conjunction flattening: `process_assertion()` flattens top-level And trees before asserting — each leaf predicate is asserted individually into the fact store. `collect_ground_leaves()` helper follows And nodes, Skolemized Exists, tense wrappers, and deontic wrappers to extract leaf node IDs with tense context. Ground material conditionals (Or(Not(P), Q) patterns from sentence connectives) are auto-registered as zero-variable UniversalRuleRecord entries for backward-chaining modus ponens.

- REPL `:load <filepath>` command: batch-loads a `.lojban` file into the knowledge base; reads file line by line, skips blank lines and `#` comment lines, asserts each remaining line via `call_assert_text`; per-line refueling prevents fuel exhaustion on large files; reports per-line fact IDs or errors with line numbers; final summary shows asserted/skipped/errors counts; use with `readme.lojban` ontological prelude to bootstrap the KB

**Transparency Triad UI (Steps 1-10 of 10 complete):**
- Step 1: Workspace scaffolding + Dioxus web app (nibli-ui crate, dx serve)
- Step 2: Two-row layout with tabs (Source/Lojban/Back-translation) + query bar + proof panel
- Step 3: GraphQL backend server (nibli-server + nibli-engine, native Rust — no WASM)
- Step 4: Assert/query commands via query bar (unified proof-query only, no separate simple query)
- Step 5: smuni-dictionary crate with PHF glosses + robotic back-translation (10,521 entries)
- Step 6: Ollama integration — English→Lojban translation via server proxy (qwen3-coder:30b default)
- Step 7: Proof trace visualization with human-readable internal representation formatting
- Step 8: Fact management — KB status bar with per-line validation, .lojban file loading, clear button; Ollama temperature fixed to 0.0
- Step 9: UI polish — keyboard shortcuts (Ctrl+L/K/O plus Ctrl+Enter in the Source tab), loading spinners, structured error display, output log improvements (cap/clear/auto-scroll), proof tree connectors, default example syllogism; unified query/proof across entire stack (engine, server, REPL, UI)
- Step 10: Integration tests (16 tests via nibli-engine), Justfile targets (check, test-engine, test-all), README documentation (updated REPL examples, added Transparency Triad UI section)

**UI query semantics:** Every query resets the engine, re-asserts the full Lojban tab text as the KB, then runs the query. The query bar is pure queries only (no assertions). The Lojban tab is the single source of truth.

**Gossip Network Integration (Prompts 7-10):**
- Prompt 7: Training data pipeline — `nibli-validate` binary for batch Lojban validation; `generate_training_data.py` (5 balanced domains, Anthropic API, gerna gate); `training_stats.py` (pass rate analysis, HuggingFace export)
- Prompt 8: Fine-tuning pipeline — `nibli_model.py` (QLoRA on Qwen2.5-7B, train/eval/refine/push commands)
- Prompt 9: LLM gossip agent — `nibli_agent.py` (TCP JSON Lines client for tavla, Claude API / local model, gerna gate with 3 retries, auto-gossip mode, epistemic stances)
- Prompt 10: Network visualization — gossip types in nibli-protocol; nibli-server embeds GossipNode with GraphQL queries (networkSnapshot, agentEnvelopes, envelopeDetail, gossipEvents) and mutations (gossipAssert, gossipRetract, resolveContradiction), plus `/healthz`, `/readyz`, and `/metrics`; nibli-ui Network tab with agent cards (stance distribution bars), envelope list, event feed, contradictions panel, gossip assertion bar with stance selector, and a status badge driven by `/readyz`; auto-refresh every 3 seconds. Retraction is still API-level, not yet a dedicated UI control.

**Server hardening:** nibli-server has request timeouts (default 30s, `NIBLI_SERVER_REQUEST_TIMEOUT_SECS`) and rate limiting (default 50 req/s, `NIBLI_SERVER_RATE_LIMIT_RPS`) on GraphQL endpoints. Health/readiness/metrics endpoints are not rate limited. Rate limit rejections return HTTP 429. Timeouts return HTTP 408. **Bounded + cancellable reasoning:** `query_with_kb` now runs under a wall-clock **cancellation watchdog** — a cooperative `Arc<AtomicBool>` cancel flag lives on logji's `KnowledgeBaseInner` (checked at the central reasoning entries `check_formula_holds`/`_traced`, the backward-chain workhorses, and inside the `TypedMultiCartesian` candidate iterator), exposed via `KnowledgeBase::set_cancel_flag`/`clear_cancel_flag` and `NibliEngine::set_cancel_flag`/`clear_cancel_flag`; a native `tokio` timer on each request raises the flag when `request_timeout_secs` elapses, so the blocking reasoning task aborts (via the existing `Result` error channel — no new `QueryResult` variant) and frees its thread instead of running a pathological query to completion. The flag is **clock-free** (the timer is the native server's, not the engine's), so the WASI sandbox "no clock" guarantee holds and gasnu/lasna (which never install a flag) behave byte-identically. GraphQL also enforces `limit_depth`(32)/`limit_complexity`(10000) and disables introspection unless the playground is enabled; `NIBLI_SERVER_RATE_LIMIT_RPS=0` clamps to 1 with a warning instead of panicking. (Per-client keyed rate limiting was considered and dropped — the global token bucket is accepted by design; a single noisy client can still consume the shared quota.)

**Gossip signature verification:** tavla envelopes are signed with ed25519 (auto-generated ephemeral keypair per `GossipNode`). On ingest, signatures are verified against registered peer keys or TOFU (trust on first use — public key embedded in envelope). `SignaturePolicy::AcceptUnsigned` (default) accepts legacy unsigned envelopes; `SignaturePolicy::RequireSigned` rejects them. Forged envelopes (tampered content) are rejected before trust evaluation.

All crates compile cleanly with rustc 1.94.0. All 35 tavla tests pass.

**Next up:** Deferred items remain outside the closed roadmap: async compute backend follow-up work, richer browser-side transport/UI controls, and book-polish passes tracked in their local notes.
