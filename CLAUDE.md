# Nibli

A Zero-Hallucination Symbolic Reasoning Engine.

Nibli is a deterministic theorem prover compiled to WebAssembly (WASI P2). It compiles Lojban natural language syntax into First-Order Logic and performs inference via demand-driven backward-chaining over an indexed fact store. Every conclusion is a formal derivation from the facts + rules you assert, under a closed-world + closed-domain assumption (compute-backend results are trusted as axioms); nothing is fabricated. A `FALSE` verdict means "not derivable," not "proved ¬P". (README's "What zero-hallucination means here" has the full scoping.)

## Environment

- **OS:** Ubuntu on WSL2 (accessed from Windows via `\\wsl.localhost\Ubuntu\...`)
- **Dev shell:** Nix flake — all tools (rustc, cargo-component, wac, just, wasmtime) come from `flake.nix`
- **Enter dev shell:** `nix develop --extra-experimental-features nix-command --extra-experimental-features flakes`
- **Run commands from Windows side:** `wsl -d Ubuntu -e bash -lc "cd ~/projects/dhilipsiva/nibli && nix ... develop --command bash -c '<CMD>'"`
- **Set `CARGO_INCREMENTAL=0`** if running cargo from Windows side (filesystem lock issues)

## Build & Test

All commands must run inside the Nix dev shell. Use `just` as the primary task runner (see `Justfile`):

| Command | What it does |
|---------|-------------|
| `just run` | Full pipeline: clean WASM -> build lasna component -> launch REPL |
| `just check` | Fast type-check all workspace crates (`cargo check --workspace`) |
| `just test` | Run all unit tests (`cargo test --lib -- --nocapture --test-threads=1`) |
| `just test-engine` | Run nibli-engine integration tests (full pipeline: parse → compile → reason) |
| `just test-gerna` | Run gerna (parser) tests only |
| `just test-klaro` | Run klaro (surface-syntax front-end) tests only — dev loop; `just test` already sweeps them into `ci` |
| `just verify-klaro` | The two Klaro conformance gates (oracle-free, never skip): (1) seam conformance — the CONSTRUCT_INVENTORY sweep (every SURFACE_SYNTAX §3–§9 construct compiles; Lojban twins compile canonically EQUAL), the O3 (`must past P` → `Obligatory(Past(…))`) and O7 (block-every ≡ ro-da prenex) pins, metamorphic pairs, fail-closed negatives; (2) the Klaro↔Lojban translation battery — every seam-compilable line of the shipped corpora + seeded generator programs + conversion pairs must render (`klaro::render`) to Klaro that compiles to the SAME canonicalized LogicBuffer (~400 lines; `KNOWN_UNRENDERABLE` is exact-line value-pinned with a staleness invariant, currently EMPTY — the first sweep's 7 lines were all lifted 2026-07-12: converted-tanru aliases + selbri-connective bridi-level expansion + the coined-word corpus edit). Part of `ci`. |
| `just test-klaro-dict` | Run klaro-dictionary (Klaro alias map) tests only — dev loop; `just test` already sweeps them into `ci` |
| `just migrate-corpora` | Regenerate the four repo-root `.klaro` corpus twins from their `.lojban` sources (klaro's `lojban2klaro` bin: line-by-line, structure-preserving — comments/blanks/`:`-commands byte-copied, `? ` prefixes kept, payloads `gerna::parse_checked` → `klaro::render` — fail-closed). RE-RUN after ANY corpus edit while gerna lives; `verify-klaro-twins` fails on a stale twin |
| `just verify-klaro-twins` | The corpora-twins honesty gate (the shipped-corpora leg of SURFACE_SYNTAX §13 obligation 3; never skips): twin existence both directions, line-structure correspondence at identical line numbers, per-payload-line canonicalized-LogicBuffer equality (144 pairs full mode; dual-mode — the CI fallback build vocab-skips twin lines needing generated aliases), PLUS `determinism_corpus_klaro_native` — the `.klaro` determinism corpus replayed natively in Klaro mode against the same byte-identical `# =>` verdicts as the Lojban leg (curated-core vocab, full-strength in both modes). Part of `ci`. |
| `just verify-klaro-dict` | Alias-map differential gate: the SHIPPED klaro-dictionary alias map must agree with the SHIPPED smuni-dictionary — per-alias arity equality against `smuni_dictionary::get_arity` (the leg that caught the dilcu=3/jmive=1 fallback flap AND the 34 klaro-curated gismu that were absent from smuni's fallback build entirely, both fixed in smuni's `FALLBACK_GISMU_ENTRIES`/`CORE_GISMU_ARITIES`), `GISMU_TO_ALIAS` round-trips (canonical alias unswapped), swap validity (`2..=arity`, involution, peels to a canonical alias), reserved-word exclusion + label integrity re-asserted from the shipped map (`label_index` self-consistency), coverage floors, PLUS a behavioral battery: for EVERY shipped alias, `alias(A, B, …)` via klaro+smuni must compile canonically EQUAL to the direct-gismu (se/te/ve/xe-converted) Lojban via gerna+smuni. Dual-mode, never skips: full local build checks all ~1,341 aliases, CI fallback checks the curated core (FALLBACK MODE banner); a MIXED-MODE build (one dictionary crate stale) fails loud. Part of `ci`. |
| `just test-backend` | Run Python backend tests |
| `just test-gasnu` | Run gasnu host unit tests (trap classification, error/verdict formatting, arithmetic) |
| `just test-fanva` | Run nibli-fanva native tests (agentic loop + history trim in both languages, the language-aware local gates incl. the Klaro render round-trip gate, the two shipped-prompt guard twins, semantic verification turn incl. the Genesis silent-mistranslation fixture, LLM request/response shapes, MCP wire/types, tool loop). Part of `ci`. |
| `just test-ui` | Run nibli-ui's native tests — the shipped-examples guard (`shipped_examples_compile`): every example KB line + preset query compiles through the Klaro front-end. Dual-mode: the CI fallback build vocab-skips "unknown predicate" long-tail KB lines (with a floor); preset QUERIES never skip — curated-core vocabulary by policy, so the dropdown works even in a fallback-built bundle. nibli-ui is bin-only, so `just test` (`--lib`) skips it — this gates it in `ci`. |
| `just test-fanva-wasm` | Run nibli-fanva's wasm-only camxes-gate marshalling tests under node (`wasm-pack test --node`); skips cleanly when wasm-pack is absent. Part of `ci-wasm`. |
| `just test-all` | Run every test suite (unit + integration + Python) |
| `just ci` | Fast native CI gate (fmt-check, clippy, all native test suites incl. `test-gasnu` + `test-ui` + `test-fanva` + `verify-soundness` + `verify-klaro` + `verify-klaro-dict` + `verify-klaro-twins` + `verify-parser` + `verify-dict` + `verify-proofs`). No WASM build. |
| `just verify-soundness` | Differential soundness gate (Track A), **two oracles**: **(1) Vampire** (classical FOL) over the Horn/NAF-free fragment — curated cases (incl. ground `du`-equality, mapped to TPTP native `=` so congruence closure matches nibli's union-find in both directions), a seeded batch of random Horn programs with mixed-in `du` links (`NIBLI_VERIFY_RANDOM_COUNT`, default 200; `nibli-verify --random N` for a manual sweep), the auto-extracted mappable slice of the `gdpr`/`ddi` corpora, and a **Predilex taxonomy leg** (`run_predilex_taxonomy`) — real-vocabulary rule programs (direct edges, 2-hop chains, CWA negative controls) generated from the vendored Predilex hypernym links (`cukta`⇒`rutni`, `bloti`⇒`marce`⇒`rutni`, …; the master's λ-calculus `definition` formulas are higher-order and stay out of fragment); **(2) clingo** (ASP/Datalog) over the stratified-NAF + closed-world fragment Vampire can't cover — a curated NAF corpus (incl. the real `gdpr.lojban:101` deontic-NAF erasure rule: `se bilga` is a plain gismu and the `lo nu` abstraction is modeled as an opaque constant keyed by its `__abs_<hash>`; and `du`×NAF cases — the translator canonicalizes `du`-equivalence classes to representative constants, covering the NAF-through-equality shape) + random stratified-NAF programs (`NIBLI_VERIFY_NAF_RANDOM_COUNT`, default 100; `nibli-verify --asp [--random N]`), where nibli's closed-world verdict must equal clingo's unique stable/perfect model. Tense flavors (`pu`/`ca`/`ba`) are covered by both oracles via the flavorization pre-pass (`nibli-verify/src/tense.rs`: flavor-suffixed predicates; unmarked rules instantiated per occurring flavor; explicit tenses pinned). Exact-count queries (`PA lo X cu Y`) are covered by the ASP oracle as `#count` aggregates (curated + random, `NIBLI_VERIFY_COUNT_RANDOM_COUNT`, default 100) — since the 2026-07-02 entity-level counting decision (GUARANTEES §Aggregation) INCLUDING KBs with rules and `du`: the engine excludes xorlo presupposition witnesses from counting and collapses `du`-equivalence classes, matching the translator, so the random count generator mixes both shapes in. Conservatively skipped: non-ground / negated / numeric `du`; tense×NAF (the engine’s `NegatedExistsGroup` is tenseless — not canonized); tense×abstraction. Plus a **(3) non-stratified-rejection differential** (`strat_diff.rs`) — seeded random rule programs asserted rule-by-rule; every accept/reject decision must match an independent implementation of the proven criterion (written from the Lean statement, never from `rules.rs`; `NIBLI_VERIFY_STRAT_RANDOM_COUNT`, default 300), with a post-rejection fresh-replay equivalence battery (a rejected rule leaves no trace); native-only, never skips. Plus a **(4) retraction metamorphic differential** (`retract_diff.rs`) — seeded random op sequences (ground/∃/rule/`du`/NAF asserts + retractions); after every retraction the incremental engine must equal a fresh replay of the survivors (retract ≡ never-asserted, both the O(1) and full-rebuild paths; `NIBLI_VERIFY_RETRACT_RANDOM_COUNT`, default 200); native-only, never skips. Plus a **(5) compiler-seam** check (`gerna_smuni_seam_conformance`, `nibli-verify/src/seam.rs`) — the front-end analog of the two oracle gates: compiles source Lojban text end-to-end (gerna→smuni) and checks the FOL against hand-verified structure (event decomposition + arg→role, `na`→¬, `.e`/`.a`→∧/∨, `ro lo`→∀-implication vs `lo`→∃-conjunction, `se`-swap INCLUDING inside tanru units — both the smuni swap-drop and gerna's whole-tanru leading-SE scope were fixed 2026-07-12 to match CLL/camxes per-unit scope) plus metamorphic pairs (`E se P F` ≡ `F P E`, canonicalized by positional var-rename) over a seeded batch; needs no solver, so it always runs. Needs `vampire` + `clingo` for oracles (1)+(2) (Nix shell; each side skips if its solver is absent). Part of `ci`. |
| `just verify-parser` | Parse-differential gate (front-end): every sentence gerna accepts (shipped corpora + seeded random batches from all four case generators — incl. `random_giha_case`, the parser-only GIhA + solid-`.ije` fuzzer) must parse under the official Lojban grammar — **ilmentufa camxes** driven via `node` (both provided by the Nix shell: `nodejs` + the pinned `ilmentufa` flake input, exported as `NIBLI_CAMXES_DIR`; skips cleanly if absent). One-directional (gerna implements a fragment, so gerna-rejects carry no signal); acceptance-level only (parse trees are not compared — the seam gate covers text-to-FOL structure). The `KNOWN_DIVERGENCES` allowlist (`nibli-verify/tests/parser_differential.rs`, still-diverging invariant) is currently empty — the five first-run divergences were fixed at the source (`.fenituin.`→`.fenitoin.` corpus rename; readme `je na`→official `jenai`), and bare `na` after a selbri connective is now REJECTED at the grammar level (2026-07-10, surfaced by int19h's feedback probe; the compound `jenai`/`janai`/`jonai`/`junai` forms are the supported spelling). Part of `ci`. |
| `just verify-dict` | Dictionary-arity differential gate: the shipped `smuni-dictionary` arities must COVER independent lower bounds derived from **Predilex** (CC0 thesaurus of sememes-as-predicates, vendored + SHA-pinned in `nibli-verify/vendor/predilex/`; the data rules live in `nibli-verify/src/predilex.rs`). Predilex sememes are systematically coarser than Lojban place structures, so the sound invariant is a LOWER BOUND: an undercount (dictionary arity < bound) flags a truncated place structure (its first run caught the `cusku` 3→4 override pin); overcounts pass as expected coarseness. `KNOWN_UNDERCOUNTS` allowlist (value-pinned, still-undercounting invariant) holds hand-verified lensisku definition-text gaps only. Dual-mode, never skips: full local build (`just fetch-dict`) checks ~198 words; the CI fallback build checks the curated core set (loud FALLBACK MODE banner). Part of `ci`. |
| `just verify-proofs` | Mechanized-proof gate (Track B): check the Lean 4 soundness proofs in `proofs/` (needs `lean`, provided by the Nix shell; skips if absent). Part of `ci`. |
| `just ci-wasm` | WASM behavioral gate: build the lasna component + run the gasnu smokes — payloads in KLARO since THE FLIP (script, trap recovery, persist-replay, statement-split + buffer replay, NAF note, `:debug`, quiet, strict, …) plus the Lojban-pinned ones (the seven go'i smokes, backend-unavailable, non-finite — each under `:lojban`), `smoke-gasnu-script-lojban` (legacy-language pin) and `smoke-gasnu-mixed-mode` (Klaro assert clears the Lojban pro-bridi context). Determinism legs: `smoke-gasnu-determinism` (now the pinned `determinism-corpus.klaro` through the Wasmtime component) and `verify-wasm-node` (the same `.klaro` twin through the wasm32-unknown-unknown build under node/V8, session explicitly in Klaro; skips cleanly if wasm-pack is absent). The primary native leg is the `.klaro` twin (`determinism_corpus_klaro_native`, `verify-klaro-twins`); the Lojban twin leg (`determinism_corpus_lojban_twin`) rides `verify-soundness`. Also runs `test-fanva-wasm` (nibli-fanva's camxes-gate marshalling tests under node). |
| `just ci-all` | Comprehensive pre-push / pre-release gate: `ci` + `ci-wasm` |
| `just build-wasm` | Build single lasna WASM component |
| `just build-gasnu` | Build native Wasmtime host gasnu (runner) |
| `just backend` | Start the Python reference compute backend (port 5555) |
| `just run-with-backend` | Build + run with `NIBLI_COMPUTE_ADDR=127.0.0.1:5555` |
| `just ui` | Launch the standalone Transparency Triad web UI (Dioxus, port 8080) — engine runs fully in-browser |
| `just build-ui` | Build the nibli-ui web bundle for release (`dx build --release` → `target/dx/nibli-ui/release/web/public/`) — a local preview / pre-merge sanity build; the PRODUCTION build runs in the external dhilipsiva.dev site repo (see `DEPLOY.md`) |
| `just fetch-dict` | Download the lensisku English dictionary to `dictionary-en.json` (public dump, no login) — see Dictionary Data below |
| `just import FILE [ARGS]` | Import an RDF Turtle / OWL file into a fresh KB via the `nibli-import` CLI (`--raw` skips OWL class handling, `--export` prints the round-trip view, `--lang klaro\|lojban` selects the `--query` front-end, `--query "<text>"` runs entailment checks — reaches only names the selected front-end can SPELL: Lojban-lexable local names in Lojban mode, dictionary/alias-resolvable names fail-closed in Klaro mode; English/camelCase RDF predicates import as facts but await the v2 schema registry, SURFACE_SYNTAX §14.1). NOTE: just's variadic args lose shell quoting — run `./target/debug/nibli-import` directly for multi-word queries |
| `just bench-book` | Timing pins for the book's quoted figures (Ch 13 latency numbers, Ch 20 sequence): release-profile native bench over `gdpr.lojban` — corpus load / lawful-basis query / full Ch-20 sequence (load + query + consent retraction + 2 re-queries), min/median/max over `NIBLI_BENCH_RUNS` runs (default 10), every verdict asserted every run. The source for any latency figure the book quotes — never hand-write timings |
| `just verify-book-refs` | Book-reference conformance gate (detection only): every WIT name, REPL command (`gasnu` + `nibli` debug REPL), Rust struct field, and notation form the book quotes must match the repo (`book/tools/verify_book_refs.py`, per-claim report). EXPECTED red until the book reconciliation pass; wiring into `ci` is a book-repo decision after that. Skips when `book/` is absent |
| `just count-tests` | Derive the current test-suite counts (unit + native integration/bin targets). The source for any doc that needs a figure — never hand-write test counts (pre-commit checklist) |
| `just clean` | `cargo clean` |
| `just fuzz-parse [SECS]` | Fuzz gerna parser (libFuzzer via the Nix shell's pinned nightly, `NIBLI_NIGHTLY_BIN` — no rustup needed). Pass seconds to limit run time. |
| `just fuzz-assert [SECS]` | Fuzz nibli-engine assert_text (full pipeline) |
| `just fuzz-query [SECS]` | Fuzz nibli-engine assert + query (stateful KB; split-half input: first half asserted, second half queried) |
| `just fuzz-klaro [SECS]` | Fuzz the Klaro front-end (parse → resolve → emit): any accepted input must compile through smuni WITHOUT a "corrupt AST buffer" rejection — a structurally invalid emitted buffer is a klaro bug, surfaced as an oracle panic (plus the usual crash/leak detection) |
| `just fuzz-seed` | Seed `fuzz/corpus/` for all four targets — the shipped `.lojban` corpus files for the three Lojban-pipeline targets, `klaro/tests/acceptance.klaro` for `fuzz_klaro` |
| `just fuzz-ci [SECS]` | Unattended fuzz gate: `fuzz-seed` + all four targets × SECS (default 120) each; non-zero on crash/OOM/leak (LeakSanitizer is ON — the arena AST is leak-free by invariant, see `gerna/src/ast.rs`). Runs as a parallel `fuzz` job in the GitHub workflow. |
| `just mutants [JOBS]` | Mutation-testing gate over the soundness paths (scope + per-mutant test set in `.cargo/mutants.toml`: logji/smuni/nibli-engine suites per mutant). Diffs survivors against `mutants-baseline.txt` (line:col-stripped): fails on any NEW survivor, prompts a shrink for killed ones. On-demand (~17 min full sweep), not in the per-push gate. Baseline + stats in GUARANTEES.md. |

**Important:**
- Always use `cargo test --lib` (NOT `cargo test`) — cdylib linker chokes on WIT export symbols containing `@`
  - Exception: `gasnu` is a normal bin (no cdylib/WIT export), so `cargo test -p gasnu` (the `test-gasnu` recipe, gated by `ci`) links fine; the `@` issue only affects `lasna`/the component crates.
- `ci` is the fast native gate (no WASM build). The WASM component + gasnu fuel/memory/trap-recovery behavior is gated by `ci-wasm` (the six smokes); run `ci-all` (= `ci` + `ci-wasm`) as the comprehensive pre-push gate.
- **Regenerate WIT bindings:** `cargo component build` (bindings appear in each crate's `src/bindings.rs`)
  - Note: full build fails on `io-extras` crate (`#![feature]` on stable). Bindings still generate successfully before the failure.
- **REPL uses reedline** — does not work with piped stdin
- Logji (reasoning) tests require `--test-threads=1` (shared global state via RefCell). The Justfile handles this.

## Dictionary Data

`smuni-dictionary` (arity + English gloss + place-frame for every gismu/lujvo/cmavo) is built
at compile time by `smuni-dictionary/build.rs`, which parses **`dictionary-en.json`** at the
repo root — the **lensisku** English bulk export (`lojban/lensisku`,
`DictionaryEntry`: a JSON array, one canonical entry per word, with `word`, `word_type`,
`definition` (place structure as `$x_{N}$` markers), and `gloss_keywords`). This replaced the
legacy reCAPTCHA-gated `jbovlaste-en.xml`, which is now fully retired — nothing reads it
(the book's `verify_book.py` VOCAB check reads this JSON too, skipping cleanly when absent).

- The file is **gitignored**, so it is NOT in the repo. Get it with **`just fetch-dict`** —
  lensisku's nightly cached dumps are public (no login;
  `GET https://lensisku.lojban.org/api/export/cached/{lang}/{format}` — GET only, HEAD 401s) —
  or download `dictionary-en.json` manually and drop it at the repo root.
- **Without the file** the build falls back to the in-tree `FALLBACK_GISMU_ENTRIES` +
  gloss/cmavo override tables (~175 curated entries; every gismu the klaro-dictionary
  curated alias table references is included with its full-derivation arity — the
  `verify-klaro-dict` gate enforces this) — this is exactly what CI uses, so CI
  needs no network. With the file you get the full dictionary — the export carries
  ~17.5k entries across all word types; the build consumes the gismu/lujvo/cmavo types
  (1,338 gismu, 8,322 lujvo, 1,261 cmavo in the current export).
- Arity/gloss/template for all curated/corpus/test words come from the in-tree tables
  *before* the data file is consulted, so those are **identical with or without** the file;
  only the non-curated long tail differs.

## Compute Backend

The gasnu (runner) acts as a TCP client to an external compute backend server via JSON Lines protocol.

- **Env var:** `NIBLI_COMPUTE_ADDR=host:port` — configures the backend address at startup
- **REPL command:** `:backend [host:port]` — show or change backend address at runtime
- **Protocol:** One JSON object per line. Request: `{"relation":"tenfa","args":[{"type":"number","value":8.0},...]}`. Response: `{"result":true}` or `{"error":"..."}`.
- **Fallback:** Built-in arithmetic (pilji/sumji/dilcu) always handled locally. Unknown predicates forward to external backend. If no backend configured, returns error.
  - **Tolerant equality (disclosed):** pilji/sumji/dilcu compare the result with `isclose` (rel_tol `1e-9`, abs_tol `0`) — a deliberate float approximation so `0.3 = 0.1 + 0.2` is TRUE — in the single shared evaluator `nibli-types/src/arithmetic.rs` (logji guest + gasnu host + `nibli_backend.py` all mirror it). The equality predicate `dunli` is EXACT `==` (`logji/src/compute.rs`). README's "Compute Backend" discloses this to users.
- **Lazy connection:** TCP connects on first external dispatch, auto-reconnects on failure.
- **Trusted oracle (disclosed):** a backend `true` reply is AUTO-ASSERTED as a ground fact mid-query (`assert_typed_fact`), which downstream rules then chain on — the backend is part of the trusted computing base, an axiom source, NOT a derivation. See the TRUST BOUNDARY note at `logji/src/lib.rs` and `logji/src/compute.rs`. README's "What zero-hallucination means here" + "Compute Backend" disclose this to users.
- **Reference server:** `python/nibli_backend.py` — handles pilji, sumji, dilcu, tenfa (exponent), dugri (log). Extend via `HANDLERS` dict.

## Architecture

Core component crates + runtime surfaces:

| Crate | Lojban meaning | Role | Key files |
|-------|---------------|------|-----------|
| `gerna` | grammar | Lojban text -> AST -> flat WIT buffer; shared `go'i` pro-bridi resolver | `grammar.rs`, `ast.rs`, `lib.rs` (flattener), `lexer.rs`, `goi.rs` (`resolve_go_i`, shared by lasna + nibli-engine) |
| `smuni` | meaning | Flat AST buffer -> FOL logic IR -> flat WIT logic buffer | `semantic.rs`, `ir.rs`, `lib.rs` (flattener) |
| `logji` | logic | FOL logic buffer -> backward-chaining assert/query | `lib.rs` (single file, all logic) |
| `lasna` | fasten/connect | Glue: chains gerna/klaro -> smuni -> logji. LANGUAGE DISPATCH mirrors nibli-engine's: `Cell<Language>` on the Session (default KLARO since THE FLIP), seeded from the `NIBLI_LANG` WASI env in `Session::new` (post-trap rebuilds restore the mode), switched via the WIT `set-language`; the Klaro arm of `compile_pipeline` skips `go'i` and clears the prior-bridi snapshot; interactive Klaro text inputs additionally emit the §12 lint notes as verbose-gated `[Note: …]` guest-stdout echoes (the `[Skolem]` precedent — `NIBLI_QUIET=1` suppresses; the replay path never lints) | `lib.rs` |
| `gasnu` | agent/doer | Native Wasmtime host, REPL, external compute backend TCP client. Language-aware: `lang` on the Repl (NIBLI_LANG at startup; ALWAYS env-forwarded at (re)instantiation), `:lang [klaro\|lojban]`/`:klaro`/`:lojban` commands (the `:strict` pattern via the WIT `set-language`), `:load` extension rule (file-scoped), `--script` extension rule (session-scoped — a `.lojban` script keeps legacy runs working) | `main.rs` |
| `nibli-engine` | — | Native in-process embedding of the pipeline (used by tests + the store layer; no Wasmtime). Stateful prior-bridi snapshot resolves `go'i` identically to lasna (shared `gerna::goi`). LANGUAGE DISPATCH (`set_language`/`language`, `nibli_types::lang::Language`, default KLARO since THE FLIP 2026-07-12): `compile_text` is the sole text→AST seam — the Klaro arm (`klaro::parse_checked`) skips `go'i` resolution and CLEARS the prior-bridi snapshot (a later Lojban `go'i` fails closed "no antecedent"); `reset()` keeps the language (configuration, like verbose/strict); buffer replay is language-free | `lib.rs` |
| `nibli-ui` | — | Standalone Dioxus web UI (browser, port 8080) — klaro/gerna/smuni/logji compiled in, reasons fully in-browser; KLARO-FIRST since 2026-07-12 (`kb_lang` App signal, default Klaro; a legacy-Lojban toggle in the settings modal writes it directly — a KB mode, not an LLM setting, so it needs no saved key; preloaded examples are ALWAYS Klaro-sourced). Optional client-side BYO-key LLM **Formalize** (Source→KB; renamed from Translate — "compile" stays reserved for the deterministic step): the agentic self-correcting loop (`nibli-fanva`) → per-language gates (klaro+smuni+round-trip / gerna+smuni+camxes, per-language `gate_order` chips) → fresh-context semantic verification → feed errors back → retry, with a self-correction trace. jbotci (McpClient + the tersmu deep-meaning view + the settings section) is LOJBAN-MODE-ONLY; camxes `<script>` assets stay for legacy mode. The LLM client is single-sourced on `nibli_fanva::llm`; nibli-ui holds a thin `Settings { llm, proxy_url, jbotci_enabled, max_attempts }` wrapper. Klaro lint notes (§12 L1–L9) ride `nibli_protocol::LineResult::notes` (fresh `Linter` per query run — the stateless-KB model) and render as `[Note: …]` rows in the KB status bar (which now also expands for notes, not just errors). First native test: the `shipped_examples_compile` guard (`just test-ui`) | `main.rs`, `examples.rs` |
| `nibli-fanva` | translate | Agentic English→KB formalizer engine consumed by nibli-ui — every entry point takes `nibli_types::lang::Language` (Klaro-first; legacy Lojban behind the same seam): a multi-turn LLM client (5 providers) with a `Chat` seam + wasm `HttpChat` transport; the language-aware local gates (`gates.rs`) — KLARO: `klaro::parse_checked` (gate "klaro") + smuni + the **render round-trip gate** (`GateError::RoundTrip`: the candidate's canonical `klaro::render` re-spelling must re-compile to the SAME `LogicBuffer` — klaro's fixpoint contract as a per-candidate drift-catcher; native + wasm) / LOJBAN: `gerna::parse_checked` + smuni + the "official" **camxes** gate (vendored ilmentufa `camxes.js` via a `window.camxes_validate` shim; wasm-only, Lojban-only); per-language prompts (`KLARO_SYSTEM_PROMPT` + `LOJBAN_SYSTEM_PROMPT`, each pinned by a shipped-examples guard twin — the Klaro few-shots are curated-core so the guard holds in fallback builds); an MCP client (`mcp/`) for jbotci tool-use — LOJBAN-ONLY (callers pass an empty proxy in Klaro mode; `fanva-proxy/` is legacy-only) — the `translate_agentic` loop (lang param, line-by-line KB validation, attempt cap, oscillation guard, `degraded` flag), and the **semantic verification turn** (`verify.rs`, int19h feedback): after the gates pass, a FRESH-context judge reads the overlay-free `nibli_render` back-translation (IR-level, language-free) of each KB line; MISMATCH retries as `GateError::Verification` — best-effort advisory. Pure logic is native-tested (incl. the Genesis fixture); the browser transport + camxes gate are wasm-only | `agent.rs`, `gates.rs`, `llm/`, `mcp/`, `tools.rs` |
| `nibli-wasm` | — | wasm-bindgen wrapper exposing the in-browser pipeline (powers the live demo). LANGUAGE: `set_language("klaro"\|"lojban")` export; the session default is PINNED LOJBAN (not `Language::default()`) — the deployed playground rebuilds on every push (redeploy-site.yml), so the default flips only in the dhilipsiva.dev site-repo migration window; `back_translate_ir` likewise stays Lojban until then (`compile_for_render` is language-parameterized, a one-arg flip). The V8 determinism leg runs the `.klaro` twin with an explicit `set_language("klaro")` | `lib.rs` |
| `nibli` | — | Native debug REPL and `nibli-validate` tooling. Language-aware surfaces (2026-07-12): REPL `:klaro`/`:lojban`/`:lang` + `NIBLI_LANG` at startup + `:load` picks the front-end by extension (`.lojban`/`.klaro`, file-scoped — restored after); `nibli-validate` and `nibli-import` take `--lang klaro\|lojban` + `NIBLI_LANG` (flag wins; flag errors fatal, env errors warn-and-default). Since THE FLIP the no-flag default is KLARO — Lojban callers pin it: `verify-book` sets `NIBLI_LANG=lojban`, the python training tools pass `--lang lojban` | `main.rs`, `src/bin/validate.rs` |
| `nibli-types` | — | Shared flat WIT-compatible types used by every stage: `AstBuffer`, `LogicBuffer`/`LogicNode`, `NibliError`, the front-end `Language` selector (`lang.rs`: Klaro\|Lojban + FromStr/Display, default KLARO since THE FLIP 2026-07-12), and the single shared arithmetic evaluator (tolerant equality). The `LogicBuffer` IR is publicly specified in repo-root `LOGIC_IR.md` — keep that spec in sync when the IR or its emitted-shape invariants change | `ast.rs`, `logic.rs`, `error.rs`, `lang.rs`, `arithmetic.rs` |
| `smuni-dictionary` | — | Compile-time PHF dictionary (arity + gloss + place-frame per gismu/lujvo/cmavo), built by `build.rs` from `dictionary-en.json` with an in-tree curated fallback — see Dictionary Data below | `build.rs`, `lib.rs` |
| `klaro` | clear (working name) | The Klaro surface-syntax front-end — **SHIPPED as the DEFAULT language on every runtime surface since THE FLIP (2026-07-12)**; the v0.1 compat profile of `SURFACE_SYNTAX.md` is fully implemented (`KLARO_TODO.md` tracks the residual program: lint catalog, the site-window nibli-wasm flip, gerna retirement at v2). Parses the predicate-call language (`goes(me, to: some market).`) into the same `AstBuffer` gerna produces, reusing smuni/logji unchanged. Parser tech = **pest** (2026-07-12 decision): `src/klaro.pest` is the EXECUTABLE grammar (normative SURFACE_SYNTAX §15 — grammar↔parser drift impossible by construction; keyword rules self-guarded against the `everyday`→`every day` boundary class and pinned both-directions against klaro-dictionary's reserved list by a conformance test). The FULL v0.1 grammar surface (terms, determiners, positional+named args, operator chain, tense/deontic prefixes, binary `=`, tanru/`[ ]`/`+`, `.label` selectors — O8 adjacency rule, linked args, `where`/`also` rel clauses with mandatory-`it`, abstractions, `via` tags, block determiners, `all` prenex) + the resolve pass (`src/resolve.rs`: fail-closed name resolution alias→identity-gismu→COMPILE ERROR, place checks, the 3-variable lowering cap, `it`/`slot` position rules); the walker owns the §6/§7 errata as targeted positioned errors. `src/emit.rs` lowers the tree to the `AstBuffer` (aliases→gismu with `Converted` swaps, `$vars`→da/de/di, operators at sentence level, every/some blocks via `Prenex+GanaiGi`/`GeGi`; exactly/the blocks fail closed for now); **public `klaro::parse_checked` mirrors `gerna::parse_checked`** (no go'i step) — in-crate twin tests pin Klaro statements compiling to LogicBuffers EQUAL to their Lojban twins' through the same smuni. `src/render.rs` is the inverse (AstBuffer→Klaro; PARITY LAYER 1: zero wildcard arms + `__ast_parity_guard`, so a new AST variant breaks the build; §10 constructs fail closed BY NAME; gerna-origin buffers render via the §11 collapses — `.inaja`→`->`, forethought→operators; SELBRI CONNECTIVES on a main relation render via bridi-level expansion (block-every for a universal subject / operator claim for a constant — canonical equality gate-pinned), conversions inside tanru units via curated converted aliases; sumti connectives + connected-selbri-outside-main + `Converted`-over-non-root still fail closed by name); `tests/acceptance.klaro` (30 stmts, honest-generic §16) is pinned by render∘parse fixpoint tests; `src/bin/lojban2klaro.rs` is the corpus migration bin behind `just migrate-corpora`. `src/lint.rs` is the §12 LINT CATALOG (L1–L9, shipped 2026-07-12): a data-returning pass (`klaro::lint::Linter`, stateful per session for L1 the-trap introductions / L4 first-use alias echoes / the L7 norm-mixing latch; `reset()` rides KB resets) — non-blocking `[Note: …]` compile notes; `parse_checked` stays note-free, surfaces opt in (nibli REPL prints, lasna guest-echoes verbose-gated, nibli-ui carries `LineResult::notes`) | `src/klaro.pest`, `src/parser.rs`, `src/resolve.rs`, `src/emit.rs`, `src/render.rs`, `src/lint.rs`, `src/ast.rs` |
| `klaro-dictionary` | — | Compile-time alias map for the Klaro front-end (shipped with THE FLIP; spec `SURFACE_SYNTAX.md`): English alias → gismu + optional x1↔xN swap + per-place labels. FULL mode (local, `dictionary-en.json` present) generates all 1,338 gismu — alias = pinned spelling else first gloss keyword base-form; arity from `smuni-dictionary` as a BUILD dependency (agreement by construction); label tier chain curated → lensisku `place_keywords` → prose heuristic (`src/label.rs`) → positional. FALLBACK mode (CI) = the curated Rust const tables only (~96 words, corpus/spec vocabulary, 3rd-person spellings — the pin tier, byte-identical across modes). Fail-closed build validation (unpinned collisions/keyword-hits/dictionary-shadows fail the build with the offender list); `src/reserved.rs` single-sources the Klaro keyword list for the future klaro lexer. Deliberately separate from `smuni-dictionary` (reverse map + labels stay out of the web bundle) | `build.rs`, `src/curated.rs`, `src/label.rs`, `src/reserved.rs`, `src/lib.rs` |
| `nibli-protocol` | — | Shared wire-format proof-trace types: nibli-engine (native) serializes, nibli-ui/nibli-wasm (browser) deserialize | `lib.rs` |
| `nibli-store` | — | Persistent redb stores: `NibliStore` (durable fact registry — provenance, retraction tombstones, `:export`) and `RedbFactStore` (the KB's typed write-through mirror, schema-versioned, fail-closed decode) | `lib.rs`, `typed_store.rs` |
| `nibli-render` | — | Shared English rendering of proofs/verdicts (the collapsed `[Why]` narrative; `DomainGloss` overlays are curated-UI-example-only — overlay→dictionary→generic fallback, never CLI/book) | `lib.rs`, `corpus_overlay.rs` |
| `nibli-import` | — | RDF Turtle / OWL import + fact export, wired as the `nibli-import` CLI (`just import <file.ttl>`; `--raw/--export/--query` flags — Lojban queries reach only Lojban-lexable relation names) | `lib.rs`, `rdf.rs`, `owl.rs` |
| `nibli-verify` | — | Differential SOUNDNESS gate (Track A), two oracles: exports the smuni FOL IR (`LogicBuffer`) and checks nibli's verdict against (1) **Vampire** over the Horn/NAF-free fragment (TPTP), and (2) **clingo** over the stratified-NAF + closed-world fragment (ASP — the translator regroups the event decomposition back to function-free surface Datalog). Plus (3) a **gerna→smuni compiler-seam** check (`seam.rs`) — compiles source text end-to-end and checks the FOL against hand-verified structure + metamorphic invariants (no solver). Plus (4) a **gerna↔camxes parse-differential** (`parser_diff.rs`, `just verify-parser`) — gerna-accepted sentences must parse under the official grammar (ilmentufa via node). Plus (5) the **Klaro conformance gates** (`klaro_battery.rs` + `tests/klaro_gate.rs`, `just verify-klaro`) — the CONSTRUCT_INVENTORY seam sweep with Lojban twins + the Klaro↔Lojban translation battery (render→reparse→equal canonicalized LogicBuffers over the corpora + generators). Plus (6) the **alias-map differential** (`tests/alias_differential.rs`, `just verify-klaro-dict`) — the SHIPPED klaro-dictionary map vs the SHIPPED smuni-dictionary (arity equality, round-trips, swap/label integrity, per-alias behavioral twins; dual-mode, mixed-mode-build detection). Plus (7) the **corpora-twins honesty gate** (`tests/klaro_twins.rs`, `just verify-klaro-twins`) — the committed `.klaro` twins vs their `.lojban` sources (existence both directions, line-structure correspondence, per-payload-line canonicalized-buffer equality) + the Klaro determinism native leg. Not a runtime surface — a CI gate | `lib.rs`, `tptp.rs`, `asp.rs`, `filter.rs`, `oracle.rs`, `oracle_asp.rs`, `seam.rs`, `klaro_battery.rs`, `parser_diff.rs`, `strat_diff.rs`, `retract_diff.rs`, `tense.rs`, `corpus.rs`, `corpus_naf.rs` |
| `python/` | — | Reference compute backend server (TCP + JSON Lines) | `nibli_backend.py` |

- **WIT interfaces:** `wit/world.wit` defines only the SHIPPING component's boundary: `logic-types` (FOL IR), `error-types`, `compute-backend` (host import), `lasna` (session export — incl. `enum language { klaro, lojban }` + `set-language` on the session resource, the set-strict pattern). `cargo component build -p lasna` regenerates `lasna/src/bindings.rs` (the ONLY crate with WIT bindings; gerna/smuni/logji/klaro are plain Rust libs using `nibli-types` directly).
- **WIT worlds:** `lasna-pipeline` is the SOLE world — a single WASM component importing `compute-backend`, exporting `lasna`, with gerna/smuni/logji linked as internal Rust crate deps. (The legacy per-stage `gerna-component`/`smuni-component`/`logji-component` worlds + `gerna`/`smuni`/`logji`/`ast-types` interfaces were removed — they were never built and misled contributors into thinking a per-component architecture existed.)
- **Rust structs:** `LasnaPipeline` (the WASM component) is the only WIT-binding struct.
- **Boundary data:** Flat index-based arrays (`LogicBuffer` for `:debug`/proof output, `LogicalTerm` args) with `u32` indices cross the SINGLE host↔lasna WASM boundary — no heap pointers. The internal gerna→smuni→logji stages are Rust function calls (no WASM boundary), using `nibli-types` flat buffers directly.
- **Compute dispatch:** logji uses injectable function pointers (`logji::KnowledgeBase::set_compute_dispatch`) instead of cfg-gated WIT imports. Lasna registers host-bridge functions at Session creation; native (nibli-engine) and in-browser (nibli-ui/nibli-wasm) modes leave dispatch unregistered — built-in arithmetic (pilji/sumji/dilcu) still resolves locally, external predicates (tenfa/dugri) return an error.

## Canonical Runtime Surfaces

Use these assumptions when selecting entrypoints:

- `gasnu` is the canonical local/operator runtime for the theorem prover. It is the main single-node REPL and the default way to exercise the WASM-hosted pipeline. The session language defaults to KLARO (THE FLIP); `NIBLI_LANG=lojban`, `:lojban`, or a `.lojban` `--script`/`:load` extension selects Lojban (the book capture harness pins `NIBLI_LANG=lojban`).
  - **Strict mode:** `NIBLI_STRICT=1` (or `:strict on|off` at runtime) makes arity mismatches and integrity-constraint violations REJECT the offending fact and fail the assertion atomically, instead of the default permissive warn-and-insert (GUARANTEES §Predicate Validation / §Integrity Constraints). Plumbing pinned by the `smoke-gasnu-strict` gate (in `ci-wasm`); rejection behavior pinned at the logji level.
  - **Quiet mode:** `NIBLI_QUIET=1` suppresses the per-assertion bookkeeping echoes — `[Fact #N] …` (host) plus `[Skolem]`/`[Rule]`/`[Constraint]` (guest; gasnu forwards the flag into the component's WASI env, since `lasna::Session::new` reads it to decide `kb.set_verbose`). The verdict, `[Why]`, proof trace, `[Find]`, `[Note: …]`, `[Retract]`, and `:facts` output are unaffected. Opt-in — a live `just run` REPL stays verbose. The book's capture harness (`book/tools/capture_book.py`) sets it by default for clean transcripts; the `smoke-gasnu-quiet` gate (in `ci-wasm`) pins both directions.
- `nibli-ui` is the canonical browser frontend — a standalone Dioxus app with the engine (klaro/gerna→smuni→logji) compiled into the WASM bundle, KLARO-FIRST since 2026-07-12 (default input language Klaro; a legacy-Lojban toggle in the settings modal — it writes the `kb_lang` signal directly, so it needs no LLM key; the KB tab's label follows the mode). It reasons fully in-browser; there is no server. The one optional network call is the Source→KB **Formalize** (renamed from Translate 2026-07-12 — the LLM step is interpretive formalization behind gates; NEVER label it "Compile") — the agentic self-correcting loop in `nibli-fanva` (LLM client single-sourced on `nibli_fanva::llm`): a bring-your-own-key request sent directly from the browser to a user-chosen LLM (Anthropic/OpenAI/OpenRouter/Gemini/Custom), with the key held in tab memory only. **Hosting:** the site is rebuilt by the external `dhilipsiva.dev` repo, pinged by `.github/workflows/redeploy-site.yml` on push to `main` — its `build_nibli.sh` must fetch `dictionary-en.json` before building so the deployed bundle ships the FULL alias map (fallback would break the Klaro examples' long-tail vocabulary; see DEPLOY.md); `just build-ui` produces a local release bundle. See `DEPLOY.md` (and `fanva-proxy/DEPLOY.md` for the legacy-only jbotci proxy).
  - **Query model (state, don't ask):** a query is an entailment check of a *proposition* — you state `eats(Adam).` ("Adam eats"; legacy mode: `la .adam. cu citka`) and the engine returns `TRUE`/`FALSE`/`UNKNOWN`. There is no interrogative form. The affix shown in the UI query box — "?" in Klaro mode, `xu` in legacy Lojban mode — is a decorative reading cue: not part of `query_text`, never sent to the engine. Keep UI/docs/book copy phrased as "state a claim," never "ask a question."
  - **Example dropdown:** the header offers preloaded book KBs (`nibli-ui/src/examples.rs` — Syllogism/GDPR/Drug-interactions; GDPR+drug corpora `include_str!`-ed from the repo-root `*.klaro` twins, whose per-line equality with the tested `.lojban` sources `verify-klaro-twins` pins; every KB line + preset query additionally pinned by `just test-ui`). Examples are ALWAYS compiled with the Klaro front-end, whatever the toggle says. Selecting one is read-only (Formalize disabled) and turns the query box into a preset-query dropdown that auto-runs; default **Custom** (`example == None`) is the editable mode. The `example` signal lives in `App` and is rendered *conditionally* — Custom buffers are never overwritten. Keep the `name`/`label` strings byte-stable (book Ch 19 quotes them; no gate catches a desync).
- `nibli-wasm` is the wasm-bindgen wrapper exposing the same in-browser pipeline to JS (powers the live demo at dhilipsiva.dev/nibli).
- `nibli-engine` is an internal native embedding library, not a user-facing runtime surface.
- `nibli` is developer tooling: a native direct-crate REPL and the `nibli-validate` binary used for validation/data-generation workflows. It is not the canonical production runtime.

## Code Conventions

- Gerna (parser) tests use `as_bridi(&r.sentences[0])` helper to unwrap `Sentence::Simple`
- Grammar tests use `parse_ok()` / `parse_err()` + token constructors `cmavo()`, `gismu()`
- Smuni (semantics) tests use `compile_one(selbris, sumtis, bridi)` helper returning `(LogicalForm, SemanticCompiler)`
- `resolve(&compiler, &spur)` helper to get string from interner in tests
- Connective enums (Je/Ja/Jo/Ju) are shared between selbri and sumti connectives
- BAI tags map to underlying gismu: ri'a->rinka, ni'i->nibli, mu'i->mukti, ki'u->krinu, pi'o->pilno, ba'i->basti
- **Test discipline — flat vs surface (logji):** logji's flat `make_*` test helpers hand-build bare `LogicBuffer`s and skip smuni's event decomposition + `transform_compute_nodes`, so they match the shipped pipeline on *verdicts* but NOT on shape-dependent behavior (`cwa_false`/`naf_dependent` flags, the `ComputeCheck` step, witness/Skolem shape). For anything shape-dependent, build the buffer the real way via `compile_surface("<lojban>")` (a logji test helper = `gerna::parse_checked` → `smuni::compile_from_gerna_ast` → `transform_compute_nodes`), or use the `make_decomposed_*` helpers, or write a `nibli-engine` integration test — never assert those on a bare flat buffer. `logji/src/tests.rs`'s `mod flat_vs_surface` is a metamorphic guard that keeps every behavior class' flat verdict == the surface verdict; keep it green. (See the header comment above the flat helpers in `logji/src/tests.rs`.)

## Codebase Exclusions

When analyzing or searching the codebase:
- **Exclude `docs/` folder** — generated/reference documentation, not source
- **Exclude `**/bindings.rs`** — auto-generated by `cargo component build`, not hand-written
- **`proofs/`** is Lean 4, not Rust — the mechanized soundness proofs (Track B; `proofs/README.md`). Each `.lean` mirrors a Rust component and is kept in lock-step with an exhaustive Rust conformance test. Checked by `just verify-proofs`.

## Known Issues

- `cargo component build` fails on `io-extras` crate — pre-existing, unrelated to our changes. Bindings generate before the failure.
- **rustc ICE in `check_mod_deathness`** — `wasmtime::component::bindgen!` macro triggers compiler panic in library crates (not binary crates like gasnu). Workaround: `#![allow(dead_code)]` at crate root in nibli-engine. Fixed in rustc 1.94.0 (updated via flake).
## Roadmap

The **soundness-by-proof frontier is complete** (P1–P4 gaps cleared; P5 done): Track A ships two
differential gates — **Vampire** (classical FOL, Horn/NAF-free) and **clingo** (ASP, stratified-NAF
+ closed-world, incl. the GDPR deontic-NAF erasure rule) — and Track B ships **six mechanized Lean 4
proofs** of the soundness-critical core (combiner, stratification criterion, SCC decomposition,
unifier, rule firing, and the capstone *trace ⇒ perfect-model* theorem), each bridged to the engine
by a conformance test (`proofs/README.md`; `just verify-soundness` + `just verify-proofs`). The
**Klaro program's runtime migration is complete** (2026-07-12): Klaro is the default front-end on
every surface, docs included; `KLARO_TODO.md` holds the residuals (lint catalog L1–L9, the
site-window nibli-wasm default flip, gerna retirement at clean-core v2). The remaining ceiling is
**adoption**, not engine code — chiefly a reproducible non-expert authoring study (round-trip
fidelity + silent-mistranslation rate), now measurable in Klaro rather than Lojban, which belongs
to the book/UX track.

## Pre-commit Checklist

Before every commit, always:
1. Update `CLAUDE.md` — if required
2. Update `README.md` — if Lojban coverage or reasoning capabilities changed
3. **Never hand-write test counts (or other derivable figures) into docs.** If a figure is needed, derive it at writing time with `just count-tests` — stale hard-coded counts in GUARANTEES.md were an audit finding; prefer floor phrases ("a four-figure suite") that survive growth
4. Then commit all code + doc changes together
