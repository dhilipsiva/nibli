## objections

- target: Gates 4 and 6 vs S0.3 implementation boundary | objection: Gate 4 requires the yeast scenario to 'run end-to-end' and 'emit a complete replayable receipt', and Gate 6 requires 'pinned re-execution reproduces exact results' — but S0.3 forbids all implementation ('No crates, no partial compiler, no plugin runtime'), spikes are throwaway, and the only permitted durable asset is the S15 corpus, which is 'specification, not implementation'. Nothing the prompt permits building can execute the slice or be re-executed. Gates 4 and 6 are unpassable by construction within the stated boundary, and the staging table assigns them to Stage E, a research stage. Either the gates gate a future implementation phase (then say so and remove them from the research staging), or the boundary must permit a reference implementation of the slice. As written, a diligent researcher must either violate S0.3 or report Stage E as failed. | severity: high | evidence: S0.3: 'Everything else is forbidden. No crates, no partial compiler, no plugin runtime.' Gate 4: 'Pass only if the yeast scenario runs end-to-end, emits a complete replayable receipt'. Gate 6: 'pinned re-execution reproduces exact results'. Receipt emission and re-execution are runtime behaviours; no permitted activity produces a runtime. The preamble's own critique #1 (rev. 1 'gates work, then demands everything at once') applies unchanged to this residue.
- target: S14 'epistemically interesting outcome' — carbon balance | objection: The supplied numbers refute not only complete respiration but also the Crabtree explanation the scenario endorses as 'the scientifically correct conclusion'. Observed CO2 is 0.95 L = 38.83 ± 0.84 mmol. The maximum CO2 obtainable from 9.99 mmol glucose given 24 mmol O2 consumed, under any mix of complete respiration (6 CO2, 6 O2 per glucose) and ethanol fermentation (2 CO2, 0 O2 per glucose), is 6×4.00 + 2×5.99 = 35.98 mmol = 0.880 L — a 2.85 mmol (0.070 L, ~3.4σ of the observation) shortfall. Equivalently, explaining 38.83 mmol CO2 with 24 mmol O2 requires 11.42 mmol glucose, 12.8σ above the 9.99 ± 0.11 supplied. A system doing the atom conservation the slice itself mandates ('chemistry (composition, atom and charge conservation, stoichiometry)') would flag the dataset as internally inconsistent — verdict `conflicting` or inputs-suspect — not cleanly `refuted`-for-this-model-with-fermentation-as-the-story. Fix: raise observed CO2 shortfall or lower it below ~0.88 L (e.g. 0.80 ± 0.02 L), or raise glucose supplied, so the endorsed alternative passes mass balance. | severity: high | evidence: Computed above: max CO2 (resp+EtOH ferm) = 35.983 mmol = 0.8803 L vs observed 38.830 ± 0.842 mmol; glucose needed = 11.415 mmol vs 9.991 ± 0.111 supplied (12.8σ). The prompt says 'preserve the intended epistemic structure' — these numbers break that structure: the centrepiece verdict narrative fails the slice's own conservation check.
- target: S0.5 'fail closed' vs S14 mandated outcome and S10 response schema | objection: S0.5 defines fail-closed as 'No result artifact is emitted at all — not a partial one, not one with a warning field set', yet S14 mandates emitting a `refuted` verdict 'with a warning that the declared model's validity regime (low glucose) may not have been met' — a warning-bearing artifact produced under missing context (glucose concentration is not in the measured-inputs table; neither is culture volume, organism or strain, though the slice 'must exercise' organism/strain/metabolic regime). Meanwhile property 4 says a structurally identical missing-context case (water-vapour declaration) MUST fail closed. The prompt gives no principle distinguishing which undeclared context aborts and which downgrades to a warning, and S10's response schema ('warnings; partial or unsupported portions') institutionalizes exactly the partial artifacts S0.5 bans. Property 1's required output — 'a statement that the determination is not resolved at the stated uncertainty, with the margin reported' — is likewise a qualified partial result the S0.5 definition forbids emitting. The absolutist definition forecloses the correct designs three other sections demand. | severity: high | evidence: Direct quotes conflict: S0.5 bullet 1 vs S14 'The correct verdict is refuted ... with a warning' and S14 property 4 'missing required context must fail closed'. The regime variable (glucose concentration) appears nowhere in the measured inputs, so the mandated warning is precisely a verdict computed over undeclared context — the case S0.5 says must produce no artifact.
- target: S0.4 F1 and F4 — pre-decided criteria, and F1 contradicts 'narrowly adapted' | objection: F1 and F4 are not spike questions; they are already settled by Nibli's documented design, so the 'commit before you look' framing is pre-registration theatre — the author has plainly already looked (the prompt names nibli-lexicon and knows the compute contract shape). Nibli's vocabulary is a committed Rust corpus with fail-closed resolution and no runtime predicate declaration (the §14.1 `pred` form is explicitly v2/unimplemented), so F1's text-API reading fires deterministically; the compute contract returns `{"result":true}`-style Booleans, proof-local, so F4's first conjunct is documentation. Worse, F1 is internally incoherent with the hypothesis: the hypothesis allows 'narrowly adapted' = 'additive public API surface', and an additive declare-predicate API is exactly the cure for F1 — so F1 firing does not falsify the hypothesis as stated. And F1's own two sentences disagree with Spike 2: the spike tests 'current text APIs' while F1 says 'a public Nibli API' — a native embedder can hand nibli-reason a raw LogicBuffer carrying arbitrary relation names without touching crate source, flipping F1's truth value depending on which sentence you read. A criterion whose outcome depends on which clause the researcher privileges is not decisive; the researcher can 'kill' or 'save' the hypothesis at will, which is the same theatre rev. 1 was criticized for. | severity: high | evidence: Repo docs (CLAUDE.md/NIBLI_KR §5): 'an unknown name is refused without guessing arity', 'uncurated compounds fail closed', corpus is committed Rust source, 'pred ... that v2 feature declares new vocabulary'; compute protocol: 'Response: {"result":true} or {"error":...}'. F1 text: 'requires editing nibli-lexicon or any other Nibli crate source' vs 'narrowly adapted' text: 'changes confined to additive public API surface ... upstreamable as a non-breaking minor version' — the criterion tests the current state while the hypothesis quantifies over an adapted state.
- target: S14 predicted CO2 '1.47 ± 0.02 L' — silent limiting-reagent resolution | objection: The prompt commits the exact failure its property 1 defines. Property 1: the limiting reagent is unresolved at ~1σ and 'a system that picks one silently has failed'. But 'Predicted CO2 under the model is roughly 1.47 ± 0.02 L' is the glucose-limiting prediction only; the O2-limiting prediction is 1.500 L (61.31 mmol CO2 × 24.465 L/mol), which lies OUTSIDE the stated ±0.02 band. The honest prediction under the scenario's own ambiguity is a range ~1.47–1.50 L (the two hypotheses differ by 0.033 L, larger than the quoted uncertainty). Any implementer or grader who takes '1.47 ± 0.02' as the expected system output will build/reward exactly the silent resolution the gate ('reports the limiting-reagent ambiguity rather than resolving it silently') is supposed to catch. The refutation itself survives (both 1.47 and 1.50 are far from 0.95), but the exemplar number is wrong as a specification of correct behaviour. | severity: high | evidence: Computed: glucose-limited 1.4667 ± 0.0180 L; O2-limited 1.5000 L; band 1.47 ± 0.02 excludes 1.50. Property 1 text: 'The correct output is not "glucose is limiting"... A system that picks one silently has failed' — followed two subsections later by a prediction that picks glucose silently.
- target: S1 result taxonomy — axes are not orthogonal and the enum is incomplete | objection: The two 'axes' are not axes: Axis 2 is defined as 'why NO verdict was produced', making verdict and disposition mutually exclusive — a sum type of 7+7 values wearing a product type's clothing. Contrast S10's trust model, where a result genuinely carries 'one value on each axis'. Consequences: (a) S14 property 1's required output — a well-formed question answered with 'not resolved at stated uncertainty, margin reported' — has no home: `numerically_indeterminate` is a disposition (no verdict), but the prompt demands a substantive quantitative output, and `unknown` discards the margin; (b) `conflicting` is really a disposition (why no verdict) sitting on the verdict axis, and its boundary with `invalid_model`'s 'unsatisfiable constraints' is undefined; (c) completeness fails against the prompt's own S10: if obligations can be cancelled (S10 requires deciding this), `cancelled` is missing; a solver reachable but forbidden by the request's trust policy is neither `backend_unavailable` nor `unsupported` — `policy_refused` is missing; (d) a claim established by validated numerics (a rigorous enclosure certifying an inequality — S0.5 level 2, the product's core path) is unclassifiable: `proved` demands a 'checkable proof object under declared axioms', `supported` is 'empirical evidence' by definition. The verdict axis forecloses the main verdict this product will actually emit. Gate 0 requires the taxonomy be 'complete'; by these counterexamples it is not. | severity: high | evidence: Axis 2 header: 'disposition (why no verdict was produced)' — grammatically excludes co-occurrence with a verdict. S10: 'whether an obligation can be cancelled' with no corresponding status. S0.5 level 2 'Enclosed — provably contains the true value' vs verdict definitions of `proved` (proof object) and `supported` (empirical): an enclosure is a proof-strength computational result that is neither.
- target: S14 property 2 — the correlated-uncertainty case does not bite as constructed | objection: Property 2 claims T,P correlation 'misstates the uncertainty on the respiratory quotient' because 'both the O2 and the CO2 volume-to-moles conversions use the same T and P'. But the data table gives O2 consumed directly in mmol (24 ± 1), not as a volume — so the RQ (CO2 evolved / O2 consumed) involves exactly ONE gas conversion and the T,P correlation contributes nothing to it. The two conversions that do share T,P (O2 supplied, CO2 observed) are never combined in any quantity the analysis computes (limiting-reagent margin = O2 supplied vs glucose-derived requirement; refutation = glucose-derived prediction vs CO2 observed — one conversion each). Additionally, the quoted 'O2 supplied ≈ 61.3 ± 1.2 mmol' is reproducible only by IGNORING the P and T uncertainties entirely (volume-only propagation gives ±1.23; full independent propagation gives ±1.27 → rounds to 1.3) — the scenario's own headline number drops the very P,T uncertainty terms property 2 declares load-bearing. Fix: state O2 consumed as a volume (~0.59 ± 0.02 L at same T,P) and re-quote sigma as ±1.3. | severity: medium | evidence: Computed: vol-only sigma = 1.226 mmol (matches quoted 1.2); full quadrature = 1.267 mmol. Data table row: 'O2 consumed (observed) | 24 ± 1 mmol' — a molar quantity requiring no T,P. RQ as defined therefore carries T,P through one conversion only; correlation between two conversions cannot 'misstate' it.
- target: S14 properties 3 and 4 — the quantitative 'teeth' claims are overstated or false | objection: Property 3 claims the ideal-gas deviation is 'comparable to the stated measurement uncertainties' and 'introduces an error of the same magnitude as the thing being measured'. It is not: Z(CO2, 298.15 K, 101.325 kPa) ≈ 0.995 (virial B ≈ −125 cm³/mol gives 0.51% deviation; the quoted 0.994/'~0.6%' slightly overstates), while the dominant stated uncertainties are the 2.0% and 2.1% volume bars — the approximation error is 3–4× SMALLER than 1σ of the quantities it affects, shifts the limiting-reagent margin by ~0.04 mmol (vs a 1.44 mmol sigma), and flips no conclusion in the slice. A system that silently assumed ideal gas would produce results indistinguishable from the correct ones at the stated precision, so the scenario cannot demonstrate that the silent-approximation rule 'has teeth' — it demonstrates the opposite. Property 4's claim that 3.2 kPa water vapour (3.13% of P) is 'larger than every stated uncertainty' is false: the O2-consumed bar is 1/24 = 4.2%. Either tighten that bar (e.g. 24.0 ± 0.5 mmol) or drop 'every'. | severity: medium | evidence: Computed: Z = 0.9949–0.9951 for B in [−130,−120] cm³/mol; deviation 0.49–0.53% vs relative bars glc 1.11%, O2 sup 2.00%, CO2 obs 2.11%, O2 cons 4.17%. Non-ideality shift on O2 supplied: 0.040 mmol against a 1.43 mmol combined sigma on the limiting-reagent difference. psat(298.15 K) = 3.169 kPa = 3.13% < 4.17%.
- target: S0.4 F5, F6, F2 — unfireable, boundary-dependent, or gameable | objection: F5 (identifier round-trip injectivity) cannot fire against Nibli at all: the encoding of typed scientific terms into opaque identifiers is designed by the platform, not by Nibli, so the researcher grades their own homework — any competent mangling scheme (serialize the canonical term, or hash + side table) is injective by construction. It is also the only criterion with no assigned spike, contradicting the preamble's claim that each criterion is 'tied to a specific spike and a specific observation'. F6 tests a property that is not Nibli's: per the repo's own docs, fuel is a host budget the native engine does not enforce ('RESOURCE_EXCEEDED is structurally unpinnable'), so Spike 9's outcome is decided by which embedding Spike 8 chooses — native embedding fires F6, the Wasmtime component with fuel + memory caps passes it — and the criterion never fixes the boundary, so it is selectable. F2 clause (b) is trivially avoidable: the wrapper can always read Nibli's FALSE as 'unknown' in the open-world layer, making the criterion unfireable — but that weakening quietly discards the closed-world/NAF machinery that constitutes most of Nibli's reuse value, and no criterion detects THAT failure. The criteria measure 'can it be scoped' when the decision needs 'does scoped reuse retain value'. | severity: medium | evidence: Preamble critique #2: kill criteria 'each tied to a specific spike' — F1→Spike 2, F2→Spike 4, F3→Spike 5, F4→Spikes 6/7, F6→Spike 9, F5→nothing. CLAUDE.md verify-pins entry: 'RESOURCE_EXCEEDED is structurally unpinnable (fuel is a host budget the native engine does not enforce)'. F2(b) requires 'an answer that is correct inside Nibli and wrong in the platform's open-world reading' — unreachable if the platform never assigns open-world meaning to FALSE, which the layering in Spike 4 is free to do.
- target: Gate 0 vs S0.1(2) — the gate depends on an input the process cannot produce | objection: Gate 0 passes only if the capability matrix is 'tied to named competency questions' from 'three to five real people or roles' asking questions 'they would actually ask this week'. A researcher (human or model) cannot conjure real named users; S0.1's escape hatch is 'state the assumption you proceeded under'. But an assumed user roster satisfies Gate 0's letter while reinstating exactly the disease the requirement diagnoses — 'the capability matrix is bounded by nothing and comprehensive reverts to large', now laundered through fictional personas that look like a bound. Either Gate 0 is unpassable without a stakeholder input the prompt cannot supply (blocking all five stages), or it is hollow. The prompt should either supply the users itself (it is the only party who can), or make Gate 0's pass condition explicitly conditional ('pass-under-assumption', flagged as the report's largest risk), or accept synthetic competency questions with a stated derivation. As written it silently mixes a research task with a product-management input and gates the former on the latter. | severity: medium | evidence: S0.1(2): 'Three to five real people or roles ... Without these, the capability matrix is bounded by nothing'; Gate 0: 'Pass only if: comprehensive has a bounded capability matrix tied to named competency questions'. The conjunction is satisfiable only by invented users, which S0.1's own rationale says provide no bound — the pass criterion and its rationale contradict.
- target: S0.5 'semantic fidelity' — the totality requirement forecloses honest encodings | objection: The fidelity test brands an encoding as lossy if a well-formed domain operation 'cannot be expressed as a total, type-checked operation in the IR'. But the flagship examples are not total in their own domains: differentiation is partial (non-differentiable points), symbolic integration is partial in any effective sense (no algorithm produces closed forms for all elementary integrands), atom-mapping is non-unique and partial. An IR can make these 'total' only by Mathematica-style totalization — returning an unevaluated/symbolic residual — which is arguably the 'escape hatch' the test bans, or by restricting domains with types the surrounding sections never require. Applied as written, EVERY honest IR fails the test on integration, so Gate 1 ('judged by the §0.5 fidelity test') is either failed universally or passed by quietly reinterpreting 'total' — and a definition that must be reinterpreted to be usable is exactly the handwaving the prompt bans. Fix: replace 'total' with 'partial with a typed, explicit domain-of-definition or a first-class unevaluated form', which is the actual correct answer the current wording forecloses. | severity: medium | evidence: S0.5: 'cannot be expressed as a total, type-checked operation in the IR, but requires either an escape hatch, an untyped string, or a convention'. Counterexample: no total type-checked `integrate : Expr -> Expr` exists with the intended semantics (Risch handles elementary integrands only under decidability caveats); the standard resolution (return an inert `Integrate[...]` form) is a convention about unevaluated forms — the thing the test's ban on escape hatches most naturally reads as prohibiting.
- target: S2 epistemology + document self-reference integrity | objection: Two related defects. (1) S2 demands seventeen 'structurally distinct' categories, but S10 then factorizes results into verification-strength × provenance, and most S2 categories are just points in that product ('exact computed result' = deterministic trusted computation × derived internally; 'imported database assertion' = unverified assertion × imported from versioned database — S10 even says so). No mapping between the flat 17 and the two axes is required anywhere, so the prompt commits the axis-mixing sin it convicts rev. 1 of, in the section right before the fix; 'observation' vs 'measurement' is never distinguished at all. (2) The document fails its own citation standard: there are two sections numbered '1' (Role and Product boundary); §0.2 appears after §1; the anti-handwaving list claims the nine floating terms are 'all of which ... §0.5 now defines' when decision-grade/materially different/narrowly adapted live in §0.4, 'clean' and 'leakage' in §9, and 'actively maintained' in §11; and the mandated 'Where this prompt is wrong' section ('Nothing to add is not acceptable') reinstates the manufactured-objection theatre the prompt diagnoses in rev. 1's 'try to falsify'. For a prompt whose acceptance test is span-level cross-reference precision, its own cross-references do not resolve. | severity: low | evidence: S10: 'A database import is unverified assertion × imported from versioned database' — a direct factorization of an S2 category, with no required mapping. Section sequence in the file: '## 1. Role' (line 73) and '## 1. Product boundary' (line 274); '## 0.2' at line 94 after §1. Anti-handwaving list: 'all of which rev. 1 used undefined and §0.5 now defines' — false for at least five of the nine terms.

## arithmetic_check

All computations independent, R = 8.314462618 J/(mol·K), ideal gas unless noted. (1) Glucose: 1.80/180.156 = 9.9913 mmol; sigma 0.02/180.156 = 0.111 mmol. AGREE with '9.99 ± 0.11 mmol'. (2) O2 supplied: n = PV/RT = 101325 × 1.50e-3 / (8.3145 × 298.15) = 61.31 mmol. AGREE on 61.3. Sigma: volume-only (2.00%) gives ±1.23 → matches the quoted ±1.2; full quadrature incl. P (0.49%) and T (0.17%) gives 2.07% = ±1.27 → rounds to ±1.3. PARTIAL DISAGREE: the quoted ±1.2 is consistent only with dropping the P,T terms the scenario's property 2 declares load-bearing. (3) Required O2: 6 × 9.9913 = 59.95 ± 6×0.111 = ±0.67 mmol. AGREE with '59.9 ± 0.7'. (4) Overlap: difference 61.31 − 59.95 = 1.36 mmol; combined sigma sqrt(1.27² + 0.67²) = 1.43 → 0.95σ (0.99σ using the quoted 1.2). AGREE with 'roughly one standard deviation'. (5) Predicted CO2: 59.95 mmol × RT/P (24.465 L/mol) = 1.467 L; sigma from glucose (1.11%) + P + T = 1.23% = ±0.018 L. AGREE with '1.47 ± 0.02 L' AS THE GLUCOSE-LIMITING PREDICTION — but the O2-limiting prediction is 1.500 L, outside that band, so quoting 1.47 ± 0.02 silently resolves the ambiguity property 1 forbids resolving (objection filed). (6) CO2 compressibility: Z = 1 + BP/RT with B(298 K) ≈ −120 to −130 cm³/mol gives Z = 0.9947–0.9951, i.e. ~0.995 and a 0.49–0.53% deviation. MOSTLY AGREE: 'roughly 0.994' / '~0.6%' slightly overstates (better: 0.995 / ~0.5%); more importantly the deviation is 3–4× smaller than the 2.0–2.1% volume uncertainties, so 'comparable to the stated measurement uncertainties' fails against the dominant bars (objection filed). (7) Water vapour: psat(298.15 K) = 3.169 kPa = 3.13% of 101.325 kPa. AGREE with 'roughly 3.2 kPa' / 'about 3%'; but it is NOT 'larger than every stated uncertainty' — O2 consumed carries 1/24 = 4.2%. (8) 0.95 L observed = 38.83 ± 0.84 mmol CO2; predicted 1.47 L vs 0.95 L observed: refutation stands under either limiting-reagent hypothesis. ADDITIONAL FINDING from the check: carbon balance breaks the endorsed Crabtree narrative — with 24 mmol O2 consumed, max CO2 from 9.99 mmol glucose under any respiration+ethanol-fermentation mix is 6×4.00 + 2×5.99 = 35.98 mmol = 0.880 L, a 3.4σ shortfall vs the 38.83 ± 0.84 mmol observed; equivalently the observed CO2 requires 11.42 mmol glucose, 12.8σ above supply. The numbers as given refute the scenario's own 'scientifically correct conclusion', not just the declared model.

